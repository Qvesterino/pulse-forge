/* Ultina worklet — generated by scripts/build-ultina-worklet.mjs from src/effects/ultina-worklet.entry.js + vendored ultina-core. Do not edit. */
"use strict";
(() => {
  // src/effects/ultina-core/contracts/moduleTypes.ts
  var MODULE_TYPES = [
    "eq",
    "comp",
    "gate",
    "exciter",
    "transient",
    "clipper",
    "density",
    "sculptor",
    "phase",
    "unmask"
  ];
  function isModuleType(value) {
    return MODULE_TYPES.includes(value);
  }

  // src/effects/ultina-core/contracts/parameterIds.ts
  var GLOBAL_BYPASS_ID = "global.bypass";
  var GLOBAL_INPUT_GAIN_DB_ID = "global.inputGainDb";
  var GLOBAL_OUTPUT_GAIN_DB_ID = "global.outputGainDb";
  var GLOBAL_MIX_ID = "global.mix";
  var GLOBAL_QUALITY_MODE_ID = "global.qualityMode";
  var GLOBAL_DELTA_LISTEN_ID = "global.deltaListen";
  var GLOBAL_GAIN_MATCH_ENABLED_ID = "global.gainMatchEnabled";
  var GLOBAL_AB_SLOT_ID = "global.abSlot";
  var GLOBAL_AUTOGAIN_TARGET_LUFS_ID = "global.autogainTargetLufs";
  var EQ_ENABLED_ID = "eq.enabled";
  var EQ_CHANNEL_MODE_ID = "eq.channelMode";
  var EQ_SOFT_SATURATION_ID = "eq.softSaturation";
  var EQ_LEARN_ACTIVE_ID = "eq.learnActive";
  var EQ_MASKING_METER_ENABLED_ID = "eq.maskingMeterEnabled";
  var EQ_SIDECHAIN_ENABLED_ID = "eq.sidechainEnabled";
  var EQ_MAX_BANDS = 12;
  var COMP_ENABLED_ID = "comp.enabled";
  var COMP_MODE_ID = "comp.mode";
  var COMP_DETECTION_MODE_ID = "comp.detectionMode";
  var COMP_THRESHOLD_DB_ID = "comp.thresholdDb";
  var COMP_RATIO_ID = "comp.ratio";
  var COMP_ATTACK_MS_ID = "comp.attackMs";
  var COMP_RELEASE_MS_ID = "comp.releaseMs";
  var COMP_KNEE_DB_ID = "comp.kneeDb";
  var COMP_MAKEUP_DB_ID = "comp.makeupDb";
  var COMP_MAKEUP_AUTO_ID = "comp.makeupAuto";
  var COMP_AUTO_RELEASE_ID = "comp.autoRelease";
  var COMP_MIX_ID = "comp.mix";
  var COMP_SIDECHAIN_ENABLED_ID = "comp.sidechainEnabled";
  var COMP_SIDECHAIN_HPF_HZ_ID = "comp.sidechainHpfHz";
  var COMP_BAND_COUNT_ID = "comp.bandCount";
  var COMP_CROSSOVER_HZ1_ID = "comp.crossoverHz1";
  var COMP_CROSSOVER_HZ2_ID = "comp.crossoverHz2";
  var COMP_CROSSOVER_MODE_ID = "comp.crossoverMode";
  var COMP_CHANNEL_MODE_ID = "comp.channelMode";
  var COMP_CROSSOVER_LEARN_ID = "comp.crossoverLearn";
  var COMP_DELTA_ID = "comp.delta";
  var COMP_AUTO_LEARN_THRESHOLD_ID = "comp.autoLearnThreshold";
  var COMP_DETECTOR_HPF_HZ_ID = "comp.detectorHpfHz";
  var GATE_ENABLED_ID = "gate.enabled";
  var GATE_RANGE_DB_ID = "gate.rangeDb";
  var GATE_ATTACK_MS_ID = "gate.attackMs";
  var GATE_HOLD_MS_ID = "gate.holdMs";
  var GATE_RELEASE_MS_ID = "gate.releaseMs";
  var GATE_HYSTERESIS_DB_ID = "gate.hysteresisDb";
  var GATE_SIDECHAIN_HPF_HZ_ID = "gate.sidechainHpfHz";
  var GATE_BAND_COUNT_ID = "gate.bandCount";
  var GATE_CROSSOVER_HZ1_ID = "gate.crossoverHz1";
  var GATE_CROSSOVER_HZ2_ID = "gate.crossoverHz2";
  var GATE_CROSSOVER_MODE_ID = "gate.crossoverMode";
  var GATE_CHANNEL_MODE_ID = "gate.channelMode";
  var GATE_CROSSOVER_LEARN_ID = "gate.crossoverLearn";
  var GATE_DELTA_ID = "gate.delta";
  var EXCITER_ENABLED_ID = "exciter.enabled";
  var EXCITER_TRASH_MODE_ID = "exciter.trashMode";
  var EXCITER_TUBE_AMOUNT_ID = "exciter.tubeAmount";
  var EXCITER_WARM_AMOUNT_ID = "exciter.warmAmount";
  var EXCITER_TAPE_AMOUNT_ID = "exciter.tapeAmount";
  var EXCITER_RETRO_AMOUNT_ID = "exciter.retroAmount";
  var EXCITER_OVERDRIVE_AMOUNT_ID = "exciter.overdriveAmount";
  var EXCITER_SCREAM_AMOUNT_ID = "exciter.screamAmount";
  var EXCITER_CLIPPER_AMOUNT_ID = "exciter.clipperAmount";
  var EXCITER_SCRATCH_AMOUNT_ID = "exciter.scratchAmount";
  var EXCITER_TONE_SLIDER_ID = "exciter.toneSlider";
  var EXCITER_PRE_EMPHASIS_MODE_ID = "exciter.preEmphasisMode";
  var EXCITER_BAND_COUNT_ID = "exciter.bandCount";
  var EXCITER_CROSSOVER_HZ1_ID = "exciter.crossoverHz1";
  var EXCITER_CROSSOVER_HZ2_ID = "exciter.crossoverHz2";
  var EXCITER_CROSSOVER_MODE_ID = "exciter.crossoverMode";
  var EXCITER_CHANNEL_MODE_ID = "exciter.channelMode";
  var EXCITER_CROSSOVER_LEARN_ID = "exciter.crossoverLearn";
  var EXCITER_OVERSAMPLING_ID = "exciter.oversampling";
  var EXCITER_MIX_ID = "exciter.mix";
  var EXCITER_DELTA_ID = "exciter.delta";
  var EXCITER_TUBE_ASYM_AMOUNT_ID = "exciter.tubeAsymAmount";
  var TRANSIENT_ENABLED_ID = "transient.enabled";
  var TRANSIENT_GLOBAL_MODE_ID = "transient.globalMode";
  var TRANSIENT_CONTOUR_SHAPE_ID = "transient.contourShape";
  var TRANSIENT_ATTACK_AMOUNT_ID = "transient.attackAmount";
  var TRANSIENT_SUSTAIN_AMOUNT_ID = "transient.sustainAmount";
  var TRANSIENT_BAND_COUNT_ID = "transient.bandCount";
  var TRANSIENT_CROSSOVER_HZ1_ID = "transient.crossoverHz1";
  var TRANSIENT_CROSSOVER_HZ2_ID = "transient.crossoverHz2";
  var TRANSIENT_CROSSOVER_MODE_ID = "transient.crossoverMode";
  var TRANSIENT_CHANNEL_MODE_ID = "transient.channelMode";
  var TRANSIENT_CROSSOVER_LEARN_ID = "transient.crossoverLearn";
  var TRANSIENT_DELTA_ID = "transient.delta";
  var CLIPPER_ENABLED_ID = "clipper.enabled";
  var CLIPPER_CEILING_DB_ID = "clipper.ceilingDb";
  var CLIPPER_KNEE_DB_ID = "clipper.kneeDb";
  var CLIPPER_DRIVE_DB_ID = "clipper.driveDb";
  var CLIPPER_BAND_COUNT_ID = "clipper.bandCount";
  var CLIPPER_CROSSOVER_HZ1_ID = "clipper.crossoverHz1";
  var CLIPPER_CROSSOVER_HZ2_ID = "clipper.crossoverHz2";
  var CLIPPER_CROSSOVER_MODE_ID = "clipper.crossoverMode";
  var CLIPPER_CHANNEL_MODE_ID = "clipper.channelMode";
  var CLIPPER_CROSSOVER_LEARN_ID = "clipper.crossoverLearn";
  var CLIPPER_OVERSAMPLING_ID = "clipper.oversampling";
  var CLIPPER_DELTA_ID = "clipper.delta";
  var DENSITY_ENABLED_ID = "density.enabled";
  var DENSITY_THRESHOLD_DB_ID = "density.thresholdDb";
  var DENSITY_RANGE_DB_ID = "density.rangeDb";
  var DENSITY_RATIO_ID = "density.ratio";
  var DENSITY_ATTACK_MS_ID = "density.attackMs";
  var DENSITY_RELEASE_MS_ID = "density.releaseMs";
  var DENSITY_BAND_COUNT_ID = "density.bandCount";
  var DENSITY_CROSSOVER_HZ1_ID = "density.crossoverHz1";
  var DENSITY_CROSSOVER_HZ2_ID = "density.crossoverHz2";
  var DENSITY_CROSSOVER_MODE_ID = "density.crossoverMode";
  var DENSITY_CHANNEL_MODE_ID = "density.channelMode";
  var DENSITY_CROSSOVER_LEARN_ID = "density.crossoverLearn";
  var DENSITY_DELTA_ID = "density.delta";
  var SCULPTOR_ENABLED_ID = "sculptor.enabled";
  var SCULPTOR_TARGET_PROFILE_ID = "sculptor.targetProfile";
  var SCULPTOR_AMOUNT_ID = "sculptor.amount";
  var SCULPTOR_LOW_FREQ_BOUNDARY_HZ_ID = "sculptor.lowFreqBoundaryHz";
  var SCULPTOR_HIGH_FREQ_BOUNDARY_HZ_ID = "sculptor.highFreqBoundaryHz";
  var SCULPTOR_DRY_WET_ID = "sculptor.dryWet";
  var SCULPTOR_CHANNEL_MODE_ID = "sculptor.channelMode";
  var SCULPTOR_DELTA_ID = "sculptor.delta";
  var PHASE_ENABLED_ID = "phase.enabled";
  var PHASE_LEARN_ACTIVE_ID = "phase.learnActive";
  var PHASE_ROTATION_DEGREES_ID = "phase.rotationDegrees";
  var PHASE_TIME_SHIFT_MS_ID = "phase.timeShiftMs";
  var PHASE_SIDECHAIN_ENABLED_ID = "phase.sidechainEnabled";
  var PHASE_AUTO_ALIGN_ACTIVE_ID = "phase.autoAlignActive";
  var PHASE_DELTA_ID = "phase.delta";
  var UNMASK_ENABLED_ID = "unmask.enabled";
  var UNMASK_AMOUNT_ID = "unmask.amount";
  var UNMASK_SIDECHAIN_ENABLED_ID = "unmask.sidechainEnabled";
  var UNMASK_MASKING_THRESHOLD_DB_ID = "unmask.maskingThresholdDb";
  var UNMASK_RESPONSE_SPEED_HZ_ID = "unmask.responseSpeedHz";
  var UNMASK_CHANNEL_MODE_ID = "unmask.channelMode";
  var UNMASK_LEARN_ACTIVE_ID = "unmask.learnActive";
  var UNMASK_DELTA_ID = "unmask.delta";
  var UNMASK_ECOSYSTEM_ENABLED_ID = "unmask.ecosystemEnabled";

  // src/effects/ultina-core/contracts/parameterSchema.ts
  function p(id, name, defaultValue, minValue, maxValue, unit, automatable = true, extra) {
    return { id, name, defaultValue, minValue, maxValue, unit, automatable, ...extra };
  }
  var GLOBAL_PARAMS = [
    p(GLOBAL_BYPASS_ID, "Bypass", 0, 0, 1, "boolean"),
    p(GLOBAL_INPUT_GAIN_DB_ID, "Input Gain", 0, -24, 24, "db"),
    p(GLOBAL_OUTPUT_GAIN_DB_ID, "Output Gain", 0, -24, 24, "db"),
    p(GLOBAL_MIX_ID, "Mix", 100, 0, 100, "percent"),
    p(
      GLOBAL_QUALITY_MODE_ID,
      "Quality",
      1,
      0,
      2,
      "enum",
      false,
      { enumValues: ["tracking", "mix", "hq"] }
    ),
    p(GLOBAL_DELTA_LISTEN_ID, "Delta Listen", 0, 0, 1, "boolean", false),
    p(GLOBAL_GAIN_MATCH_ENABLED_ID, "Gain Match", 0, 0, 1, "boolean", false),
    p(
      GLOBAL_AB_SLOT_ID,
      "A/B Slot",
      0,
      0,
      1,
      "enum",
      false,
      { enumValues: ["A", "B"] }
    ),
    p(GLOBAL_AUTOGAIN_TARGET_LUFS_ID, "Autogain Target LUFS", -14, -30, 0, "db", false)
  ];
  var EQ_GLOBAL_PARAMS = [
    p(EQ_ENABLED_ID, "EQ Enabled", 0, 0, 1, "boolean"),
    p(
      EQ_CHANNEL_MODE_ID,
      "EQ Channel Mode",
      0,
      0,
      4,
      "enum",
      true,
      { enumValues: ["stereo", "mid", "side", "transient", "sustain"] }
    ),
    p(EQ_SOFT_SATURATION_ID, "EQ Soft Saturation", 0, 0, 1, "boolean"),
    p(EQ_LEARN_ACTIVE_ID, "EQ Learn", 0, 0, 1, "boolean", false),
    p(EQ_MASKING_METER_ENABLED_ID, "Masking Meter", 0, 0, 1, "boolean", false),
    p(EQ_SIDECHAIN_ENABLED_ID, "EQ Sidechain", 0, 0, 1, "boolean")
  ];
  var EQ_DEFAULT_FREQS = [
    80,
    200,
    350,
    800,
    1500,
    3e3,
    5e3,
    7e3,
    1e4,
    12e3,
    15e3,
    18e3
  ];
  var EQ_SHAPES = [
    "bell",
    "highShelf",
    "lowShelf",
    "highPass",
    "lowPass",
    "notch",
    "tilt",
    "bandPass",
    "flat",
    "dynamicBell",
    "dynamicShelf",
    "dynamicTilt"
  ];
  var EQ_BAND_MODES = ["static", "dynamic", "sidechain"];
  function eqBandParams(bandIndex) {
    const prefix = `eq.band${bandIndex}`;
    return [
      p(`${prefix}.enabled`, `Band ${bandIndex + 1} Enabled`, 0, 0, 1, "boolean"),
      p(
        `${prefix}.freqHz`,
        `Band ${bandIndex + 1} Frequency`,
        EQ_DEFAULT_FREQS[bandIndex],
        20,
        2e4,
        "hz"
      ),
      p(`${prefix}.gainDb`, `Band ${bandIndex + 1} Gain`, 0, -18, 18, "db"),
      p(`${prefix}.q`, `Band ${bandIndex + 1} Q`, 1, 0.1, 24, "ratio"),
      p(
        `${prefix}.shape`,
        `Band ${bandIndex + 1} Shape`,
        0,
        0,
        11,
        "enum",
        true,
        { enumValues: EQ_SHAPES }
      ),
      p(
        `${prefix}.mode`,
        `Band ${bandIndex + 1} Mode`,
        0,
        0,
        2,
        "enum",
        true,
        { enumValues: EQ_BAND_MODES }
      ),
      p(`${prefix}.dynamicRangeDb`, `Band ${bandIndex + 1} Dyn Range`, 6, 0, 18, "db"),
      p(`${prefix}.dynamicThresholdDb`, `Band ${bandIndex + 1} Dyn Threshold`, -24, -60, 0, "db"),
      p(`${prefix}.sidechainEnabled`, `Band ${bandIndex + 1} Sidechain`, 0, 0, 1, "boolean"),
      p(`${prefix}.solo`, `Band ${bandIndex + 1} Solo`, 0, 0, 1, "boolean", false),
      // Dynamic EQ per-band compressor parameters
      p(`${prefix}.dynamicAttackMs`, `Band ${bandIndex + 1} Dyn Attack`, 15, 0.1, 200, "ms"),
      p(`${prefix}.dynamicReleaseMs`, `Band ${bandIndex + 1} Dyn Release`, 150, 5, 2e3, "ms"),
      p(`${prefix}.dynamicRatio`, `Band ${bandIndex + 1} Dyn Ratio`, 3, 1, 20, "ratio"),
      p(`${prefix}.dynamicKneeDb`, `Band ${bandIndex + 1} Dyn Knee`, 0, 0, 24, "db")
    ];
  }
  var EQ_BAND_PARAMS_ALL = [];
  for (let i = 0; i < EQ_MAX_BANDS; i++) {
    EQ_BAND_PARAMS_ALL.push(...eqBandParams(i));
  }
  var COMP_PARAMS = [
    p(COMP_ENABLED_ID, "Compressor Enabled", 0, 0, 1, "boolean"),
    p(
      COMP_MODE_ID,
      "Comp Mode",
      1,
      0,
      4,
      "enum",
      true,
      { enumValues: ["punch", "modern", "vintage", "opto", "fet"] }
    ),
    p(
      COMP_DETECTION_MODE_ID,
      "Detection",
      1,
      0,
      2,
      "enum",
      true,
      { enumValues: ["peak", "rms", "trueEnvelope"] }
    ),
    p(COMP_THRESHOLD_DB_ID, "Threshold", -20, -60, 0, "db"),
    p(COMP_RATIO_ID, "Ratio", 3, 1, 20, "ratio"),
    p(COMP_ATTACK_MS_ID, "Attack", 10, 0.1, 200, "ms"),
    p(COMP_RELEASE_MS_ID, "Release", 100, 5, 2e3, "ms"),
    p(COMP_KNEE_DB_ID, "Knee", 0, 0, 24, "db"),
    p(COMP_MAKEUP_DB_ID, "Makeup", 0, 0, 24, "db"),
    p(COMP_MAKEUP_AUTO_ID, "Auto Makeup", 0, 0, 1, "boolean", false),
    p(COMP_AUTO_RELEASE_ID, "Auto Release", 0, 0, 1, "boolean"),
    p(COMP_MIX_ID, "Comp Mix", 100, 0, 100, "percent"),
    p(COMP_SIDECHAIN_ENABLED_ID, "Comp Sidechain", 0, 0, 1, "boolean"),
    p(COMP_SIDECHAIN_HPF_HZ_ID, "SC HPF", 20, 20, 2e3, "hz"),
    p(COMP_DETECTOR_HPF_HZ_ID, "Det HPF", 20, 20, 1e3, "hz"),
    p(
      COMP_BAND_COUNT_ID,
      "Comp Bands",
      1,
      1,
      3,
      "enum",
      false,
      { enumValues: ["1", "2", "3"] }
    ),
    p(COMP_CROSSOVER_HZ1_ID, "Comp Xover 1", 250, 20, 2e4, "hz"),
    p(COMP_CROSSOVER_HZ2_ID, "Comp Xover 2", 2500, 20, 2e4, "hz"),
    p(
      COMP_CROSSOVER_MODE_ID,
      "Comp Xover Mode",
      0,
      0,
      1,
      "enum",
      true,
      { enumValues: ["analog", "hybrid"] }
    ),
    p(
      COMP_CHANNEL_MODE_ID,
      "Comp Channel Mode",
      0,
      0,
      4,
      "enum",
      true,
      { enumValues: ["stereo", "mid", "side", "transient", "sustain"] }
    ),
    p(COMP_CROSSOVER_LEARN_ID, "Comp Xover Learn", 0, 0, 1, "boolean", false),
    p(COMP_DELTA_ID, "Comp Delta", 0, 0, 1, "boolean", false),
    p(COMP_AUTO_LEARN_THRESHOLD_ID, "Auto-Learn Threshold", 0, 0, 1, "boolean", false),
    // Per-band threshold overrides
    p("comp.band0.thresholdDb", "Band 1 Threshold", -20, -60, 0, "db"),
    p("comp.band1.thresholdDb", "Band 2 Threshold", -20, -60, 0, "db"),
    p("comp.band2.thresholdDb", "Band 3 Threshold", -20, -60, 0, "db")
  ];
  var GATE_PARAMS = [
    p(GATE_ENABLED_ID, "Gate Enabled", 0, 0, 1, "boolean"),
    p(GATE_RANGE_DB_ID, "Gate Range", -20, -80, 0, "db"),
    p(GATE_ATTACK_MS_ID, "Gate Attack", 1, 0.1, 100, "ms"),
    p(GATE_HOLD_MS_ID, "Gate Hold", 50, 0, 1e3, "ms"),
    p(GATE_RELEASE_MS_ID, "Gate Release", 100, 5, 5e3, "ms"),
    p(GATE_HYSTERESIS_DB_ID, "Gate Hysteresis", 6, 0, 24, "db"),
    p(GATE_SIDECHAIN_HPF_HZ_ID, "Gate SC HPF", 20, 20, 2e3, "hz"),
    p(
      GATE_BAND_COUNT_ID,
      "Gate Bands",
      1,
      1,
      3,
      "enum",
      false,
      { enumValues: ["1", "2", "3"] }
    ),
    p(GATE_CROSSOVER_HZ1_ID, "Gate Xover 1", 250, 20, 2e4, "hz"),
    p(GATE_CROSSOVER_HZ2_ID, "Gate Xover 2", 2500, 20, 2e4, "hz"),
    p(
      GATE_CROSSOVER_MODE_ID,
      "Gate Xover Mode",
      0,
      0,
      1,
      "enum",
      true,
      { enumValues: ["analog", "hybrid"] }
    ),
    p(
      GATE_CHANNEL_MODE_ID,
      "Gate Channel Mode",
      0,
      0,
      4,
      "enum",
      true,
      { enumValues: ["stereo", "mid", "side", "transient", "sustain"] }
    ),
    p(GATE_CROSSOVER_LEARN_ID, "Gate Xover Learn", 0, 0, 1, "boolean", false),
    p(GATE_DELTA_ID, "Gate Delta", 0, 0, 1, "boolean", false),
    // Per-band open/close thresholds
    p("gate.band0.openThresholdDb", "Band 1 Open", -40, -80, 0, "db"),
    p("gate.band0.closeThresholdDb", "Band 1 Close", -50, -80, 0, "db"),
    p("gate.band1.openThresholdDb", "Band 2 Open", -40, -80, 0, "db"),
    p("gate.band1.closeThresholdDb", "Band 2 Close", -50, -80, 0, "db"),
    p("gate.band2.openThresholdDb", "Band 3 Open", -40, -80, 0, "db"),
    p("gate.band2.closeThresholdDb", "Band 3 Close", -50, -80, 0, "db"),
    p("gate.mix", "Gate Mix", 100, 0, 100, "percent")
  ];
  var EXCITER_PARAMS = [
    p(EXCITER_ENABLED_ID, "Exciter Enabled", 0, 0, 1, "boolean"),
    p(EXCITER_TRASH_MODE_ID, "Trash Mode", 0, 0, 1, "boolean"),
    p(EXCITER_TUBE_AMOUNT_ID, "Tube", 0, 0, 100, "percent"),
    p(EXCITER_TUBE_ASYM_AMOUNT_ID, "Tube+", 0, 0, 100, "percent"),
    p(EXCITER_WARM_AMOUNT_ID, "Warm", 30, 0, 100, "percent"),
    p(EXCITER_TAPE_AMOUNT_ID, "Tape", 0, 0, 100, "percent"),
    p(EXCITER_RETRO_AMOUNT_ID, "Retro", 0, 0, 100, "percent"),
    p(EXCITER_OVERDRIVE_AMOUNT_ID, "Overdrive", 0, 0, 100, "percent"),
    p(EXCITER_SCREAM_AMOUNT_ID, "Scream", 0, 0, 100, "percent"),
    p(EXCITER_CLIPPER_AMOUNT_ID, "Clipper", 0, 0, 100, "percent"),
    p(EXCITER_SCRATCH_AMOUNT_ID, "Scratch", 0, 0, 100, "percent"),
    p(EXCITER_TONE_SLIDER_ID, "Tone", 0, -100, 100, "generic"),
    p(
      EXCITER_PRE_EMPHASIS_MODE_ID,
      "Pre-Emphasis",
      0,
      0,
      3,
      "enum",
      true,
      { enumValues: ["full", "defined", "clean", "flat"] }
    ),
    p(
      EXCITER_BAND_COUNT_ID,
      "Exciter Bands",
      1,
      1,
      3,
      "enum",
      false,
      { enumValues: ["1", "2", "3"] }
    ),
    p(EXCITER_CROSSOVER_HZ1_ID, "Exciter Xover 1", 2e3, 20, 2e4, "hz"),
    p(EXCITER_CROSSOVER_HZ2_ID, "Exciter Xover 2", 8e3, 20, 2e4, "hz"),
    p(
      EXCITER_CROSSOVER_MODE_ID,
      "Exciter Xover Mode",
      0,
      0,
      1,
      "enum",
      true,
      { enumValues: ["analog", "hybrid"] }
    ),
    p(
      EXCITER_CHANNEL_MODE_ID,
      "Exciter Channel Mode",
      0,
      0,
      4,
      "enum",
      true,
      { enumValues: ["stereo", "mid", "side", "transient", "sustain"] }
    ),
    p(EXCITER_CROSSOVER_LEARN_ID, "Exciter Xover Learn", 0, 0, 1, "boolean", false),
    p(EXCITER_OVERSAMPLING_ID, "Exciter Oversampling", 1, 0, 1, "boolean"),
    p(EXCITER_MIX_ID, "Exciter Mix", 50, 0, 100, "percent"),
    p(EXCITER_DELTA_ID, "Exciter Delta", 0, 0, 1, "boolean", false)
  ];
  var TRANSIENT_PARAMS = [
    p(TRANSIENT_ENABLED_ID, "Transient Enabled", 0, 0, 1, "boolean"),
    p(
      TRANSIENT_GLOBAL_MODE_ID,
      "Global Mode",
      1,
      0,
      2,
      "enum",
      true,
      { enumValues: ["precise", "balanced", "loose"] }
    ),
    p(
      TRANSIENT_CONTOUR_SHAPE_ID,
      "Contour Shape",
      1,
      0,
      2,
      "enum",
      true,
      { enumValues: ["sharp", "medium", "smooth"] }
    ),
    p(TRANSIENT_ATTACK_AMOUNT_ID, "Attack", 0, -100, 100, "generic"),
    p(TRANSIENT_SUSTAIN_AMOUNT_ID, "Sustain", 0, -100, 100, "generic"),
    p(
      TRANSIENT_BAND_COUNT_ID,
      "Transient Bands",
      1,
      1,
      3,
      "enum",
      false,
      { enumValues: ["1", "2", "3"] }
    ),
    p(TRANSIENT_CROSSOVER_HZ1_ID, "Transient Xover 1", 250, 20, 2e4, "hz"),
    p(TRANSIENT_CROSSOVER_HZ2_ID, "Transient Xover 2", 2500, 20, 2e4, "hz"),
    p(
      TRANSIENT_CROSSOVER_MODE_ID,
      "Transient Xover Mode",
      0,
      0,
      1,
      "enum",
      true,
      { enumValues: ["analog", "hybrid"] }
    ),
    p(
      TRANSIENT_CHANNEL_MODE_ID,
      "Transient Channel Mode",
      0,
      0,
      2,
      "enum",
      true,
      { enumValues: ["stereo", "mid", "side"] }
    ),
    p(TRANSIENT_CROSSOVER_LEARN_ID, "Transient Xover Learn", 0, 0, 1, "boolean", false),
    p(TRANSIENT_DELTA_ID, "Transient Delta", 0, 0, 1, "boolean", false),
    p("transient.mix", "Transient Mix", 100, 0, 100, "percent")
  ];
  var CLIPPER_PARAMS = [
    p(CLIPPER_ENABLED_ID, "Clipper Enabled", 0, 0, 1, "boolean"),
    p(CLIPPER_CEILING_DB_ID, "Ceiling", -1, -24, 0, "db"),
    p(CLIPPER_KNEE_DB_ID, "Knee", 2, 0, 12, "db"),
    p(CLIPPER_DRIVE_DB_ID, "Drive", 0, 0, 24, "db"),
    p(
      CLIPPER_BAND_COUNT_ID,
      "Clipper Bands",
      1,
      1,
      3,
      "enum",
      false,
      { enumValues: ["1", "2", "3"] }
    ),
    p(CLIPPER_CROSSOVER_HZ1_ID, "Clipper Xover 1", 250, 20, 2e4, "hz"),
    p(CLIPPER_CROSSOVER_HZ2_ID, "Clipper Xover 2", 2500, 20, 2e4, "hz"),
    p(
      CLIPPER_CROSSOVER_MODE_ID,
      "Clipper Xover Mode",
      0,
      0,
      1,
      "enum",
      true,
      { enumValues: ["analog", "hybrid"] }
    ),
    p(
      CLIPPER_CHANNEL_MODE_ID,
      "Clipper Channel Mode",
      0,
      0,
      4,
      "enum",
      true,
      { enumValues: ["stereo", "mid", "side", "transient", "sustain"] }
    ),
    p(CLIPPER_CROSSOVER_LEARN_ID, "Clipper Xover Learn", 0, 0, 1, "boolean", false),
    p(CLIPPER_OVERSAMPLING_ID, "Clipper Oversampling", 1, 0, 1, "boolean"),
    p(CLIPPER_DELTA_ID, "Clipper Delta", 0, 0, 1, "boolean", false),
    p("clipper.mix", "Clipper Mix", 100, 0, 100, "percent")
  ];
  var DENSITY_PARAMS = [
    p(DENSITY_ENABLED_ID, "Density Enabled", 0, 0, 1, "boolean"),
    p(DENSITY_THRESHOLD_DB_ID, "Threshold", -30, -60, 0, "db"),
    p(DENSITY_RANGE_DB_ID, "Range", 6, 0, 24, "db"),
    p(DENSITY_RATIO_ID, "Ratio", 2, 1, 10, "ratio"),
    p(DENSITY_ATTACK_MS_ID, "Attack", 10, 0.5, 200, "ms"),
    p(DENSITY_RELEASE_MS_ID, "Release", 150, 10, 2e3, "ms"),
    p(
      DENSITY_BAND_COUNT_ID,
      "Density Bands",
      1,
      1,
      3,
      "enum",
      false,
      { enumValues: ["1", "2", "3"] }
    ),
    p(DENSITY_CROSSOVER_HZ1_ID, "Density Xover 1", 250, 20, 2e4, "hz"),
    p(DENSITY_CROSSOVER_HZ2_ID, "Density Xover 2", 2500, 20, 2e4, "hz"),
    p(
      DENSITY_CROSSOVER_MODE_ID,
      "Density Xover Mode",
      0,
      0,
      1,
      "enum",
      true,
      { enumValues: ["analog", "hybrid"] }
    ),
    p(
      DENSITY_CHANNEL_MODE_ID,
      "Density Channel Mode",
      0,
      0,
      2,
      "enum",
      true,
      { enumValues: ["stereo", "mid", "side"] }
    ),
    p(DENSITY_CROSSOVER_LEARN_ID, "Density Xover Learn", 0, 0, 1, "boolean", false),
    p(DENSITY_DELTA_ID, "Density Delta", 0, 0, 1, "boolean", false),
    p("density.mix", "Density Mix", 100, 0, 100, "percent")
  ];
  var SCULPTOR_PARAMS = [
    p(SCULPTOR_ENABLED_ID, "Sculptor Enabled", 0, 0, 1, "boolean"),
    p(
      SCULPTOR_TARGET_PROFILE_ID,
      "Target",
      5,
      0,
      5,
      "enum",
      true,
      { enumValues: ["guitar", "bass", "kick", "piano", "snare", "speech"] }
    ),
    p(SCULPTOR_AMOUNT_ID, "Amount", 50, 0, 100, "percent"),
    p(SCULPTOR_LOW_FREQ_BOUNDARY_HZ_ID, "Low Boundary", 20, 20, 1e3, "hz"),
    p(SCULPTOR_HIGH_FREQ_BOUNDARY_HZ_ID, "High Boundary", 16e3, 1e3, 2e4, "hz"),
    p(SCULPTOR_DRY_WET_ID, "Dry/Wet", 100, 0, 100, "percent"),
    p(
      SCULPTOR_CHANNEL_MODE_ID,
      "Sculptor Channel Mode",
      0,
      0,
      2,
      "enum",
      true,
      { enumValues: ["stereo", "mid", "side"] }
    ),
    p(SCULPTOR_DELTA_ID, "Sculptor Delta", 0, 0, 1, "boolean", false)
  ];
  var PHASE_PARAMS = [
    p(PHASE_ENABLED_ID, "Phase Enabled", 0, 0, 1, "boolean"),
    p(PHASE_LEARN_ACTIVE_ID, "Phase Learn", 0, 0, 1, "boolean", false),
    p(PHASE_ROTATION_DEGREES_ID, "Rotation", 0, -180, 180, "degrees"),
    p(PHASE_TIME_SHIFT_MS_ID, "Time Shift", 0, -50, 50, "ms"),
    p(PHASE_SIDECHAIN_ENABLED_ID, "Phase Sidechain", 0, 0, 1, "boolean"),
    p(PHASE_AUTO_ALIGN_ACTIVE_ID, "Auto Align", 0, 0, 1, "boolean", false),
    p(PHASE_DELTA_ID, "Phase Delta", 0, 0, 1, "boolean", false),
    p("phase.mix", "Phase Mix", 100, 0, 100, "percent")
  ];
  var UNMASK_PARAMS = [
    p(UNMASK_ENABLED_ID, "Unmask Enabled", 0, 0, 1, "boolean"),
    p(UNMASK_AMOUNT_ID, "Amount", 50, 0, 100, "percent"),
    p(UNMASK_SIDECHAIN_ENABLED_ID, "Unmask Sidechain", 0, 0, 1, "boolean"),
    p(UNMASK_MASKING_THRESHOLD_DB_ID, "Masking Threshold", -15, -40, 0, "db"),
    p(UNMASK_RESPONSE_SPEED_HZ_ID, "Response Speed", 5, 0.5, 50, "hz"),
    p(
      UNMASK_CHANNEL_MODE_ID,
      "Unmask Channel Mode",
      0,
      0,
      4,
      "enum",
      true,
      { enumValues: ["stereo", "mid", "side", "transient", "sustain"] }
    ),
    p(UNMASK_LEARN_ACTIVE_ID, "Unmask Learn", 0, 0, 1, "boolean", false),
    p(UNMASK_DELTA_ID, "Unmask Delta", 0, 0, 1, "boolean", false),
    p(UNMASK_ECOSYSTEM_ENABLED_ID, "Unmask Ecosystem", 0, 0, 1, "boolean", false),
    p("unmask.mix", "Unmask Mix", 100, 0, 100, "percent")
  ];
  var ALL_PARAMS = [
    ...GLOBAL_PARAMS,
    ...EQ_GLOBAL_PARAMS,
    ...EQ_BAND_PARAMS_ALL,
    ...COMP_PARAMS,
    ...GATE_PARAMS,
    ...EXCITER_PARAMS,
    ...TRANSIENT_PARAMS,
    ...CLIPPER_PARAMS,
    ...DENSITY_PARAMS,
    ...SCULPTOR_PARAMS,
    ...PHASE_PARAMS,
    ...UNMASK_PARAMS
  ];
  var PARAM_BY_ID = new Map(
    ALL_PARAMS.map((d) => [d.id, d])
  );
  function buildDefaultParams() {
    const result = {};
    for (const def of ALL_PARAMS) {
      result[def.id] = def.defaultValue;
    }
    return result;
  }
  function clampParam(id, value) {
    const def = PARAM_BY_ID.get(id);
    if (!def) return Number.isFinite(value) ? value : 0;
    if (!Number.isFinite(value)) return def.defaultValue;
    return Math.max(def.minValue, Math.min(def.maxValue, value));
  }

  // src/effects/ultina-core/contracts/state.ts
  var DEFAULT_MODULE_ORDER = [
    "gate",
    "eq",
    "comp",
    "exciter",
    "transient",
    "density",
    "sculptor",
    "clipper",
    "phase",
    "unmask"
  ];
  var DEFAULT_MODULE_ENABLED = Object.fromEntries(
    DEFAULT_MODULE_ORDER.map((m) => [m, false])
  );

  // src/effects/ultina-core/contracts/moduleGraph.ts
  function createDefaultModuleGraph() {
    return DEFAULT_MODULE_ORDER.map((type) => ({
      type,
      enabled: false
    }));
  }
  function createSnapshot(entries, version = 1) {
    const frozenEntries = Object.freeze(
      entries.map((e) => Object.freeze({ ...e }))
    );
    return {
      entries: frozenEntries,
      version
    };
  }
  function moveModule(entries, fromIndex, toIndex) {
    if (fromIndex < 0 || fromIndex >= entries.length) return [...entries];
    if (toIndex < 0) return [...entries];
    const cap = Math.min(toIndex, entries.length);
    const result = entries.map((e) => ({ ...e }));
    const [moved] = result.splice(fromIndex, 1);
    const target = Math.max(0, Math.min(cap, result.length));
    result.splice(target, 0, moved);
    return result;
  }
  function toggleModule(entries, moduleType) {
    return entries.map(
      (e) => e.type === moduleType ? { ...e, enabled: !e.enabled } : e
    );
  }
  function setModuleEnabled(entries, moduleType, enabled) {
    return entries.map(
      (e) => e.type === moduleType ? { ...e, enabled } : e
    );
  }
  function getActiveModules(entries) {
    return entries.filter((e) => e.enabled);
  }
  function validateGraph(entries) {
    if (!Array.isArray(entries)) return createDefaultModuleGraph();
    const result = [];
    const seen = /* @__PURE__ */ new Set();
    for (const entry of entries) {
      if (typeof entry !== "object" || entry === null) continue;
      const type = entry.type;
      if (typeof type !== "string" || !isModuleType(type)) continue;
      if (seen.has(type)) continue;
      seen.add(type);
      result.push({
        type,
        enabled: Boolean(entry.enabled)
      });
    }
    for (const t of MODULE_TYPES) {
      if (!seen.has(t)) {
        result.push({ type: t, enabled: false });
      }
    }
    return result;
  }

  // src/effects/ultina-core/dsp/moduleGraphRuntime.ts
  var ModuleGraphRuntime = class {
    /** Mutable graph entries (control-thread only). */
    entries;
    /** Current immutable snapshot (published for audio thread). */
    snapshot;
    /** Current active chain snapshot (derived from snapshot). */
    activeChain;
    /** Monotonic version counter. */
    versionCounter = 0;
    constructor(entries) {
      this.entries = entries ? validateGraph(entries) : createDefaultModuleGraph();
      this.snapshot = this.buildSnapshot();
      this.activeChain = this.buildActiveChain();
    }
    // ── Audio-thread-safe reads ──────────────────────────────
    /**
     * Get the current immutable graph snapshot.
     * Safe to call from the audio thread — no locks, no allocation.
     */
    getActiveSnapshot() {
      return this.snapshot;
    }
    /**
     * Get the current active chain (enabled modules only).
     * Safe to call from the audio thread.
     */
    getActiveChain() {
      return this.activeChain;
    }
    /**
     * Get a read-only copy of the full graph entries (including disabled modules).
     * Allocates — use only on the control thread.
     */
    getEntries() {
      return this.entries.map((e) => ({ ...e }));
    }
    /**
     * Get the active module types in order.
     */
    getActiveModules() {
      return [...this.activeChain.modules];
    }
    // ── Control-thread mutations ─────────────────────────────
    /**
     * Move a module from one position to another.
     * Returns true if the graph changed.
     */
    moveModule(fromIndex, toIndex) {
      const newEntries = moveModule(this.entries, fromIndex, toIndex);
      if (newEntries.length !== this.entries.length) return false;
      const changed = !entriesEqual(newEntries, this.entries);
      if (changed) {
        this.entries = newEntries;
        this.publish();
      }
      return changed;
    }
    /**
     * Move a module by type to a target index.
     */
    moveModuleByType(moduleType, toIndex) {
      const fromIndex = this.entries.findIndex((e) => e.type === moduleType);
      if (fromIndex === -1) return false;
      return this.moveModule(fromIndex, toIndex);
    }
    /**
     * Toggle a module's enabled state.
     */
    toggleModule(moduleType) {
      const newEntries = toggleModule(this.entries, moduleType);
      const changed = !entriesEqual(newEntries, this.entries);
      if (changed) {
        this.entries = newEntries;
        this.publish();
      }
      return changed;
    }
    /**
     * Set a module's enabled state explicitly.
     */
    setModuleEnabled(moduleType, enabled) {
      const newEntries = setModuleEnabled(this.entries, moduleType, enabled);
      const changed = !entriesEqual(newEntries, this.entries);
      if (changed) {
        this.entries = newEntries;
        this.publish();
      }
      return changed;
    }
    /**
     * Enable a module.
     */
    enableModule(moduleType) {
      return this.setModuleEnabled(moduleType, true);
    }
    /**
     * Disable a module.
     */
    disableModule(moduleType) {
      return this.setModuleEnabled(moduleType, false);
    }
    /**
     * Enable only the specified modules (disable all others).
     */
    setActiveModules(moduleTypes) {
      const typeSet = new Set(moduleTypes);
      const newEntries = this.entries.map((e) => ({
        ...e,
        enabled: typeSet.has(e.type)
      }));
      const changed = !entriesEqual(newEntries, this.entries);
      if (changed) {
        this.entries = newEntries;
        this.publish();
      }
      return changed;
    }
    /**
     * Disable all modules (bypass the entire chain).
     */
    disableAll() {
      const newEntries = this.entries.map((e) => ({ ...e, enabled: false }));
      const changed = !entriesEqual(newEntries, this.entries);
      if (changed) {
        this.entries = newEntries;
        this.publish();
      }
      return changed;
    }
    /**
     * Reset the graph to default order, all disabled.
     */
    reset() {
      this.entries = createDefaultModuleGraph();
      this.publish();
    }
    /**
     * Load graph entries from external data (validated).
     */
    loadEntries(raw) {
      const validated = validateGraph(raw);
      const changed = !entriesEqual(validated, this.entries);
      this.entries = validated;
      if (changed) {
        this.publish();
      }
      return changed;
    }
    /**
     * Get the index of a module type in the graph.
     */
    indexOf(moduleType) {
      return this.entries.findIndex((e) => e.type === moduleType);
    }
    /**
     * Check if a module is enabled.
     */
    isModuleEnabled(moduleType) {
      const entry = this.entries.find((e) => e.type === moduleType);
      return entry?.enabled ?? false;
    }
    /**
     * Check if a module type exists in the graph.
     */
    hasModule(moduleType) {
      return this.entries.some((e) => e.type === moduleType);
    }
    /**
     * Get the current version number.
     */
    getVersion() {
      return this.versionCounter;
    }
    // ── Internal ─────────────────────────────────────────────
    /**
     * Build a new immutable snapshot and publish it atomically.
     */
    publish() {
      this.snapshot = this.buildSnapshot();
      this.activeChain = this.buildActiveChain();
    }
    buildSnapshot() {
      this.versionCounter++;
      return createSnapshot(this.entries, this.versionCounter);
    }
    buildActiveChain() {
      const active = getActiveModules(this.entries).map((e) => e.type);
      return {
        modules: Object.freeze([...active]),
        version: this.versionCounter,
        count: active.length
      };
    }
  };
  function entriesEqual(a, b) {
    if (a.length !== b.length) return false;
    for (let i = 0; i < a.length; i++) {
      if (a[i].type !== b[i].type || a[i].enabled !== b[i].enabled) {
        return false;
      }
    }
    return true;
  }

  // src/effects/ultina-core/contracts/meters.ts
  function createDefaultGlobalMeters() {
    return {
      inputPeakL: -100,
      inputPeakR: -100,
      inputRmsL: -100,
      inputRmsR: -100,
      outputPeakL: -100,
      outputPeakR: -100,
      outputRmsL: -100,
      outputRmsR: -100,
      outputShortTermLufs: -70,
      outputTruePeakDb: -100,
      autoGainCorrectionDb: 0,
      autoGainErrorDb: 0,
      autoGainActive: false,
      qualityMode: 1,
      timestamp: 0,
      outputWaveform: null,
      inputSpectrumDb: null
    };
  }
  function createDefaultMeters() {
    return {
      global: createDefaultGlobalMeters(),
      modules: {},
      learn: { eq: null, crossover: null }
    };
  }

  // src/effects/ultina-core/dsp/primitives.ts
  function clamp(v, min, max) {
    return v < min ? min : v > max ? max : v;
  }
  function dbToLinear(db) {
    return Math.pow(10, db / 20);
  }
  function linearToDb(lin) {
    return lin <= 1e-12 ? -240 : 20 * Math.log10(lin);
  }
  function ampToDb(amp) {
    return amp < 1e-10 ? -200 : 20 * Math.log10(amp);
  }
  function smoothCoef(timeConstantMs, sampleRate2) {
    if (timeConstantMs <= 0) return 1;
    return 1 - Math.exp(-1 / (timeConstantMs / 1e3 * sampleRate2));
  }
  function fastTanh(x) {
    const ax = x < 0 ? -x : x;
    if (ax > 3) return x < 0 ? -1 : 1;
    const x2 = x * x;
    const x4 = x2 * x2;
    return x * (945 + 105 * x2 + x4) / (945 + 420 * x2 + 15 * x4);
  }
  function sanitizeSample(x) {
    if (!Number.isFinite(x)) return 0;
    if (x > 32) return 32;
    if (x < -32) return -32;
    return x;
  }
  var OnePoleSmoother = class {
    alpha = 1;
    value = 0;
    setTimeConstant(ms, sampleRate2) {
      this.alpha = smoothCoef(ms, sampleRate2);
    }
    setCoeff(a) {
      this.alpha = a;
    }
    reset(v = 0) {
      this.value = v;
    }
    process(target) {
      if (!Number.isFinite(target)) return this.value;
      this.value += this.alpha * (target - this.value);
      if (!Number.isFinite(this.value)) this.value = 0;
      return this.value;
    }
    getValue() {
      return this.value;
    }
  };
  function createBiquad(channelCount) {
    return {
      coeffs: { b0: 1, b1: 0, b2: 0, a1: 0, a2: 0 },
      z1: new Array(channelCount).fill(0),
      z2: new Array(channelCount).fill(0)
    };
  }
  function resetBiquad(bq) {
    bq.z1.fill(0);
    bq.z2.fill(0);
  }
  function setLowPass(c, freq, q, sampleRate2) {
    const nyq = sampleRate2 * 0.5;
    const f = clamp(freq, 10, nyq * 0.99);
    const w0 = 2 * Math.PI * f / sampleRate2;
    const cosW = Math.cos(w0);
    const sinW = Math.sin(w0);
    const alpha = sinW / (2 * Math.max(1e-6, q));
    const a0 = 1 + alpha;
    c.b0 = (1 - cosW) / 2 / a0;
    c.b1 = (1 - cosW) / a0;
    c.b2 = (1 - cosW) / 2 / a0;
    c.a1 = -2 * cosW / a0;
    c.a2 = (1 - alpha) / a0;
  }
  function setHighPass(c, freq, q, sampleRate2) {
    const nyq = sampleRate2 * 0.5;
    const f = clamp(freq, 10, nyq * 0.99);
    const w0 = 2 * Math.PI * f / sampleRate2;
    const cosW = Math.cos(w0);
    const sinW = Math.sin(w0);
    const alpha = sinW / (2 * Math.max(1e-6, q));
    const a0 = 1 + alpha;
    c.b0 = (1 + cosW) / 2 / a0;
    c.b1 = -(1 + cosW) / a0;
    c.b2 = (1 + cosW) / 2 / a0;
    c.a1 = -2 * cosW / a0;
    c.a2 = (1 - alpha) / a0;
  }
  function setBell(c, freq, gainDb, q, sampleRate2) {
    const nyq = sampleRate2 * 0.5;
    const f = clamp(freq, 10, nyq * 0.99);
    const A = Math.pow(10, gainDb / 40);
    const w0 = 2 * Math.PI * f / sampleRate2;
    const cosW = Math.cos(w0);
    const sinW = Math.sin(w0);
    const alpha = sinW / (2 * Math.max(1e-6, q));
    const a0 = 1 + alpha / A;
    c.b0 = (1 + alpha * A) / a0;
    c.b1 = -2 * cosW / a0;
    c.b2 = (1 - alpha * A) / a0;
    c.a1 = -2 * cosW / a0;
    c.a2 = (1 - alpha / A) / a0;
  }
  function setHighShelf(c, freq, gainDb, q, sampleRate2) {
    const nyq = sampleRate2 * 0.5;
    const f = clamp(freq, 10, nyq * 0.99);
    const A = Math.pow(10, gainDb / 40);
    const w0 = 2 * Math.PI * f / sampleRate2;
    const cosW = Math.cos(w0);
    const sinW = Math.sin(w0);
    const alpha = sinW / (2 * Math.max(1e-6, q));
    const a0 = A + 1 - (A - 1) * cosW + 2 * Math.sqrt(A) * alpha;
    c.b0 = A * (A + 1 + (A - 1) * cosW + 2 * Math.sqrt(A) * alpha) / a0;
    c.b1 = -2 * A * (A - 1 + (A + 1) * cosW) / a0;
    c.b2 = A * (A + 1 + (A - 1) * cosW - 2 * Math.sqrt(A) * alpha) / a0;
    c.a1 = 2 * (A - 1 - (A + 1) * cosW) / a0;
    c.a2 = (A + 1 - (A - 1) * cosW - 2 * Math.sqrt(A) * alpha) / a0;
  }
  function setLowShelf(c, freq, gainDb, q, sampleRate2) {
    const nyq = sampleRate2 * 0.5;
    const f = clamp(freq, 10, nyq * 0.99);
    const A = Math.pow(10, gainDb / 40);
    const w0 = 2 * Math.PI * f / sampleRate2;
    const cosW = Math.cos(w0);
    const sinW = Math.sin(w0);
    const alpha = sinW / (2 * Math.max(1e-6, q));
    const a0 = A + 1 + (A - 1) * cosW + 2 * Math.sqrt(A) * alpha;
    c.b0 = A * (A + 1 - (A - 1) * cosW + 2 * Math.sqrt(A) * alpha) / a0;
    c.b1 = 2 * A * (A - 1 - (A + 1) * cosW) / a0;
    c.b2 = A * (A + 1 - (A - 1) * cosW - 2 * Math.sqrt(A) * alpha) / a0;
    c.a1 = -2 * (A - 1 + (A + 1) * cosW) / a0;
    c.a2 = (A + 1 + (A - 1) * cosW - 2 * Math.sqrt(A) * alpha) / a0;
  }
  function setNotch(c, freq, q, sampleRate2) {
    const nyq = sampleRate2 * 0.5;
    const f = clamp(freq, 10, nyq * 0.99);
    const w0 = 2 * Math.PI * f / sampleRate2;
    const cosW = Math.cos(w0);
    const sinW = Math.sin(w0);
    const alpha = sinW / (2 * Math.max(1e-6, q));
    const a0 = 1 + alpha;
    c.b0 = 1 / a0;
    c.b1 = -2 * cosW / a0;
    c.b2 = 1 / a0;
    c.a1 = -2 * cosW / a0;
    c.a2 = (1 - alpha) / a0;
  }
  function setBandPass(c, freq, q, sampleRate2) {
    const nyq = sampleRate2 * 0.5;
    const f = clamp(freq, 10, nyq * 0.99);
    const w0 = 2 * Math.PI * f / sampleRate2;
    const cosW = Math.cos(w0);
    const sinW = Math.sin(w0);
    const alpha = sinW / (2 * Math.max(1e-6, q));
    const a0 = 1 + alpha;
    c.b0 = alpha / a0;
    c.b1 = 0;
    c.b2 = -alpha / a0;
    c.a1 = -2 * cosW / a0;
    c.a2 = (1 - alpha) / a0;
  }
  function guardBiquadState(z) {
    for (let ch = 0; ch < z.length; ch++) {
      if (!Number.isFinite(z[ch])) {
        return true;
      }
    }
    return false;
  }
  function processBiquad(bq, channels, frameCount) {
    const { b0, b1, b2, a1, a2 } = bq.coeffs;
    for (let ch = 0; ch < bq.z1.length && ch < channels.length; ch++) {
      let z1 = bq.z1[ch];
      let z2 = bq.z2[ch];
      const data = channels[ch];
      for (let i = 0; i < frameCount; i++) {
        const x = data[i];
        const y = b0 * x + z1;
        z1 = b1 * x - a1 * y + z2;
        z2 = b2 * x - a2 * y;
        data[i] = y;
      }
      bq.z1[ch] = z1;
      bq.z2[ch] = z2;
    }
    if (guardBiquadState(bq.z1) || guardBiquadState(bq.z2)) {
      bq.z1.fill(0);
      bq.z2.fill(0);
    }
  }
  function processBiquadChannel(bq, data, ch, frameCount) {
    const { b0, b1, b2, a1, a2 } = bq.coeffs;
    let z1 = bq.z1[ch];
    let z2 = bq.z2[ch];
    for (let i = 0; i < frameCount; i++) {
      const x = data[i];
      const y = b0 * x + z1;
      z1 = b1 * x - a1 * y + z2;
      z2 = b2 * x - a2 * y;
      data[i] = y;
    }
    bq.z1[ch] = z1;
    bq.z2[ch] = z2;
    if (!Number.isFinite(z1) || !Number.isFinite(z2)) {
      bq.z1[ch] = 0;
      bq.z2[ch] = 0;
    }
  }
  var BiquadCoeffSmoother = class {
    from = { b0: 1, b1: 0, b2: 0, a1: 0, a2: 0 };
    target = { b0: 1, b1: 0, b2: 0, a1: 0, a2: 0 };
    current = { b0: 1, b1: 0, b2: 0, a1: 0, a2: 0 };
    rampRemaining = 0;
    rampLength = 1;
    /** Ramp duration in samples for the given time constant. */
    setTimeConstant(ms, sampleRate2) {
      this.rampLength = Math.max(1, Math.round(ms / 1e3 * sampleRate2));
    }
    /** Publish a new target coefficient set and start gliding toward it. */
    setTarget(c) {
      this.from.b0 = this.current.b0;
      this.from.b1 = this.current.b1;
      this.from.b2 = this.current.b2;
      this.from.a1 = this.current.a1;
      this.from.a2 = this.current.a2;
      this.target.b0 = c.b0;
      this.target.b1 = c.b1;
      this.target.b2 = c.b2;
      this.target.a1 = c.a1;
      this.target.a2 = c.a2;
      this.rampRemaining = this.rampLength;
    }
    /** Jump straight to the target (reset / hard bypass). */
    snap() {
      this.current.b0 = this.target.b0;
      this.current.b1 = this.target.b1;
      this.current.b2 = this.target.b2;
      this.current.a1 = this.target.a1;
      this.current.a2 = this.target.a2;
      this.from.b0 = this.target.b0;
      this.from.b1 = this.target.b1;
      this.from.b2 = this.target.b2;
      this.from.a1 = this.target.a1;
      this.from.a2 = this.target.a2;
      this.rampRemaining = 0;
    }
    /** True while a ramp is in progress. */
    isRamping() {
      return this.rampRemaining > 0;
    }
    /** Advance the interpolation by one sample (in place on `current`). */
    tick() {
      if (this.rampRemaining > 0) {
        this.rampRemaining--;
        if (this.rampRemaining === 0) {
          this.current.b0 = this.target.b0;
          this.current.b1 = this.target.b1;
          this.current.b2 = this.target.b2;
          this.current.a1 = this.target.a1;
          this.current.a2 = this.target.a2;
        } else {
          const t = 1 - this.rampRemaining / this.rampLength;
          this.current.b0 = this.from.b0 + (this.target.b0 - this.from.b0) * t;
          this.current.b1 = this.from.b1 + (this.target.b1 - this.from.b1) * t;
          this.current.b2 = this.from.b2 + (this.target.b2 - this.from.b2) * t;
          this.current.a1 = this.from.a1 + (this.target.a1 - this.from.a1) * t;
          this.current.a2 = this.from.a2 + (this.target.a2 - this.from.a2) * t;
        }
      }
      return this.current;
    }
  };
  function processBiquadSmoothed(bq, smoother, channels, frameCount) {
    const channelCount = Math.min(bq.z1.length, channels.length);
    for (let i = 0; i < frameCount; i++) {
      const c = smoother.tick();
      for (let ch = 0; ch < channelCount; ch++) {
        const data = channels[ch];
        const x = data[i];
        const y = c.b0 * x + bq.z1[ch];
        bq.z1[ch] = c.b1 * x - c.a1 * y + bq.z2[ch];
        bq.z2[ch] = c.b2 * x - c.a2 * y;
        data[i] = y;
      }
    }
    if (guardBiquadState(bq.z1) || guardBiquadState(bq.z2)) {
      bq.z1.fill(0);
      bq.z2.fill(0);
    }
  }
  var EnvelopeFollower = class {
    envelope = 0;
    attackCoef = 0;
    releaseCoef = 0;
    prepare(attackMs, releaseMs, sampleRate2) {
      this.attackCoef = smoothCoef(attackMs, sampleRate2);
      this.releaseCoef = smoothCoef(releaseMs, sampleRate2);
      this.envelope = 0;
    }
    setAttack(attackMs, sampleRate2) {
      this.attackCoef = smoothCoef(attackMs, sampleRate2);
    }
    setRelease(releaseMs, sampleRate2) {
      this.releaseCoef = smoothCoef(releaseMs, sampleRate2);
    }
    reset() {
      this.envelope = 0;
    }
    process(input) {
      if (!Number.isFinite(input)) return this.envelope;
      const abs = Math.abs(input);
      const coef = abs > this.envelope ? this.attackCoef : this.releaseCoef;
      this.envelope += coef * (abs - this.envelope);
      if (!Number.isFinite(this.envelope)) this.envelope = 0;
      return this.envelope;
    }
    getEnvelope() {
      return this.envelope;
    }
  };
  var RmsDetector = class {
    sumSq = 0;
    windowSize = 1;
    window = [];
    writePos = 0;
    prepare(windowMs, sampleRate2) {
      this.windowSize = Math.max(1, Math.round(windowMs / 1e3 * sampleRate2));
      this.window = new Array(this.windowSize).fill(0);
      this.sumSq = 0;
      this.writePos = 0;
    }
    reset() {
      this.sumSq = 0;
      this.window.fill(0);
      this.writePos = 0;
    }
    process(input) {
      if (!Number.isFinite(input)) {
        this.sumSq = Math.max(0, this.sumSq);
        return Math.sqrt(this.sumSq / this.windowSize);
      }
      const sq = input * input;
      this.sumSq -= this.window[this.writePos];
      this.window[this.writePos] = sq;
      this.sumSq += sq;
      this.writePos++;
      if (this.writePos >= this.windowSize) {
        this.writePos = 0;
      }
      if (!Number.isFinite(this.sumSq)) this.sumSq = 0;
      return Math.sqrt(Math.max(0, this.sumSq) / this.windowSize);
    }
  };
  function softClip(x, drive) {
    const d = x * drive;
    return d - d * d * d / 3;
  }
  function hardClip(x, threshold) {
    if (x > threshold) return threshold;
    if (x < -threshold) return -threshold;
    return x;
  }
  function tubeSaturation(x, drive) {
    const d = x * drive;
    if (d > 0) return fastTanh(d * 1.2);
    return fastTanh(d * 0.8);
  }
  function tapeSaturation(x, drive) {
    const d = x * drive;
    return fastTanh(d) * 0.7 + fastTanh(d * 2) * 0.3;
  }
  function warmSaturation(x, drive, amount) {
    const d = x * drive;
    const sat = fastTanh(d);
    return x * (1 - amount) + sat * amount;
  }
  function tubeAsymSaturation(x, drive) {
    const d = x * drive;
    const bias = 0.3;
    const tB = fastTanh(bias);
    const norm = (1 - tB * tB) * Math.max(1e-6, drive);
    return (fastTanh(d + bias) - tB) / norm;
  }
  var INV_SQRT2 = 1 / Math.SQRT2;
  function encodeMidSide(left, right, frameCount, midOut, sideOut) {
    for (let i = 0; i < frameCount; i++) {
      midOut[i] = (left[i] + right[i]) * INV_SQRT2;
      sideOut[i] = (left[i] - right[i]) * INV_SQRT2;
    }
  }
  function decodeMidSide(mid, side, frameCount, leftOut, rightOut) {
    for (let i = 0; i < frameCount; i++) {
      leftOut[i] = (mid[i] + side[i]) * INV_SQRT2;
      rightOut[i] = (mid[i] - side[i]) * INV_SQRT2;
    }
  }
  var DEFAULT_FIR_TAPS = 63;
  function createFirFilter(maxTaps) {
    const len = maxTaps % 2 === 0 ? maxTaps + 1 : maxTaps;
    return {
      taps: new Float32Array(len),
      delayLine: new Float32Array(len),
      writePos: 0,
      length: len,
      latency: (len - 1) / 2
    };
  }
  function designLowPassFir(fir, freqHz, sampleRate2, numTaps) {
    let N = Math.max(3, numTaps | 0);
    if (N % 2 === 0) N++;
    const M = (N - 1) / 2;
    const fc = freqHz / sampleRate2;
    let sum = 0;
    for (let n = 0; n < N; n++) {
      const k = n - M;
      let h;
      if (k === 0) {
        h = 2 * fc;
      } else {
        h = Math.sin(2 * Math.PI * fc * k) / (Math.PI * k);
      }
      const w = 0.42 - 0.5 * Math.cos(2 * Math.PI * n / (N - 1)) + 0.08 * Math.cos(4 * Math.PI * n / (N - 1));
      fir.taps[n] = h * w;
      sum += fir.taps[n];
    }
    if (sum !== 0) {
      for (let n = 0; n < N; n++) fir.taps[n] /= sum;
    }
    fir.length = N;
    fir.latency = M;
  }
  function resetFirFilter(fir) {
    fir.delayLine.fill(0);
    fir.writePos = 0;
  }
  function processFirSample(fir, input) {
    const N = fir.length;
    const dl = fir.delayLine;
    const taps = fir.taps;
    dl[fir.writePos] = input;
    let output = 0;
    let readPos = fir.writePos;
    for (let n = 0; n < N; n++) {
      output += taps[n] * dl[readPos];
      readPos--;
      if (readPos < 0) readPos += N;
    }
    fir.writePos++;
    if (fir.writePos >= N) fir.writePos = 0;
    return output;
  }

  // src/effects/ultina-core/dsp/spectrumAnalyzer.ts
  var FFT_SIZE = 256;
  var SPECTRUM_BINS = 64;
  var MIN_DISPLAY_FREQ = 20;
  var MAX_DISPLAY_FREQ = 2e4;
  var DB_FLOOR = -100;
  var SpectrumAnalyzer = class {
    /** Real part of FFT input/output (reused to avoid allocation). */
    real = new Float64Array(FFT_SIZE);
    /** Imaginary part of FFT input/output. */
    imag = new Float64Array(FFT_SIZE);
    /** Hann window coefficients (precomputed). */
    window = new Float64Array(FFT_SIZE);
    /** Output bins in dB. */
    binsDb = new Float32Array(SPECTRUM_BINS);
    /** Bit-reversal lookup table. */
    bitReverseTable;
    /** Precomputed log-frequency bin boundaries (linear FFT bin indices). */
    binEdges = new Float64Array(SPECTRUM_BINS + 1);
    /** Sample rate (set in prepare). */
    sampleRate = 44100;
    /** Whether the window has been initialized. */
    initialized = false;
    /**
     * Create a new spectrum analyzer.
     * The Hann window and bit-reversal table are precomputed once.
     */
    constructor() {
      this.bitReverseTable = this.buildBitReverseTable();
      this.computeWindow();
      this.computeBinEdges();
      this.initialized = true;
    }
    /**
     * Prepare the analyzer for a given sample rate.
     * Bin edges depend on sample rate so they must be recomputed.
     */
    prepare(sampleRate2) {
      this.sampleRate = sampleRate2;
      this.computeBinEdges();
    }
    /**
     * Process a block of mono samples and update internal spectrum.
     * If frameCount < FFT_SIZE, earlier slots are zero-filled.
     */
    process(mono, frameCount) {
      if (!this.initialized) {
        this.computeWindow();
        this.computeBinEdges();
        this.initialized = true;
      }
      const n = FFT_SIZE;
      const samplesToCopy = Math.min(frameCount, n);
      for (let i = 0; i < n; i++) {
        if (i < samplesToCopy) {
          this.real[i] = mono[i] * this.window[i];
        } else {
          this.real[i] = 0;
        }
        this.imag[i] = 0;
      }
      this.fftInPlace(this.real, this.imag);
      const halfN = n >> 1;
      const mag = this.real;
      for (let i = 0; i < halfN; i++) {
        const re = this.real[i];
        const im = this.imag[i];
        mag[i] = Math.sqrt(re * re + im * im);
      }
      const nyquist = this.sampleRate * 0.5;
      const normFloor = 1e-10;
      const dbRange = -DB_FLOOR;
      for (let b = 0; b < SPECTRUM_BINS; b++) {
        const startIdx = Math.floor(this.binEdges[b]);
        const endIdx = Math.ceil(this.binEdges[b + 1]);
        const lo = Math.max(1, startIdx);
        const hi = Math.min(halfN - 1, endIdx);
        if (lo > hi) {
          this.binsDb[b] = DB_FLOOR;
          continue;
        }
        let peakMag = 0;
        for (let i = lo; i <= hi; i++) {
          if (mag[i] > peakMag) peakMag = mag[i];
        }
        const normalized = peakMag / (n * 0.5);
        const clamped = Math.max(normFloor, normalized);
        let db = 20 * Math.log10(clamped);
        if (db < DB_FLOOR) db = DB_FLOOR;
        this.binsDb[b] = db;
      }
      void nyquist;
      void dbRange;
    }
    /**
     * Get the current spectrum bins (64 values in dBFS).
     * Returns a reference to the internal array — do not modify.
     */
    getBins() {
      return this.binsDb;
    }
    /** Reset the analyzer state. */
    reset() {
      this.real.fill(0);
      this.imag.fill(0);
      this.binsDb.fill(DB_FLOOR);
    }
    // ── Internal helpers ───────────────────────────────────────
    /** Compute Hann window coefficients. */
    computeWindow() {
      const n = FFT_SIZE;
      for (let i = 0; i < n; i++) {
        this.window[i] = 0.5 * (1 - Math.cos(2 * Math.PI * i / (n - 1)));
      }
    }
    /**
     * Compute log-spaced bin edges in terms of linear FFT bin indices.
     * Each edge maps a frequency to its corresponding FFT bin.
     */
    computeBinEdges() {
      const binFreqStep = this.sampleRate / FFT_SIZE;
      const logMin = Math.log10(MIN_DISPLAY_FREQ);
      const logMax = Math.log10(Math.min(MAX_DISPLAY_FREQ, this.sampleRate * 0.49));
      const logRange = logMax - logMin;
      for (let i = 0; i <= SPECTRUM_BINS; i++) {
        const t = i / SPECTRUM_BINS;
        const freq = Math.pow(10, logMin + t * logRange);
        this.binEdges[i] = freq / binFreqStep;
      }
    }
    /**
     * Build the bit-reversal permutation table for FFT_SIZE.
     */
    buildBitReverseTable() {
      const n = FFT_SIZE;
      const bits = Math.log2(n);
      const table = new Int32Array(n);
      for (let i = 0; i < n; i++) {
        let rev = 0;
        let val = i;
        for (let j = 0; j < bits; j++) {
          rev = rev << 1 | val & 1;
          val >>= 1;
        }
        table[i] = rev;
      }
      return table;
    }
    /**
     * In-place radix-2 Cooley-Tukey FFT.
     * Uses the precomputed bit-reversal table.
     */
    fftInPlace(re, im) {
      const n = FFT_SIZE;
      const table = this.bitReverseTable;
      for (let i = 0; i < n; i++) {
        const j = table[i];
        if (j > i) {
          const tmpRe = re[i];
          re[i] = re[j];
          re[j] = tmpRe;
          const tmpIm = im[i];
          im[i] = im[j];
          im[j] = tmpIm;
        }
      }
      for (let size = 2; size <= n; size <<= 1) {
        const halfSize = size >> 1;
        const angleStep = -2 * Math.PI / size;
        for (let i = 0; i < n; i += size) {
          for (let j = i; j < i + halfSize; j++) {
            const k = j + halfSize;
            const angle = angleStep * (j - i);
            const cosA = Math.cos(angle);
            const sinA = Math.sin(angle);
            const tre = re[k] * cosA - im[k] * sinA;
            const tim = re[k] * sinA + im[k] * cosA;
            re[k] = re[j] - tre;
            im[k] = im[j] - tim;
            re[j] += tre;
            im[j] += tim;
          }
        }
      }
    }
  };

  // src/effects/ultina-core/dsp/lufsMeter.ts
  var MOMENTARY_WINDOW_MS = 400;
  var SHORT_TERM_WINDOW_MS = 3e3;
  var BLOCK_DURATION_MS = 400;
  var BLOCK_HOP_MS = 100;
  var ABSOLUTE_GATE_LUFS = -70;
  var RELATIVE_GATE_OFFSET_DB = -10;
  var TP_FIR = new Float64Array([
    29698735387511e-18,
    70003751031468e-18,
    81337354444092e-18,
    7352482573811e-18,
    -171443230401889e-18,
    -385835516489692e-18,
    -472793149418841e-18,
    -252046233891145e-18,
    33186308513175e-17,
    0.001082606634264892,
    0.001551606505214169,
    0.001232552432543188,
    -97236732539107e-18,
    -0.002070062087615603,
    -0.003694453494244438,
    -0.0037419330669312,
    -0.001466481529468804,
    0.002696331102054647,
    0.006927635470624972,
    0.00864497378668053,
    0.005844033752768363,
    -0.00144067741106001,
    -0.010439900966216513,
    -0.016408227706803714,
    -0.014837366346022311,
    -0.004226172602751034,
    0.012098269623627688,
    0.0264776071470274,
    0.030107468248552035,
    0.01779427028790533,
    -0.008049720987342666,
    -0.03667241169411967,
    -0.0527769559117434,
    -0.04386728620008791,
    -0.0079467727459168,
    0.042574859272561806,
    0.08380350621329652,
    0.09017918872809959,
    0.047661919674382054,
    -0.03568901636678014,
    -0.12754997976280233,
    -0.18028738706862538,
    -0.1488283031158332,
    -0.0107117950985055,
    0.22032296472748047,
    0.49394878339712095,
    0.7376235433709863,
    0.8809918832418508,
    0.8809918832418508,
    0.7376235433709863,
    0.49394878339712095,
    0.22032296472748047,
    -0.0107117950985055,
    -0.1488283031158332,
    -0.18028738706862538,
    -0.12754997976280233,
    -0.03568901636678014,
    0.047661919674382054,
    0.09017918872809959,
    0.08380350621329637,
    0.042574859272561806,
    -0.0079467727459168,
    -0.04386728620008791,
    -0.0527769559117434,
    -0.03667241169411967,
    -0.008049720987342666,
    0.01779427028790533,
    0.030107468248552084,
    0.0264776071470274,
    0.012098269623627688,
    -0.004226172602751039,
    -0.014837366346022311,
    -0.016408227706803714,
    -0.010439900966216513,
    -0.00144067741106001,
    0.005844033752768363,
    0.008644973786680525,
    0.006927635470624972,
    0.002696331102054647,
    -0.001466481529468804,
    -0.0037419330669312,
    -0.003694453494244438,
    -0.002070062087615603,
    -97236732539107e-18,
    0.001232552432543188,
    0.001551606505214169,
    0.001082606634264891,
    331863085131751e-18,
    -252046233891145e-18,
    -472793149418841e-18,
    -385835516489693e-18,
    -171443230401889e-18,
    7352482573811e-18,
    81337354444092e-18,
    70003751031469e-18,
    29698735387511e-18
  ]);
  var TP_FIR_PHASES = 4;
  var TP_FIR_TAPS = 24;
  var TP_POLY = (() => {
    const phases = [];
    for (let p2 = 0; p2 < TP_FIR_PHASES; p2++) {
      const c = new Float64Array(TP_FIR_TAPS);
      for (let t = 0; t < TP_FIR_TAPS; t++) {
        c[t] = TP_FIR[p2 + 4 * (TP_FIR_TAPS - 1 - t)];
      }
      phases.push(c);
    }
    return phases;
  })();
  function createKWeightChannel() {
    return { coeffs: { b0: 1, b1: 0, b2: 0, a1: 0, a2: 0 }, z1: 0, z2: 0 };
  }
  function processKWeightSample(kw, x) {
    const { b0, b1, b2, a1, a2 } = kw.coeffs;
    const y = b0 * x + kw.z1;
    kw.z1 = b1 * x - a1 * y + kw.z2;
    kw.z2 = b2 * x - a2 * y;
    return y;
  }
  var LufsMeter = class _LufsMeter {
    // K-weighting filters (2 stages × 2 channels)
    kStage1 = [createKWeightChannel(), createKWeightChannel()];
    kStage2 = [createKWeightChannel(), createKWeightChannel()];
    // Momentary (400 ms) sliding window
    momentaryBuf = new Float64Array(0);
    momentaryWritePos = 0;
    momentarySum = 0;
    momentaryCount = 0;
    // Short-term (3 s) sliding window
    shortTermBuf = new Float64Array(0);
    shortTermWritePos = 0;
    shortTermSum = 0;
    shortTermCount = 0;
    // Gated block accumulation
    blockBuf = new Float64Array(0);
    blockWritePos = 0;
    blockSum = 0;
    blockCount = 0;
    blockSamplesNeeded = 0;
    hopSamplesNeeded = 0;
    hopCounter = 0;
    // Block history for integrated measurement (bounded ring buffer)
    static MAX_BLOCK_HISTORY = 6e3;
    blockHistoryBuf = new Float64Array(_LufsMeter.MAX_BLOCK_HISTORY);
    blockHistoryLen = 0;
    blockHistoryWrite = 0;
    /** True when new gated blocks arrived — integrated/LRA recompute is
     * deferred to the next read (control thread) instead of every hop. */
    historyDirty = false;
    // Scratch buffer for computeLufsRange — reused per block on the audio
    // thread (no allocation, in-place sort).
    gatedBuf = new Float64Array(_LufsMeter.MAX_BLOCK_HISTORY);
    // True-peak
    truePeakLinear = 0;
    /**
     * ITU-R BS.1770-4 4× oversampling true-peak detector state:
     * last 12 input samples per channel (the 48-tap spec FIR spans 12
     * input samples in each of its 4 polyphase branches).
     */
    tpHistL = new Float64Array(TP_FIR_TAPS);
    tpHistR = new Float64Array(TP_FIR_TAPS);
    // State
    totalSamples = 0;
    momentaryLufs = ABSOLUTE_GATE_LUFS;
    shortTermLufs = ABSOLUTE_GATE_LUFS;
    integratedLufs = ABSOLUTE_GATE_LUFS;
    lufsRange = 0;
    // ── Lifecycle ────────────────────────────────────────────
    prepare(sampleRate2, _maxBlockSize) {
      const stage1Bq = createBiquad(1);
      const stage2Bq = createBiquad(1);
      setHighShelf(stage1Bq.coeffs, 1500, 4, 0.7071, sampleRate2);
      this.kStage1[0].coeffs = { ...stage1Bq.coeffs };
      this.kStage1[1].coeffs = { ...stage1Bq.coeffs };
      setHighPass(stage2Bq.coeffs, 38, 0.5, sampleRate2);
      this.kStage2[0].coeffs = { ...stage2Bq.coeffs };
      this.kStage2[1].coeffs = { ...stage2Bq.coeffs };
      const momentarySamples = Math.floor(sampleRate2 * MOMENTARY_WINDOW_MS / 1e3);
      const shortTermSamples = Math.floor(sampleRate2 * SHORT_TERM_WINDOW_MS / 1e3);
      this.momentaryBuf = new Float64Array(momentarySamples);
      this.shortTermBuf = new Float64Array(shortTermSamples);
      this.momentaryWritePos = 0;
      this.shortTermWritePos = 0;
      this.momentarySum = 0;
      this.shortTermSum = 0;
      this.momentaryCount = 0;
      this.shortTermCount = 0;
      this.blockSamplesNeeded = Math.floor(sampleRate2 * BLOCK_DURATION_MS / 1e3);
      this.hopSamplesNeeded = Math.floor(sampleRate2 * BLOCK_HOP_MS / 1e3);
      this.blockBuf = new Float64Array(this.blockSamplesNeeded);
      this.blockWritePos = 0;
      this.blockSum = 0;
      this.blockCount = 0;
      this.hopCounter = 0;
      this.blockHistoryLen = 0;
      this.historyDirty = false;
      this.blockHistoryWrite = 0;
      this.truePeakLinear = 0;
      this.tpHistL.fill(0);
      this.tpHistR.fill(0);
      this.totalSamples = 0;
      this.momentaryLufs = ABSOLUTE_GATE_LUFS;
      this.shortTermLufs = ABSOLUTE_GATE_LUFS;
      this.integratedLufs = ABSOLUTE_GATE_LUFS;
      this.lufsRange = 0;
    }
    reset() {
      for (const kw of [...this.kStage1, ...this.kStage2]) {
        kw.z1 = 0;
        kw.z2 = 0;
      }
      this.momentaryBuf.fill(0);
      this.shortTermBuf.fill(0);
      this.momentaryWritePos = 0;
      this.shortTermWritePos = 0;
      this.momentarySum = 0;
      this.shortTermSum = 0;
      this.momentaryCount = 0;
      this.shortTermCount = 0;
      this.blockBuf.fill(0);
      this.blockWritePos = 0;
      this.blockSum = 0;
      this.blockCount = 0;
      this.hopCounter = 0;
      this.blockHistoryLen = 0;
      this.historyDirty = false;
      this.blockHistoryWrite = 0;
      this.truePeakLinear = 0;
      this.tpHistL.fill(0);
      this.tpHistR.fill(0);
      this.totalSamples = 0;
      this.momentaryLufs = ABSOLUTE_GATE_LUFS;
      this.shortTermLufs = ABSOLUTE_GATE_LUFS;
      this.integratedLufs = ABSOLUTE_GATE_LUFS;
      this.lufsRange = 0;
    }
    // ── Processing ───────────────────────────────────────────
    process(left, right, frameCount) {
      const bufL = left;
      const bufR = right ?? left;
      for (let i = 0; i < frameCount; i++) {
        const sampleL = bufL[i];
        const sampleR = bufR[i];
        const kwL = processKWeightSample(
          this.kStage2[0],
          processKWeightSample(this.kStage1[0], sampleL)
        );
        const kwR = processKWeightSample(
          this.kStage2[1],
          processKWeightSample(this.kStage1[1], sampleR)
        );
        const absL = Math.abs(sampleL);
        const absR = Math.abs(sampleR);
        const maxAbs = Math.max(absL, absR);
        if (maxAbs > this.truePeakLinear) this.truePeakLinear = maxAbs;
        {
          const hl = this.tpHistL;
          const hr = this.tpHistR;
          hl.copyWithin(0, 1);
          hl[TP_FIR_TAPS - 1] = sampleL;
          hr.copyWithin(0, 1);
          hr[TP_FIR_TAPS - 1] = sampleR;
          for (let p2 = 1; p2 < TP_FIR_PHASES; p2++) {
            const c = TP_POLY[p2];
            let sumL = 0;
            let sumR = 0;
            for (let t = 0; t < TP_FIR_TAPS; t++) {
              const coef = c[t];
              sumL += coef * hl[t];
              sumR += coef * hr[t];
            }
            if (sumL < 0) sumL = -sumL;
            if (sumR < 0) sumR = -sumR;
            if (sumL > this.truePeakLinear) this.truePeakLinear = sumL;
            if (sumR > this.truePeakLinear) this.truePeakLinear = sumR;
          }
        }
        const ms = kwL * kwL + kwR * kwR;
        const oldMS = this.momentaryBuf[this.momentaryWritePos];
        this.momentaryBuf[this.momentaryWritePos] = ms;
        this.momentarySum += ms - oldMS;
        this.momentaryWritePos = (this.momentaryWritePos + 1) % this.momentaryBuf.length;
        if (this.momentaryCount < this.momentaryBuf.length) this.momentaryCount++;
        const oldSTS = this.shortTermBuf[this.shortTermWritePos];
        this.shortTermBuf[this.shortTermWritePos] = ms;
        this.shortTermSum += ms - oldSTS;
        this.shortTermWritePos = (this.shortTermWritePos + 1) % this.shortTermBuf.length;
        if (this.shortTermCount < this.shortTermBuf.length) this.shortTermCount++;
        const oldBS = this.blockBuf[this.blockWritePos];
        this.blockBuf[this.blockWritePos] = ms;
        this.blockSum += ms - oldBS;
        this.blockWritePos = (this.blockWritePos + 1) % this.blockSamplesNeeded;
        if (this.blockCount < this.blockSamplesNeeded) this.blockCount++;
        this.hopCounter++;
        if (this.blockCount >= this.blockSamplesNeeded && this.hopCounter >= this.hopSamplesNeeded) {
          this.hopCounter = 0;
          const blockMean = this.blockSum / this.blockSamplesNeeded;
          const blockLufs = blockMean > 1e-12 ? -0.691 + 10 * Math.log10(blockMean) : ABSOLUTE_GATE_LUFS;
          this.blockHistoryBuf[this.blockHistoryWrite] = blockLufs;
          this.blockHistoryWrite = (this.blockHistoryWrite + 1) % _LufsMeter.MAX_BLOCK_HISTORY;
          if (this.blockHistoryLen < _LufsMeter.MAX_BLOCK_HISTORY) this.blockHistoryLen++;
          this.historyDirty = true;
        }
        this.totalSamples++;
      }
      if (this.momentaryCount > 0) {
        const meanMS = this.momentarySum / this.momentaryCount;
        this.momentaryLufs = meanMS > 1e-12 ? -0.691 + 10 * Math.log10(meanMS) : ABSOLUTE_GATE_LUFS;
      }
      if (this.shortTermCount > 0) {
        const meanMS = this.shortTermSum / this.shortTermCount;
        this.shortTermLufs = meanMS > 1e-12 ? -0.691 + 10 * Math.log10(meanMS) : ABSOLUTE_GATE_LUFS;
      }
    }
    // ── Readouts ─────────────────────────────────────────────
    getMomentaryLufs() {
      return this.momentaryLufs;
    }
    getShortTermLufs() {
      return this.shortTermLufs;
    }
    /**
     * Integrated loudness — LAZY: recomputed only when new gated blocks
     * arrived since the last read. Called from the control/UI thread
     * (meter polling), so the audio thread never pays for the
     * O(history) walk + sort.
     */
    getIntegratedLufs() {
      this.ensureIntegratedFresh();
      return this.integratedLufs;
    }
    getLufsRange() {
      this.ensureIntegratedFresh();
      return this.lufsRange;
    }
    getTruePeakDb() {
      if (this.truePeakLinear <= 1e-10) return -200;
      return 20 * Math.log10(this.truePeakLinear);
    }
    getTotalSamples() {
      return this.totalSamples;
    }
    getReading() {
      this.ensureIntegratedFresh();
      return {
        momentaryLufs: this.momentaryLufs,
        shortTermLufs: this.shortTermLufs,
        integratedLufs: this.integratedLufs,
        lufsRange: this.lufsRange,
        truePeakDb: this.getTruePeakDb(),
        totalSamples: this.totalSamples
      };
    }
    /** Recompute the integrated/LRA readouts if new blocks arrived. */
    ensureIntegratedFresh() {
      if (this.historyDirty) {
        this.historyDirty = false;
        this.recomputeIntegrated();
      }
    }
    // ── Private: integrated loudness recompute ───────────────
    recomputeIntegrated() {
      if (this.blockHistoryLen === 0) {
        this.integratedLufs = ABSOLUTE_GATE_LUFS;
        return;
      }
      const buf = this.blockHistoryBuf;
      const len = this.blockHistoryLen;
      let sumAbove70 = 0;
      let countAbove70 = 0;
      for (let i = 0; i < len; i++) {
        const lufs = buf[i];
        if (lufs > ABSOLUTE_GATE_LUFS) {
          sumAbove70 += Math.pow(10, (lufs + 0.691) / 10);
          countAbove70++;
        }
      }
      if (countAbove70 === 0) {
        this.integratedLufs = ABSOLUTE_GATE_LUFS;
        return;
      }
      const ungatedMean = -0.691 + 10 * Math.log10(sumAbove70 / countAbove70);
      const relativeGate = ungatedMean + RELATIVE_GATE_OFFSET_DB;
      const relGateAbs = Math.max(relativeGate, ABSOLUTE_GATE_LUFS);
      let sumGated = 0;
      let countGated = 0;
      for (let i = 0; i < len; i++) {
        const lufs = buf[i];
        if (lufs > relGateAbs) {
          sumGated += Math.pow(10, (lufs + 0.691) / 10);
          countGated++;
        }
      }
      if (countGated > 0) {
        this.integratedLufs = -0.691 + 10 * Math.log10(sumGated / countGated);
        this.computeLufsRange(relGateAbs);
      } else {
        this.integratedLufs = ungatedMean;
      }
    }
    computeLufsRange(gateLufs) {
      const src = this.blockHistoryBuf;
      const len = this.blockHistoryLen;
      const dst = this.gatedBuf;
      let n = 0;
      for (let i = 0; i < len; i++) {
        const v = src[i];
        if (v > gateLufs) {
          dst[n++] = v;
        }
      }
      if (n < 2) {
        this.lufsRange = 0;
        return;
      }
      dst.subarray(0, n).sort();
      const idx95 = Math.min(Math.floor(n * 0.95), n - 1);
      const idx10 = Math.max(Math.floor(n * 0.1), 0);
      this.lufsRange = dst[idx95] - dst[idx10];
    }
  };

  // src/effects/ultina-core/dsp/autoGain.ts
  var MIN_VALID_LUFS = -60;
  var MAX_GAIN_CORRECTION_DB = 12;
  var CONVERGENCE_TIME_CONSTANT_MS = 2e3;
  var BLOCK_UPDATE_MS = 200;
  var STARTUP_DELAY_MS = 1e3;
  var AutoGainController = class {
    enabled = false;
    targetLufs = -14;
    sampleRate = 48e3;
    // Controller state
    gainCorrectionDb = 0;
    currentErrorDb = 0;
    currentLufs = -70;
    msSinceStart = 0;
    msSinceLastUpdate = 0;
    // Smoothing for the gain target
    gainSmootherAlpha = 0;
    smoothedGainDb = 0;
    /** Prepare the controller for operation. */
    prepare(sampleRate2, _maxBlockSize) {
      this.sampleRate = sampleRate2;
      this.gainSmootherAlpha = smoothCoef(CONVERGENCE_TIME_CONSTANT_MS, sampleRate2);
      this.reset();
    }
    /** Reset all state. */
    reset() {
      this.gainCorrectionDb = 0;
      this.currentErrorDb = 0;
      this.currentLufs = -70;
      this.msSinceStart = 0;
      this.msSinceLastUpdate = 0;
      this.smoothedGainDb = 0;
    }
    /** Set whether auto-gain is enabled. */
    setEnabled(enabled) {
      if (enabled && !this.enabled) {
        this.msSinceStart = 0;
        this.msSinceLastUpdate = 0;
      }
      this.enabled = enabled;
      if (!enabled) {
      }
    }
    /** Set target LUFS. */
    setTargetLufs(lufs) {
      this.targetLufs = lufs;
    }
    /**
     * Process one block. Reads current short-term LUFS and updates
     * the gain correction.
     *
     * @param shortTermLufs  Current short-term LUFS from the LUFS meter
     * @param frameCount     Number of samples in this block
     * @returns              The gain correction in dB to add to output gain
     */
    process(shortTermLufs, frameCount) {
      const blockMs = frameCount / this.sampleRate * 1e3;
      this.msSinceStart += blockMs;
      this.msSinceLastUpdate += blockMs;
      this.currentLufs = shortTermLufs;
      if (!this.enabled) {
        this.gainCorrectionDb = 0;
        this.smoothedGainDb += (0 - this.smoothedGainDb) * Math.min(1, this.gainSmootherAlpha * frameCount * 4);
        return this.smoothedGainDb;
      }
      if (this.msSinceStart < STARTUP_DELAY_MS) {
        this.smoothedGainDb += (0 - this.smoothedGainDb) * Math.min(1, this.gainSmootherAlpha * frameCount * 4);
        return this.smoothedGainDb;
      }
      if (this.msSinceLastUpdate >= BLOCK_UPDATE_MS) {
        this.msSinceLastUpdate = 0;
        this.updateController();
      }
      const alpha = Math.min(1, this.gainSmootherAlpha * frameCount);
      this.smoothedGainDb += (this.gainCorrectionDb - this.smoothedGainDb) * alpha;
      if (!Number.isFinite(this.smoothedGainDb)) {
        this.smoothedGainDb = 0;
        this.gainCorrectionDb = 0;
      }
      return this.smoothedGainDb;
    }
    /** Internal: compute the PI controller update with anti-windup. */
    updateController() {
      if (!Number.isFinite(this.currentLufs) || this.currentLufs < MIN_VALID_LUFS) {
        return;
      }
      this.currentErrorDb = this.targetLufs - this.currentLufs;
      const gainPerUpdate = BLOCK_UPDATE_MS / CONVERGENCE_TIME_CONSTANT_MS;
      const candidate = this.gainCorrectionDb + this.currentErrorDb * gainPerUpdate;
      if (candidate > MAX_GAIN_CORRECTION_DB) {
        this.gainCorrectionDb = MAX_GAIN_CORRECTION_DB;
      } else if (candidate < -MAX_GAIN_CORRECTION_DB) {
        this.gainCorrectionDb = -MAX_GAIN_CORRECTION_DB;
      } else {
        this.gainCorrectionDb = candidate;
      }
    }
    /** Pooled reading — getReading() runs on the audio thread (meter cadence)
     * inside UltinaProcessor.getMeters(); callers copy the scalars out
     * immediately (the next call overwrites every field). */
    pooledReading = {
      gainCorrectionDb: 0,
      errorDb: 0,
      currentLufs: -70,
      targetLufs: -14,
      active: false
    };
    /** Get current auto-gain state for metering/display. */
    getReading() {
      const r = this.pooledReading;
      r.gainCorrectionDb = this.smoothedGainDb;
      r.errorDb = this.currentErrorDb;
      r.currentLufs = this.currentLufs;
      r.targetLufs = this.targetLufs;
      r.active = this.enabled && this.msSinceStart >= STARTUP_DELAY_MS;
      return r;
    }
    /** Get the raw (unsmoothed) gain correction target in dB. */
    getRawGainCorrectionDb() {
      return this.gainCorrectionDb;
    }
  };

  // src/effects/ultina-core/dsp/spectralRegistry.ts
  var SPECTRAL_BANDS = 32;
  var SPECTRAL_BAND_FREQS = (() => {
    const logMin = Math.log(40);
    const logMax = Math.log(16e3);
    const freqs = [];
    for (let i = 0; i < SPECTRAL_BANDS; i++) {
      const t = i / (SPECTRAL_BANDS - 1);
      freqs.push(Math.round(Math.exp(logMin + t * (logMax - logMin))));
    }
    return freqs;
  })();
  var STALE_GRACE = 8;
  var BandAnalyzer = class _BandAnalyzer {
    filters = [];
    envelopes = new Float32Array(SPECTRAL_BANDS);
    bandLevelsDb = new Float32Array(SPECTRAL_BANDS);
    envCoef = 0.01;
    /** Analysis Q factor (matches Unmask). */
    static Q = 3;
    prepare(sampleRate2, _maxBlockSize) {
      this.envCoef = 1 - Math.exp(-1 / (50 / 1e3 * sampleRate2));
      this.envelopes.fill(0);
      this.bandLevelsDb.fill(-200);
      this.filters = [];
      for (let i = 0; i < SPECTRAL_BANDS; i++) {
        const freq = Math.min(SPECTRAL_BAND_FREQS[i], sampleRate2 / 2 - 1);
        const bq = createBiquad(1);
        setBandPass(bq.coeffs, freq, _BandAnalyzer.Q, sampleRate2);
        this.filters.push(bq);
      }
    }
    reset() {
      this.envelopes.fill(0);
      this.bandLevelsDb.fill(-200);
      for (const bq of this.filters) resetBiquad(bq);
    }
    /**
     * Analyze a block of mono audio and update per-band envelopes.
     * @param input Mono audio buffer (L+R averaged recommended)
     * @param frameCount Number of valid frames
     */
    process(input, frameCount) {
      const n = Math.min(input.length, frameCount);
      for (let b = 0; b < SPECTRAL_BANDS; b++) {
        const bq = this.filters[b];
        const c = bq.coeffs;
        let z1 = bq.z1[0];
        let z2 = bq.z2[0];
        let env = this.envelopes[b];
        for (let i = 0; i < n; i++) {
          const x = input[i];
          const y = c.b0 * x + z1;
          z1 = c.b1 * x - c.a1 * y + z2;
          z2 = c.b2 * x - c.a2 * y;
          const abs = y < 0 ? -y : y;
          env += this.envCoef * (abs - env);
        }
        bq.z1[0] = z1;
        bq.z2[0] = z2;
        this.envelopes[b] = env;
      }
      for (let b = 0; b < SPECTRAL_BANDS; b++) {
        this.bandLevelsDb[b] = this.envelopes[b] > 1e-10 ? 20 * Math.log10(this.envelopes[b]) : -200;
      }
    }
    /**
     * Get the current per-band energy in dB.
     * Returns a reference to the internal array — do not modify.
     */
    getBandLevelsDb() {
      return this.bandLevelsDb;
    }
  };
  var SpectralRegistry = class _SpectralRegistry {
    static instance = null;
    /** Map of instanceId → SpectralEntry. */
    entries = /* @__PURE__ */ new Map();
    /** Auto-incrementing instance counter. */
    instanceCounter = 0;
    constructor() {
    }
    /** Get the singleton instance. */
    static getInstance() {
      if (!_SpectralRegistry.instance) {
        _SpectralRegistry.instance = new _SpectralRegistry();
      }
      return _SpectralRegistry.instance;
    }
    /**
     * Generate a unique instance ID.
     * Called during processor construction.
     */
    generateId() {
      this.instanceCounter++;
      return `ultina-${this.instanceCounter}`;
    }
    /**
     * Register a new instance in the registry.
     * Called during prepare(), NOT from the audio thread.
     */
    register(instanceId, name = "Ultina") {
      this.entries.set(instanceId, {
        instanceId,
        name,
        bandLevelsDb: new Float32Array(SPECTRAL_BANDS).fill(-200),
        blockCounter: 0,
        active: true,
        consumerStaleness: /* @__PURE__ */ new Map()
      });
    }
    /**
     * Update the display name for an instance.
     * Called from the control thread.
     */
    setName(instanceId, name) {
      const entry = this.entries.get(instanceId);
      if (entry) entry.name = name;
    }
    /**
     * Unregister an instance.
     * Called during dispose/reset.
     */
    unregister(instanceId) {
      this.entries.delete(instanceId);
      for (const entry of this.entries.values()) {
        entry.consumerStaleness.delete(instanceId);
      }
    }
    /**
     * Publish spectral data for an instance.
     * AUDIO-THREAD SAFE: Only performs float copies, no allocation.
     *
     * @param instanceId The publishing instance
     * @param bandLevelsDb 32-band energy in dB (reference is copied)
     */
    publish(instanceId, bandLevelsDb) {
      const entry = this.entries.get(instanceId);
      if (entry) {
        if (bandLevelsDb.length <= SPECTRAL_BANDS) {
          entry.bandLevelsDb.set(bandLevelsDb);
        } else {
          entry.bandLevelsDb.set(bandLevelsDb.subarray(0, SPECTRAL_BANDS));
        }
        entry.blockCounter++;
        entry.active = true;
      }
    }
    /**
     * Compute the aggregate masker spectrum from all OTHER instances.
     *
     * Uses "max per band" strategy: for each band, the loudest other
     * instance determines the masking level. This represents the worst-
     * case masking from the entire ecosystem.
     *
     * AUDIO-THREAD SAFE: Iterates the map, writes to pre-allocated buffer.
     *
     * @param instanceId The querying instance (excluded from results)
     * @param out Pre-allocated Float32Array(32) to fill with aggregate dB values
     * @returns true if at least one other instance contributed data
     */
    getAggregateMasker(instanceId, out) {
      out.fill(-200);
      let hasData = false;
      for (const [id, entry] of this.entries) {
        if (id === instanceId) continue;
        if (!entry.active) continue;
        let seen = entry.consumerStaleness.get(instanceId);
        if (!seen) {
          seen = { counter: -1, stale: 0 };
          entry.consumerStaleness.set(instanceId, seen);
        }
        if (entry.blockCounter === seen.counter) {
          seen.stale++;
          if (seen.stale > STALE_GRACE) continue;
        } else {
          seen.counter = entry.blockCounter;
          seen.stale = 0;
        }
        hasData = true;
        const levels = entry.bandLevelsDb;
        for (let b = 0; b < SPECTRAL_BANDS; b++) {
          if (levels[b] > out[b]) {
            out[b] = levels[b];
          }
        }
      }
      return hasData;
    }
    /**
     * Get a snapshot of all registered instances (for UI display).
     * Called from the control thread only.
     */
    listInstances() {
      return Array.from(this.entries.values());
    }
    /**
     * Get the number of registered instances.
     */
    getInstanceCount() {
      return this.entries.size;
    }
    /**
     * Clear all instances (for testing).
     */
    clear() {
      this.entries.clear();
    }
  };

  // src/effects/ultina-core/dsp/eqLearn.ts
  function safeNum(x) {
    return Number.isFinite(x) ? x : 0;
  }
  var EQ_LEARN_BANDS = 16;
  var EQ_LEARN_FREQS = [
    60,
    100,
    150,
    250,
    350,
    500,
    700,
    1e3,
    1500,
    2500,
    3500,
    5e3,
    7e3,
    1e4,
    12e3,
    16e3
  ];
  var DEFAULT_SUGGEST_Q = 3;
  var MAX_CUT_DB = 12;
  var DETECTION_THRESHOLD_DB = 6;
  var ACTIVE_RANGE_DB = 40;
  var LOCAL_PEAK_MARGIN_DB = 3;
  var EqLearn = class _EqLearn {
    filters = [];
    bandEnergy = new Array(EQ_LEARN_BANDS).fill(0);
    tempBuf = new Float32Array(0);
    blockCount = 0;
    /** Analysis time constant in samples (100 ms) — process() derives the
     * per-block coefficient from the ACTUAL frame count. */
    tauSamples = 4800;
    /** Minimum blocks before results are considered reliable. */
    static MIN_BLOCKS = 20;
    prepare(sampleRate2, maxBlockSize) {
      this.tempBuf = new Float32Array(maxBlockSize);
      this.tauSamples = Math.max(1, 100 / 1e3 * sampleRate2);
      this.filters = [];
      for (let i = 0; i < EQ_LEARN_BANDS; i++) {
        const bq = createBiquad(1);
        setBandPass(bq.coeffs, EQ_LEARN_FREQS[i], 4, sampleRate2);
        this.filters.push(bq);
      }
      this.reset();
    }
    reset() {
      this.bandEnergy.fill(0);
      this.blockCount = 0;
      for (const bq of this.filters) resetBiquad(bq);
    }
    /**
     * Process a block of audio and update internal analysis state.
     * Call repeatedly with audio blocks, then call `getResult()` to
     * retrieve EQ suggestions.
     */
    process(input, frameCount) {
      this.ensureBuffers(frameCount);
      for (let b = 0; b < EQ_LEARN_BANDS; b++) {
        const bq = this.filters[b];
        for (let i = 0; i < frameCount; i++) {
          this.tempBuf[i] = input[i];
        }
        processBiquadChannel(bq, this.tempBuf, 0, frameCount);
        let sumSq = 0;
        for (let i = 0; i < frameCount; i++) {
          const s = this.tempBuf[i];
          sumSq += s * s;
        }
        const rms = Math.sqrt(sumSq / Math.max(1, frameCount));
        const coef = 1 - Math.exp(-frameCount / this.tauSamples);
        this.bandEnergy[b] += coef * (rms - this.bandEnergy[b]);
      }
      this.blockCount++;
    }
    /**
     * Compute the current EQ learn analysis result.
     */
    getResult() {
      const bandLevels = new Array(EQ_LEARN_BANDS);
      let totalWeighted = 0;
      let totalEnergy = 0;
      for (let b = 0; b < EQ_LEARN_BANDS; b++) {
        const db = 20 * Math.log10(Math.max(1e-10, this.bandEnergy[b]));
        bandLevels[b] = safeNum(db);
        const lin = Math.max(1e-10, this.bandEnergy[b]);
        totalWeighted += EQ_LEARN_FREQS[b] * lin;
        totalEnergy += lin;
      }
      const spectralCentroid = totalEnergy > 1e-10 ? totalWeighted / totalEnergy : 1e3;
      const peakBandLevel = Math.max(...bandLevels);
      const activeLevels = bandLevels.filter((v) => v > peakBandLevel - ACTIVE_RANGE_DB);
      const averageLevel = activeLevels.length > 0 ? activeLevels.reduce((a, b) => a + b, 0) / activeLevels.length : 0;
      const suggestions = [];
      for (let b = 0; b < EQ_LEARN_BANDS; b++) {
        const excess = bandLevels[b] - averageLevel;
        if (excess > DETECTION_THRESHOLD_DB) {
          const isLocalPeak = (b === 0 || bandLevels[b] > bandLevels[b - 1] + LOCAL_PEAK_MARGIN_DB) && (b === EQ_LEARN_BANDS - 1 || bandLevels[b] > bandLevels[b + 1] + LOCAL_PEAK_MARGIN_DB);
          if (!isLocalPeak) continue;
          const cutDb = Math.min(MAX_CUT_DB, excess * 0.7);
          const severity = Math.min(1, excess / (DETECTION_THRESHOLD_DB * 3));
          suggestions.push({
            freqHz: EQ_LEARN_FREQS[b],
            gainDb: -cutDb,
            q: DEFAULT_SUGGEST_Q,
            severity
          });
        }
      }
      suggestions.sort((a, b) => b.severity - a.severity);
      const isReady = this.blockCount >= _EqLearn.MIN_BLOCKS;
      return {
        suggestions,
        bandLevels,
        spectralCentroid: safeNum(spectralCentroid),
        averageLevel: safeNum(averageLevel),
        isReady
      };
    }
    ensureBuffers(requiredSize) {
      if (this.tempBuf.length < requiredSize) {
        this.tempBuf = new Float32Array(requiredSize);
      }
    }
  };

  // src/effects/ultina-core/dsp/crossoverLearn.ts
  var XOVER_LEARN_BANDS = 32;
  var XOVER_LEARN_FREQS = (() => {
    const freqs = [];
    const logMin = Math.log(40);
    const logMax = Math.log(16e3);
    for (let i = 0; i < XOVER_LEARN_BANDS; i++) {
      const t = i / (XOVER_LEARN_BANDS - 1);
      freqs.push(Math.round(Math.exp(logMin + t * (logMax - logMin))));
    }
    return freqs;
  })();
  var ANALYSIS_Q = 3;
  var MIN_BLOCKS = 30;
  var MIN_VALLEY_DEPTH_DB = 2;
  var MIN_XOVER_HZ = 80;
  var MAX_XOVER_HZ = 12e3;
  var CrossoverLearn = class {
    filters = [];
    bandEnergy = new Array(XOVER_LEARN_BANDS).fill(0);
    tempBuf = new Float32Array(0);
    blockCount = 0;
    /** Analysis time constant in samples (500 ms) — process() derives the
     * per-block coefficient from the ACTUAL frame count. */
    tauSamples = 24e3;
    prepare(sampleRate2, maxBlockSize) {
      this.tempBuf = new Float32Array(maxBlockSize);
      this.tauSamples = Math.max(1, 500 / 1e3 * sampleRate2);
      this.filters = [];
      for (let i = 0; i < XOVER_LEARN_BANDS; i++) {
        const bq = createBiquad(1);
        setBandPass(bq.coeffs, XOVER_LEARN_FREQS[i], ANALYSIS_Q, sampleRate2);
        this.filters.push(bq);
      }
      this.reset();
    }
    reset() {
      this.bandEnergy.fill(0);
      this.blockCount = 0;
      for (const bq of this.filters) resetBiquad(bq);
    }
    /**
     * Process a block of mono audio (mix stereo to mono before calling).
     */
    process(input, frameCount) {
      this.ensureBuffers(frameCount);
      for (let b = 0; b < XOVER_LEARN_BANDS; b++) {
        const bq = this.filters[b];
        for (let i = 0; i < frameCount; i++) {
          this.tempBuf[i] = input[i];
        }
        processBiquadChannel(bq, this.tempBuf, 0, frameCount);
        let sumSq = 0;
        for (let i = 0; i < frameCount; i++) {
          const s = this.tempBuf[i];
          sumSq += s * s;
        }
        const rms = Math.sqrt(sumSq / Math.max(1, frameCount));
        const coef = 1 - Math.exp(-frameCount / this.tauSamples);
        this.bandEnergy[b] += coef * (rms - this.bandEnergy[b]);
      }
      this.blockCount++;
    }
    /**
     * Compute crossover suggestions from accumulated spectral data.
     *
     * Uses an energy-balanced splitting strategy:
     * - Builds cumulative spectral energy from low to high
     * - Finds where cumulative energy crosses 50% (2-band)
     *   or 33%/67% (3-band)
     * - If a spectral valley exists near the energy-split point,
     *   prefer the valley frequency for a more natural boundary
     */
    getResult() {
      const bandLevels = new Array(XOVER_LEARN_BANDS);
      let totalWeighted = 0;
      let totalEnergy = 0;
      const linEnergy = new Array(XOVER_LEARN_BANDS);
      for (let b = 0; b < XOVER_LEARN_BANDS; b++) {
        const db = 20 * Math.log10(Math.max(1e-10, this.bandEnergy[b]));
        bandLevels[b] = Number.isFinite(db) ? db : -100;
        const lin = Math.max(0, this.bandEnergy[b]);
        linEnergy[b] = lin;
        totalWeighted += XOVER_LEARN_FREQS[b] * lin;
        totalEnergy += lin;
      }
      const spectralCentroid = totalEnergy > 1e-10 ? totalWeighted / totalEnergy : 1e3;
      const valleys = this.findValleys(bandLevels);
      let suggestion2Band = null;
      if (totalEnergy > 1e-10) {
        const splitIdx = this.findEnergySplit(linEnergy, totalEnergy, 0.5);
        if (splitIdx >= 0) {
          const valleyNearby = this.findValleyNear(valleys, splitIdx, 3);
          const targetIdx = valleyNearby ?? splitIdx;
          const freq = this.interpolateFreq(targetIdx, linEnergy);
          const depth = this.estimateDepth(bandLevels, targetIdx);
          suggestion2Band = this.toSuggestion(targetIdx, freq, depth);
        }
      }
      let suggestions3Band = null;
      if (totalEnergy > 1e-10) {
        const splitIdx1 = this.findEnergySplit(linEnergy, totalEnergy, 1 / 3);
        const splitIdx2 = this.findEnergySplit(linEnergy, totalEnergy, 2 / 3);
        if (splitIdx1 >= 0 && splitIdx2 >= 0 && splitIdx1 < splitIdx2) {
          const v1Nearby = this.findValleyNear(valleys, splitIdx1, 3) ?? splitIdx1;
          const v2Nearby = this.findValleyNear(valleys, splitIdx2, 3) ?? splitIdx2;
          suggestions3Band = [
            this.toSuggestion(
              v1Nearby,
              this.interpolateFreq(v1Nearby, linEnergy),
              this.estimateDepth(bandLevels, v1Nearby)
            ),
            this.toSuggestion(
              v2Nearby,
              this.interpolateFreq(v2Nearby, linEnergy),
              this.estimateDepth(bandLevels, v2Nearby)
            )
          ];
        }
      }
      return {
        suggestion2Band,
        suggestions3Band,
        bandLevels,
        spectralCentroid: Number.isFinite(spectralCentroid) ? spectralCentroid : 1e3,
        isReady: this.blockCount >= MIN_BLOCKS
      };
    }
    // ── Energy Split ──────────────────────────────────────────
    /**
     * Find the band index where cumulative energy crosses a fraction
     * of total energy.
     */
    findEnergySplit(linEnergy, totalEnergy, fraction) {
      const target = totalEnergy * fraction;
      let cumulative = 0;
      for (let b = 0; b < XOVER_LEARN_BANDS; b++) {
        cumulative += linEnergy[b];
        if (cumulative >= target) {
          return b;
        }
      }
      return XOVER_LEARN_BANDS - 1;
    }
    /**
     * Interpolate the exact crossover frequency between band centers
     * using linear interpolation in log-frequency domain.
     */
    interpolateFreq(bandIndex, _linEnergy) {
      const freq = XOVER_LEARN_FREQS[bandIndex];
      return freq;
    }
    // ── Valley Detection ──────────────────────────────────────
    /**
     * Find spectral valleys in the energy curve.
     * A valley is a local minimum that is significantly below
     * both its neighbors.
     */
    findValleys(levels) {
      const valleys = [];
      const smoothed = this.smoothCurve(levels, 1);
      for (let i = 2; i < XOVER_LEARN_BANDS - 2; i++) {
        const freq = XOVER_LEARN_FREQS[i];
        if (freq < MIN_XOVER_HZ || freq > MAX_XOVER_HZ) continue;
        const val = smoothed[i];
        const leftMax = Math.max(smoothed[i - 1], smoothed[i - 2]);
        const rightMax = Math.max(smoothed[i + 1], smoothed[i + 2]);
        const depthLeft = leftMax - val;
        const depthRight = rightMax - val;
        if (depthLeft >= MIN_VALLEY_DEPTH_DB && depthRight >= MIN_VALLEY_DEPTH_DB) {
          const depthDb = (depthLeft + depthRight) / 2;
          valleys.push({ index: i, freqHz: freq, depthDb });
        }
      }
      valleys.sort((a, b) => b.depthDb - a.depthDb);
      return valleys;
    }
    /**
     * Find a valley near a target band index, within a window.
     */
    findValleyNear(valleys, targetIndex, window) {
      let best = null;
      let bestDepth = 0;
      for (const v of valleys) {
        if (Math.abs(v.index - targetIndex) <= window && v.depthDb > bestDepth) {
          best = v.index;
          bestDepth = v.depthDb;
        }
      }
      return best;
    }
    /**
     * Estimate the spectral depth at a band index relative to neighbors.
     */
    estimateDepth(levels, index) {
      const left = index > 0 ? levels[index - 1] : levels[index];
      const right = index < XOVER_LEARN_BANDS - 1 ? levels[index + 1] : levels[index];
      const avgNeighbor = (left + right) / 2;
      return Math.max(0, avgNeighbor - levels[index]);
    }
    /**
     * Simple moving-average smoothing of the energy curve.
     */
    smoothCurve(levels, halfWindow) {
      const result = new Array(levels.length);
      for (let i = 0; i < levels.length; i++) {
        let sum = 0;
        let count = 0;
        for (let j = Math.max(0, i - halfWindow); j <= Math.min(levels.length - 1, i + halfWindow); j++) {
          sum += levels[j];
          count++;
        }
        result[i] = sum / Math.max(1, count);
      }
      return result;
    }
    /**
     * Convert analysis results to a suggestion with confidence.
     */
    toSuggestion(bandIndex, freqHz, depthDb) {
      const depthConfidence = Math.min(1, depthDb / 12);
      const dataConfidence = Math.min(1, this.blockCount / (MIN_BLOCKS * 2));
      const confidence = Math.min(
        1,
        0.3 * dataConfidence + 0.7 * Math.max(0.3, depthConfidence)
      );
      return {
        freqHz,
        bandIndex,
        depthDb,
        confidence
      };
    }
    ensureBuffers(requiredSize) {
      if (this.tempBuf.length < requiredSize) {
        this.tempBuf = new Float32Array(requiredSize);
      }
    }
  };

  // src/effects/ultina-core/dsp/ultinaProcessor.ts
  var StubModuleProcessor = class {
    prepare() {
    }
    process() {
    }
    reset() {
    }
    getMeters() {
      return { bands: [], stub: true };
    }
  };
  var PARAM_QUEUE_SIZE = 1024;
  var ParamChangeQueue = class {
    buffer;
    writePos = 0;
    readPos = 0;
    constructor() {
      this.buffer = new Array(PARAM_QUEUE_SIZE);
      for (let i = 0; i < PARAM_QUEUE_SIZE; i++) {
        this.buffer[i] = { id: "", value: 0 };
      }
    }
    reset() {
      this.writePos = 0;
      this.readPos = 0;
    }
    /** Control-thread: enqueue a parameter change.
     * When the ring is full (control-side burst while the audio thread is not
     * draining — e.g. a suspended context), the OLDEST entry is dropped so the
     * audio thread always converges to the most RECENT values. Dropping the
     * newest instead would silently diverge the audio state from the control
     * state (the exact values the user just set would be the ones lost). */
    enqueue(id, value) {
      const nextWrite = (this.writePos + 1) % PARAM_QUEUE_SIZE;
      if (nextWrite === this.readPos) {
        this.readPos = (this.readPos + 1) % PARAM_QUEUE_SIZE;
      }
      this.buffer[this.writePos].id = id;
      this.buffer[this.writePos].value = value;
      this.writePos = nextWrite;
      return true;
    }
    /** Audio-thread: dequeue the next change into `out`. The payload is
     * copied BEFORE the read index advances — otherwise the control
     * thread may overwrite the slot while the audio thread still reads
     * it (mismatched id/value pairs under heavy parameter traffic). */
    dequeueInto(out) {
      if (this.readPos === this.writePos) return false;
      const slot = this.buffer[this.readPos];
      out.id = slot.id;
      out.value = slot.value;
      this.readPos = (this.readPos + 1) % PARAM_QUEUE_SIZE;
      return true;
    }
    /** Drain all pending changes into a target map. */
    drainInto(target) {
      const entry = { id: "", value: 0 };
      while (this.dequeueInto(entry)) {
        target[entry.id] = entry.value;
      }
    }
  };
  var UltinaProcessor = class _UltinaProcessor {
    // Configuration
    sampleRate = 44100;
    maxBlockSize = 512;
    channelCount = 2;
    qualityMode = 1;
    prepared = false;
    // Parameters
    params;
    // Module graph runtime
    graphRuntime;
    // Module instances (one per type, lazily created)
    modules = /* @__PURE__ */ new Map();
    // Module factory registry
    factories = /* @__PURE__ */ new Map();
    // Parameter change queue (control → audio thread)
    paramQueue = new ParamChangeQueue();
    // Working buffers
    dryBufferL = new Float32Array(0);
    dryBufferR = new Float32Array(0);
    // Smoothers for global controls
    inputGainSmoother = new OnePoleSmoother();
    outputGainSmoother = new OnePoleSmoother();
    mixSmoother = new OnePoleSmoother();
    // Meter state
    lufsMeter = new LufsMeter();
    autoGain = new AutoGainController();
    inputPeakL = 0;
    inputPeakR = 0;
    outputPeakL = 0;
    outputPeakR = 0;
    inputRmsL = 0;
    inputRmsR = 0;
    outputRmsL = 0;
    outputRmsR = 0;
    totalSamples = 0;
    /** Samples processed since the LUFS meter was last fed — staleness guard
     * for the auto-gain feedback loop (meters off + gain-match off). */
    lufsUnfedSamples = 0;
    // Pooled meter snapshot — buffers and containers reused across getMeters()
    // calls so a ~21 Hz poller costs zero steady-state allocation on the
    // worklet thread. Consumers must consume immediately (postMessage clones;
    // direct readers copy): the next call overwrites every field.
    metersSnapshot = null;
    // Visualizer buffers (pre-allocated to avoid per-block GC)
    waveformBuffer = new Float32Array(256);
    waveformFillPos = 0;
    monoSumBuffer = new Float32Array(FFT_SIZE);
    spectrumAnalyzer = new SpectrumAnalyzer();
    spectrumCounter = 0;
    /** Run spectrum analysis every N blocks to reduce CPU. */
    static SPECTRUM_INTERVAL = 4;
    // Cross-instance spectral sharing
    instanceId;
    bandAnalyzer = new BandAnalyzer();
    // Learn-mode analyzers (run only while the respective learn param is on)
    eqLearn = new EqLearn();
    xoverLearn = new CrossoverLearn();
    eqLearnWasActive = false;
    xoverLearnWasActive = false;
    spectralRegistry = SpectralRegistry.getInstance();
    // Active delta module (null = no delta listen)
    deltaModule = null;
    // Metering gate. Meters feed panel UIs nobody may be watching — when the
    // host knows no consumer is attached it disables the whole analysis path
    // (spectrum FFT, 32-band analyzer, waveform, peak/RMS) to keep the audio
    // thread's cost flat. LUFS keeps running when gain-match needs it.
    metersEnabled = true;
    // Cached context object (reused across process() calls — no per-block allocation)
    cachedCtx = {
      sampleRate: 44100,
      maxBlockSize: 512,
      channelCount: 2,
      qualityMode: 1
    };
    // Pre-allocated per-chunk channel refs + module process args. process()
    // reuses these instead of allocating a channels array and an args object
    // per module per block — the processor's own REALTIME CONSTRAINTS forbid
    // allocations on the audio path (modules destructure args immediately and
    // never retain it).
    chunkChannels = [new Float32Array(0), new Float32Array(0)];
    chunkArgs = {
      channels: this.chunkChannels,
      frameCount: 0,
      sidechain: null,
      ctx: this.cachedCtx,
      params: {}
    };
    // Pre-allocated per-module param records (keyed by module type prefix)
    moduleParamCache = /* @__PURE__ */ new Map();
    moduleParamKeys = /* @__PURE__ */ new Map();
    constructor() {
      this.params = buildDefaultParams();
      this.graphRuntime = new ModuleGraphRuntime();
      this.instanceId = this.spectralRegistry.generateId();
    }
    // ── Lifecycle ────────────────────────────────────────────
    /**
     * Prepare the processor for audio processing.
     * Must be called before process().
     */
    prepare(config) {
      this.sampleRate = config.sampleRate;
      this.maxBlockSize = config.maxBlockSize;
      this.channelCount = config.channelCount;
      this.qualityMode = config.qualityMode;
      const bufSize = this.maxBlockSize;
      this.dryBufferL = new Float32Array(bufSize);
      this.dryBufferR = new Float32Array(bufSize);
      const smoothMs = 20;
      this.inputGainSmoother = new OnePoleSmoother();
      this.outputGainSmoother = new OnePoleSmoother();
      this.mixSmoother = new OnePoleSmoother();
      this.inputGainSmoother.setTimeConstant(smoothMs, this.sampleRate);
      this.outputGainSmoother.setTimeConstant(smoothMs, this.sampleRate);
      this.mixSmoother.setTimeConstant(smoothMs, this.sampleRate);
      this.inputGainSmoother.reset(
        dbToLinear(this.params[GLOBAL_INPUT_GAIN_DB_ID] ?? 0)
      );
      this.outputGainSmoother.reset(
        dbToLinear(this.params[GLOBAL_OUTPUT_GAIN_DB_ID] ?? 0)
      );
      this.mixSmoother.reset((this.params[GLOBAL_MIX_ID] ?? 100) / 100);
      this.cachedCtx.sampleRate = this.sampleRate;
      this.cachedCtx.maxBlockSize = this.maxBlockSize;
      this.cachedCtx.channelCount = this.channelCount;
      this.cachedCtx.qualityMode = this.qualityMode;
      for (const moduleType of MODULE_TYPES) {
        const existing = this.modules.get(moduleType);
        if (existing) {
          existing.prepare(this.cachedCtx);
          continue;
        }
        const factory = this.factories.get(moduleType);
        const module = factory ? factory() : new StubModuleProcessor();
        if (moduleType === "unmask" && "setInstanceId" in module) {
          module.setInstanceId(this.instanceId);
        }
        module.prepare(this.cachedCtx);
        this.modules.set(moduleType, module);
      }
      this.moduleParamKeys.clear();
      this.moduleParamCache.clear();
      for (const moduleType of MODULE_TYPES) {
        const prefix = `${moduleType}.`;
        const keys = [];
        for (const id of Object.keys(this.params)) {
          if (id.startsWith(prefix)) keys.push(id);
        }
        this.moduleParamKeys.set(prefix, keys);
        this.moduleParamCache.set(prefix, {});
      }
      this.spectrumAnalyzer.prepare(this.sampleRate);
      this.lufsMeter.prepare(this.sampleRate, this.maxBlockSize);
      this.autoGain.prepare(this.sampleRate, this.maxBlockSize);
      this.waveformBuffer.fill(0);
      this.waveformFillPos = 0;
      this.monoSumBuffer.fill(0);
      this.spectrumCounter = 0;
      this.bandAnalyzer.prepare(this.sampleRate, this.maxBlockSize);
      this.eqLearn.prepare(this.sampleRate, this.maxBlockSize);
      this.xoverLearn.prepare(this.sampleRate, this.maxBlockSize);
      this.spectralRegistry.register(this.instanceId);
      this.lufsUnfedSamples = 0;
      this.prepared = true;
    }
    /**
     * Process a block of audio.
     * channels[0] = left, channels[1] = right.
     * Modified in-place.
     */
    process(channels, frameCount, sidechain = null) {
      if (!this.prepared || channels.length < 2) return;
      const chL = channels[0];
      const chR = channels[1];
      this.paramQueue.drainInto(this.params);
      const bypass = (this.params[GLOBAL_BYPASS_ID] ?? 0) >= 0.5;
      const inputGainDb = this.params[GLOBAL_INPUT_GAIN_DB_ID] ?? 0;
      const outputGainDb = this.params[GLOBAL_OUTPUT_GAIN_DB_ID] ?? 0;
      const mixPercent = this.params[GLOBAL_MIX_ID] ?? 100;
      const deltaListen = (this.params[GLOBAL_DELTA_LISTEN_ID] ?? 0) >= 0.5;
      const gainMatchEnabled = (this.params[GLOBAL_GAIN_MATCH_ENABLED_ID] ?? 0) >= 0.5;
      const autoGainTargetLufs = this.params[GLOBAL_AUTOGAIN_TARGET_LUFS_ID] ?? -14;
      const qualityModeParam = this.params[GLOBAL_QUALITY_MODE_ID] ?? this.qualityMode;
      if (qualityModeParam !== this.qualityMode) {
        this.qualityMode = Math.round(qualityModeParam);
      }
      if (bypass) {
        if (this.metersEnabled) {
          this.updateInputMeters(chL, chR, frameCount);
          this.updateOutputMeters(chL, chR, frameCount);
        }
        this.totalSamples += frameCount;
        return;
      }
      const inputGainTarget = dbToLinear(inputGainDb);
      const mixTarget = mixPercent / 100;
      const activeChain = this.graphRuntime.getActiveChain();
      this.deltaModule = deltaListen ? this.findDeltaModule(activeChain.modules) : null;
      this.cachedCtx.qualityMode = this.qualityMode;
      let offset = 0;
      while (offset < frameCount) {
        const frames = Math.min(this.maxBlockSize, frameCount - offset);
        const chunkChannels = this.chunkChannels;
        chunkChannels[0] = offset === 0 ? chL : chL.subarray(offset, offset + frames);
        chunkChannels[1] = offset === 0 ? chR : chR.subarray(offset, offset + frames);
        const chunkL = chunkChannels[0];
        const chunkR = chunkChannels[1];
        const chunkSidechain = sidechain && offset > 0 ? sidechain.map((s) => s ? s.subarray(offset, offset + frames) : s) : sidechain;
        const args = this.chunkArgs;
        args.channels = chunkChannels;
        args.sidechain = chunkSidechain;
        args.ctx = this.cachedCtx;
        args.frameCount = frames;
        for (let i = 0; i < frames; i++) {
          const g = this.inputGainSmoother.process(inputGainTarget);
          chunkL[i] *= g;
          chunkR[i] *= g;
        }
        if (this.metersEnabled) {
          this.updateInputMeters(chunkL, chunkR, frames);
        }
        this.dryBufferL.set(chunkL.subarray(0, frames));
        this.dryBufferR.set(chunkR.subarray(0, frames));
        if (this.deltaModule !== null && activeChain.modules.length > 0) {
          let processedToDelta = false;
          for (const moduleType of activeChain.modules) {
            const module = this.getOrCreateModule(moduleType);
            if (!module) continue;
            args.params = this.getModuleParams(moduleType);
            module.process(args);
            if (moduleType === this.deltaModule) {
              processedToDelta = true;
              for (let i = 0; i < frames; i++) {
                chunkL[i] = chunkL[i] - this.dryBufferL[i];
                chunkR[i] = chunkR[i] - this.dryBufferR[i];
              }
              break;
            }
          }
          if (!processedToDelta) {
            for (const moduleType of activeChain.modules) {
              const module = this.getOrCreateModule(moduleType);
              if (!module) continue;
              args.params = this.getModuleParams(moduleType);
              module.process(args);
            }
          }
        } else {
          for (const moduleType of activeChain.modules) {
            const module = this.getOrCreateModule(moduleType);
            if (!module) continue;
            args.params = this.getModuleParams(moduleType);
            module.process(args);
          }
        }
        for (let i = 0; i < frames; i++) {
          const m = this.mixSmoother.process(mixTarget);
          const dryGain = 1 - m;
          chunkL[i] = this.dryBufferL[i] * dryGain + chunkL[i] * m;
          chunkR[i] = this.dryBufferR[i] * dryGain + chunkR[i] * m;
        }
        this.autoGain.setEnabled(gainMatchEnabled);
        this.autoGain.setTargetLufs(autoGainTargetLufs);
        const lufsStale = this.lufsUnfedSamples > this.sampleRate * 3;
        const autoGainCorrectionDb = this.autoGain.process(
          lufsStale ? -70 : this.lufsMeter.getShortTermLufs(),
          frames
        );
        const totalOutputGainTarget = dbToLinear(outputGainDb + autoGainCorrectionDb);
        for (let i = 0; i < frames; i++) {
          const g = this.outputGainSmoother.process(totalOutputGainTarget);
          chunkL[i] *= g;
          chunkR[i] *= g;
        }
        for (let i = 0; i < frames; i++) {
          chunkL[i] = sanitizeSample(chunkL[i]);
          chunkR[i] = sanitizeSample(chunkR[i]);
        }
        if (this.metersEnabled) {
          this.updateOutputMeters(chunkL, chunkR, frames);
          this.lufsUnfedSamples = 0;
        } else if (gainMatchEnabled) {
          this.lufsMeter.process(chunkL, chunkR, frames);
          this.lufsUnfedSamples = 0;
        } else {
          this.lufsUnfedSamples += frames;
        }
        offset += frames;
      }
      this.totalSamples += frameCount;
    }
    /**
     * Reset all state (filters, envelopes, etc.).
     */
    reset() {
      this.inputGainSmoother.reset(
        dbToLinear(this.params[GLOBAL_INPUT_GAIN_DB_ID] ?? 0)
      );
      this.outputGainSmoother.reset(
        dbToLinear(this.params[GLOBAL_OUTPUT_GAIN_DB_ID] ?? 0)
      );
      this.mixSmoother.reset((this.params[GLOBAL_MIX_ID] ?? 100) / 100);
      for (const module of this.modules.values()) {
        module.reset();
      }
      this.inputPeakL = 0;
      this.inputPeakR = 0;
      this.outputPeakL = 0;
      this.outputPeakR = 0;
      this.inputRmsL = 0;
      this.inputRmsR = 0;
      this.outputRmsL = 0;
      this.outputRmsR = 0;
      this.paramQueue.reset();
      this.waveformBuffer.fill(0);
      this.waveformFillPos = 0;
      this.monoSumBuffer.fill(0);
      this.spectrumAnalyzer.reset();
      this.lufsMeter.reset();
      this.autoGain.reset();
      this.spectrumCounter = 0;
      this.lufsUnfedSamples = 0;
      this.bandAnalyzer.reset();
    }
    /**
     * Dispose the processor and unregister from the spectral registry.
     * Called when the plugin instance is being destroyed.
     */
    dispose() {
      this.spectralRegistry.unregister(this.instanceId);
    }
    /**
     * Enable/disable the metering analysis path (control thread). When
     * disabled, process() skips spectrum/band/waveform/peak analysis entirely;
     * the LUFS meter keeps running only while gain-match is enabled (its
     * feedback loop reads it).
     */
    setMetersEnabled(enabled) {
      this.metersEnabled = enabled;
    }
    getMetersEnabled() {
      return this.metersEnabled;
    }
    // ── Parameter management ─────────────────────────────────
    /**
     * Set a single parameter (control thread).
     * Queues the change for the audio thread.
     */
    setParameter(id, value) {
      const clamped = clampParam(id, value);
      this.params[id] = clamped;
      this.paramQueue.enqueue(id, clamped);
    }
    /**
     * Set multiple parameters at once (control thread).
     */
    setParameters(params) {
      for (const [id, value] of Object.entries(params)) {
        this.setParameter(id, value);
      }
    }
    /**
     * Get a parameter value (control thread).
     */
    getParameter(id) {
      return this.params[id] ?? 0;
    }
    /**
     * Get all parameters (control thread).
     */
    getAllParameters() {
      return { ...this.params };
    }
    // ── Module graph ─────────────────────────────────────────
    /**
     * Get the module graph runtime for graph manipulation.
     */
    getGraphRuntime() {
      return this.graphRuntime;
    }
    /**
     * Register a module processor factory.
     */
    registerModuleFactory(moduleType, factory) {
      this.factories.set(moduleType, factory);
      if (this.modules.has(moduleType)) {
        this.cachedCtx.sampleRate = this.sampleRate;
        this.cachedCtx.maxBlockSize = this.maxBlockSize;
        this.cachedCtx.channelCount = this.channelCount;
        this.cachedCtx.qualityMode = this.qualityMode;
        const instance = factory();
        if (this.prepared) instance.prepare(this.cachedCtx);
        this.modules.set(moduleType, instance);
      }
    }
    /**
     * Register all module factories at once.
     */
    registerModuleFactories(factories) {
      for (const [type, factory] of Object.entries(factories)) {
        this.registerModuleFactory(type, factory);
      }
    }
    // ── State ────────────────────────────────────────────────
    /**
     * Load state from a parameter map + module graph.
     */
    loadState(params) {
      for (const [id, value] of Object.entries(params)) {
        this.params[id] = clampParam(id, value);
      }
    }
    // ── Meters ───────────────────────────────────────────────
    /**
     * Get current meter snapshot.
     * Safe to call from the UI thread.
     */
    getMeters() {
      if (!this.metersSnapshot) {
        this.metersSnapshot = createDefaultMeters();
        this.metersSnapshot.global.outputWaveform = new Float32Array(this.waveformBuffer.length);
        this.metersSnapshot.global.inputSpectrumDb = new Float32Array(SPECTRUM_BINS);
      }
      const snapshot = this.metersSnapshot;
      const global = snapshot.global;
      const ag = this.autoGain.getReading();
      global.inputPeakL = ampToDb(this.inputPeakL);
      global.inputPeakR = ampToDb(this.inputPeakR);
      global.inputRmsL = ampToDb(Math.sqrt(this.inputRmsL));
      global.inputRmsR = ampToDb(Math.sqrt(this.inputRmsR));
      global.outputPeakL = ampToDb(this.outputPeakL);
      global.outputPeakR = ampToDb(this.outputPeakR);
      global.outputRmsL = ampToDb(Math.sqrt(this.outputRmsL));
      global.outputRmsR = ampToDb(Math.sqrt(this.outputRmsR));
      global.qualityMode = this.qualityMode;
      global.timestamp = this.totalSamples;
      global.outputWaveform.set(this.waveformBuffer);
      global.inputSpectrumDb.set(this.spectrumAnalyzer.getBins());
      global.outputShortTermLufs = this.lufsMeter.getShortTermLufs();
      global.outputTruePeakDb = this.lufsMeter.getTruePeakDb();
      global.autoGainCorrectionDb = ag.gainCorrectionDb;
      global.autoGainErrorDb = ag.errorDb;
      global.autoGainActive = ag.active;
      snapshot.modules = {};
      for (const [type, module] of this.modules) {
        if (this.graphRuntime.isModuleEnabled(type)) {
          snapshot.modules[type] = module.getMeters();
        }
      }
      snapshot.learn.eq = null;
      snapshot.learn.crossover = null;
      const eqLearnNow = (this.params["eq.learnActive"] ?? 0) >= 0.5;
      if (eqLearnNow) {
        const res = this.eqLearn.getResult();
        snapshot.learn.eq = {
          suggestions: res.suggestions.map((s) => ({
            freqHz: s.freqHz,
            gainDb: s.gainDb,
            q: s.q,
            severity: s.severity
          })),
          isReady: res.isReady
        };
      }
      const xoverLearnNow = (this.params["comp.crossoverLearn"] ?? 0) >= 0.5 || (this.params["gate.crossoverLearn"] ?? 0) >= 0.5 || (this.params["exciter.crossoverLearn"] ?? 0) >= 0.5;
      if (xoverLearnNow) {
        const res = this.xoverLearn.getResult();
        const s3 = res.suggestions3Band;
        snapshot.learn.crossover = {
          freqHz1: s3 ? s3[0].freqHz : res.suggestion2Band?.freqHz ?? 250,
          freqHz2: s3 ? s3[1].freqHz : null,
          confidence: s3 ? s3[0].confidence : res.suggestion2Band?.confidence ?? 0,
          isReady: res.isReady
        };
      }
      return snapshot;
    }
    /**
     * Get detailed LUFS readings (momentary, short-term, integrated, LRA, true-peak).
     */
    getLufsReading() {
      return this.lufsMeter.getReading();
    }
    /**
     * Compute the total processing latency introduced by the active
     * module chain (hybrid FIR crossover, oversampler group delay).
     * Called from the control thread.
     */
    getLatencySamples() {
      let total = 0;
      const activeChain = this.graphRuntime.getActiveChain();
      for (const moduleType of activeChain.modules) {
        const module = this.modules.get(moduleType);
        if (module && typeof module.getLatency === "function") {
          total += module.getLatency();
        }
      }
      return total;
    }
    /**
     * Get current auto-gain controller state for metering.
     */
    getAutoGainReading() {
      return this.autoGain.getReading();
    }
    // ── Internal helpers ─────────────────────────────────────
    getOrCreateModule(moduleType) {
      return this.modules.get(moduleType) ?? null;
    }
    getModuleParams(moduleType) {
      const prefix = `${moduleType}.`;
      let cached = this.moduleParamCache.get(prefix);
      if (!cached) {
        cached = {};
        this.moduleParamCache.set(prefix, cached);
      }
      let keys = this.moduleParamKeys.get(prefix);
      if (!keys) {
        keys = [];
        for (const id of Object.keys(this.params)) {
          if (id.startsWith(prefix)) keys.push(id);
        }
        this.moduleParamKeys.set(prefix, keys);
      }
      for (let i = 0; i < keys.length; i++) {
        cached[keys[i]] = this.params[keys[i]];
      }
      return cached;
    }
    findDeltaModule(activeModules) {
      if (activeModules.length === 0) return null;
      return activeModules[activeModules.length - 1];
    }
    updateInputMeters(chL, chR, frameCount) {
      let peakL = 0;
      let peakR = 0;
      let sumSqL = 0;
      let sumSqR = 0;
      for (let i = 0; i < frameCount; i++) {
        const absL = Math.abs(chL[i]);
        const absR = Math.abs(chR[i]);
        if (absL > peakL) peakL = absL;
        if (absR > peakR) peakR = absR;
        sumSqL += chL[i] * chL[i];
        sumSqR += chR[i] * chR[i];
      }
      this.inputPeakL = Math.max(peakL, this.inputPeakL * 0.95);
      this.inputPeakR = Math.max(peakR, this.inputPeakR * 0.95);
      const alpha = frameCount / (this.sampleRate * 0.3 + frameCount);
      const meanSqL = sumSqL / Math.max(1, frameCount);
      const meanSqR = sumSqR / Math.max(1, frameCount);
      this.inputRmsL = this.inputRmsL * (1 - alpha) + meanSqL * alpha;
      this.inputRmsR = this.inputRmsR * (1 - alpha) + meanSqR * alpha;
      this.spectrumCounter++;
      if (this.spectrumCounter >= _UltinaProcessor.SPECTRUM_INTERVAL) {
        this.spectrumCounter = 0;
        const copyLen = Math.min(frameCount, FFT_SIZE);
        for (let i = 0; i < FFT_SIZE; i++) {
          if (i < copyLen) {
            this.monoSumBuffer[i] = (chL[i] + chR[i]) * 0.5;
          } else {
            this.monoSumBuffer[i] = 0;
          }
        }
        this.spectrumAnalyzer.process(this.monoSumBuffer, FFT_SIZE);
        this.bandAnalyzer.process(this.monoSumBuffer, copyLen);
        this.spectralRegistry.publish(this.instanceId, this.bandAnalyzer.getBandLevelsDb());
        const eqLearnOn = (this.params["eq.learnActive"] ?? 0) >= 0.5;
        if (eqLearnOn && !this.eqLearnWasActive) this.eqLearn.reset();
        this.eqLearnWasActive = eqLearnOn;
        if (eqLearnOn) this.eqLearn.process(this.monoSumBuffer, copyLen);
        const xoverLearnOn = (this.params["comp.crossoverLearn"] ?? 0) >= 0.5 || (this.params["gate.crossoverLearn"] ?? 0) >= 0.5 || (this.params["exciter.crossoverLearn"] ?? 0) >= 0.5;
        if (xoverLearnOn && !this.xoverLearnWasActive) this.xoverLearn.reset();
        this.xoverLearnWasActive = xoverLearnOn;
        if (xoverLearnOn) this.xoverLearn.process(this.monoSumBuffer, copyLen);
      }
    }
    updateOutputMeters(chL, chR, frameCount) {
      let peakL = 0;
      let peakR = 0;
      let sumSqL = 0;
      let sumSqR = 0;
      for (let i = 0; i < frameCount; i++) {
        const absL = Math.abs(chL[i]);
        const absR = Math.abs(chR[i]);
        if (absL > peakL) peakL = absL;
        if (absR > peakR) peakR = absR;
        sumSqL += chL[i] * chL[i];
        sumSqR += chR[i] * chR[i];
      }
      this.outputPeakL = Math.max(peakL, this.outputPeakL * 0.95);
      this.outputPeakR = Math.max(peakR, this.outputPeakR * 0.95);
      const alpha = frameCount / (this.sampleRate * 0.3 + frameCount);
      const meanSqL = sumSqL / Math.max(1, frameCount);
      const meanSqR = sumSqR / Math.max(1, frameCount);
      this.outputRmsL = this.outputRmsL * (1 - alpha) + meanSqL * alpha;
      this.outputRmsR = this.outputRmsR * (1 - alpha) + meanSqR * alpha;
      this.lufsMeter.process(chL, chR, frameCount);
      const bufLen = this.waveformBuffer.length;
      const decimation = Math.max(1, Math.floor(frameCount / bufLen));
      for (let i = 0; i < frameCount; i += decimation) {
        this.waveformBuffer[this.waveformFillPos] = (chL[i] + chR[i]) * 0.5;
        this.waveformFillPos = (this.waveformFillPos + 1) % bufLen;
      }
    }
  };

  // src/effects/ultina-core/dsp/maskingMeter.ts
  function safeNum2(x) {
    return Number.isFinite(x) ? x : 0;
  }
  var MASKING_BANDS = 8;
  var MASKING_FREQS = [
    100,
    250,
    500,
    1e3,
    2e3,
    4e3,
    8e3,
    16e3
  ];
  var MASKING_THRESHOLD_DB = 3;
  var MaskingMeter = class {
    mainFilters = [];
    scFilters = [];
    mainEnv = new Array(MASKING_BANDS).fill(0);
    scEnv = new Array(MASKING_BANDS).fill(0);
    mainBuf = new Float32Array(0);
    scBuf = new Float32Array(0);
    sampleRate = 48e3;
    /** Per-sample smoothing coefficient for envelope tracking. */
    smoothCoef = 0.01;
    /** Pooled result — analyze() runs per audio block on the audio thread;
     * consumers (eqModule) copy out what they need immediately. */
    pooledResult = {
      levels: new Array(MASKING_BANDS).fill(0),
      isMasking: new Array(MASKING_BANDS).fill(false),
      mainLevels: new Array(MASKING_BANDS).fill(0),
      sidechainLevels: new Array(MASKING_BANDS).fill(0)
    };
    prepare(sampleRate2, maxBlockSize) {
      this.sampleRate = sampleRate2;
      this.mainBuf = new Float32Array(maxBlockSize);
      this.scBuf = new Float32Array(maxBlockSize);
      this.smoothCoef = 1 - Math.exp(-1 / (50 / 1e3 * sampleRate2));
      this.reset();
      this.mainFilters = [];
      this.scFilters = [];
      for (let i = 0; i < MASKING_BANDS; i++) {
        const mainBq = createBiquad(1);
        setBandPass(mainBq.coeffs, MASKING_FREQS[i], 4, sampleRate2);
        this.mainFilters.push(mainBq);
        const scBq = createBiquad(1);
        setBandPass(scBq.coeffs, MASKING_FREQS[i], 4, sampleRate2);
        this.scFilters.push(scBq);
      }
    }
    reset() {
      this.mainEnv.fill(0);
      this.scEnv.fill(0);
      for (const bq of this.mainFilters) resetBiquad(bq);
      for (const bq of this.scFilters) resetBiquad(bq);
    }
    /**
     * Analyze masking between main and sidechain signals.
     * Returns per-band masking data. The result object is POOLED — copy out
     * what you need; the next call overwrites every field.
     */
    analyze(main, sidechain, frameCount) {
      this.ensureBuffers(frameCount);
      const blockCoef = 1 - Math.exp(-frameCount / (50 / 1e3 * this.sampleRate));
      const result = this.pooledResult;
      const levels = result.levels;
      const isMasking = result.isMasking;
      const mainLevels = result.mainLevels;
      const scLevels = result.sidechainLevels;
      for (let b = 0; b < MASKING_BANDS; b++) {
        const mainBq = this.mainFilters[b];
        const scBq = this.scFilters[b];
        for (let i = 0; i < frameCount; i++) {
          this.mainBuf[i] = main[i];
        }
        processBiquadChannel(mainBq, this.mainBuf, 0, frameCount);
        let mainPeak = 0;
        for (let i = 0; i < frameCount; i++) {
          const a = Math.abs(this.mainBuf[i]);
          if (a > mainPeak) mainPeak = a;
        }
        for (let i = 0; i < frameCount; i++) {
          this.scBuf[i] = sidechain[i];
        }
        processBiquadChannel(scBq, this.scBuf, 0, frameCount);
        let scPeak = 0;
        for (let i = 0; i < frameCount; i++) {
          const a = Math.abs(this.scBuf[i]);
          if (a > scPeak) scPeak = a;
        }
        this.mainEnv[b] += blockCoef * (mainPeak - this.mainEnv[b]);
        this.scEnv[b] += blockCoef * (scPeak - this.scEnv[b]);
        const mainDb = 20 * Math.log10(Math.max(1e-10, this.mainEnv[b]));
        const scDb = 20 * Math.log10(Math.max(1e-10, this.scEnv[b]));
        mainLevels[b] = mainDb;
        scLevels[b] = scDb;
        const maskingLevel = mainDb - scDb;
        levels[b] = safeNum2(maskingLevel);
        isMasking[b] = maskingLevel > MASKING_THRESHOLD_DB;
      }
      return result;
    }
    ensureBuffers(requiredSize) {
      if (this.mainBuf.length < requiredSize) {
        this.mainBuf = new Float32Array(requiredSize);
        this.scBuf = new Float32Array(requiredSize);
      }
    }
    /**
     * Get current smoothed masking levels (for continuous meter display).
     */
    getLevels() {
      const levels = new Array(MASKING_BANDS);
      for (let b = 0; b < MASKING_BANDS; b++) {
        const mainDb = 20 * Math.log10(Math.max(1e-10, this.mainEnv[b]));
        const scDb = 20 * Math.log10(Math.max(1e-10, this.scEnv[b]));
        levels[b] = mainDb - scDb;
      }
      return levels;
    }
  };

  // src/effects/ultina-core/dsp/modules/eqModule.ts
  var EQ_MAX_BANDS2 = 12;
  var EQ_SHAPES2 = [
    "bell",
    "highShelf",
    "lowShelf",
    "highPass",
    "lowPass",
    "notch",
    "tilt",
    "bandPass",
    "flat",
    "dynamicBell",
    "dynamicShelf",
    "dynamicTilt"
  ];
  var EqModuleProcessor = class {
    sampleRate = 44100;
    maxBlockSize = 512;
    bands = [];
    /**
     * Precomputed per-band parameter key strings — building
     * `eq.band${i}.xxx` templates per band per block creates 150+
     * transient strings on the audio path.
     */
    bandKeys = [];
    soloKeys = [];
    bandLevels = new Array(EQ_MAX_BANDS2).fill(-100);
    bandGainReduction = new Array(EQ_MAX_BANDS2).fill(0);
    maskingData = null;
    // M/S processing buffers
    midBuffer = new Float32Array(0);
    sideBuffer = new Float32Array(0);
    tempL = new Float32Array(0);
    tempR = new Float32Array(0);
    /** Per-sample dynamic-gain trajectory (pass 1 → pass 2 of dynamic bands). */
    dynGainBuf = new Float32Array(0);
    // Masking meter (per-band bandpass analysis)
    maskingMeter = new MaskingMeter();
    prepare(ctx) {
      this.sampleRate = ctx.sampleRate;
      this.maxBlockSize = ctx.maxBlockSize;
      this.ensureBuffers(this.maxBlockSize);
      this.bands = [];
      this.bandKeys = [];
      this.soloKeys = [];
      for (let i = 0; i < EQ_MAX_BANDS2; i++) {
        const p2 = `eq.band${i}`;
        this.bandKeys.push({
          enabled: `${p2}.enabled`,
          freqHz: `${p2}.freqHz`,
          gainDb: `${p2}.gainDb`,
          q: `${p2}.q`,
          shape: `${p2}.shape`,
          mode: `${p2}.mode`,
          dynamicRangeDb: `${p2}.dynamicRangeDb`,
          dynamicThresholdDb: `${p2}.dynamicThresholdDb`,
          sidechainEnabled: `${p2}.sidechainEnabled`,
          dynamicAttackMs: `${p2}.dynamicAttackMs`,
          dynamicReleaseMs: `${p2}.dynamicReleaseMs`,
          dynamicRatio: `${p2}.dynamicRatio`,
          dynamicKneeDb: `${p2}.dynamicKneeDb`
        });
        this.soloKeys.push(`${p2}.solo`);
        this.bands.push({
          filter: createBiquad(2),
          filter2: createBiquad(2),
          detector: createBiquad(2),
          dynFilter: createBiquad(2),
          coeffsInit: false,
          filterSmooth: new BiquadCoeffSmoother(),
          filter2Smooth: new BiquadCoeffSmoother(),
          dynSmooth: new BiquadCoeffSmoother(),
          envelope: new EnvelopeFollower(),
          currentGainDb: 0,
          dirty: true,
          cachedFreq: -1,
          cachedGain: 0,
          cachedQ: 0,
          cachedShape: -1,
          cachedAttackMs: -1,
          cachedReleaseMs: -1
        });
        this.bands[i].envelope.prepare(15, 150, this.sampleRate);
        this.bands[i].filterSmooth.setTimeConstant(10, this.sampleRate);
        this.bands[i].filter2Smooth.setTimeConstant(10, this.sampleRate);
        this.bands[i].dynSmooth.setTimeConstant(10, this.sampleRate);
      }
      this.maskingMeter.prepare(this.sampleRate, this.maxBlockSize);
    }
    process(args) {
      const { channels, frameCount, sidechain, params } = args;
      if (channels.length < 2) return;
      this.ensureBuffers(frameCount);
      const enabled = (params["eq.enabled"] ?? 0) >= 0.5;
      if (!enabled) return;
      const channelMode = Math.round(params["eq.channelMode"] ?? 0);
      const softSat = (params["eq.softSaturation"] ?? 0) >= 0.5;
      const maskingEnabled = (params["eq.maskingMeterEnabled"] ?? 0) >= 0.5;
      const sidechainEnabled = (params["eq.sidechainEnabled"] ?? 0) >= 0.5;
      let soloBand = -1;
      for (let i = 0; i < EQ_MAX_BANDS2; i++) {
        if ((params[this.soloKeys[i]] ?? 0) >= 0.5) {
          soloBand = i;
          break;
        }
      }
      const useMidSide = channelMode === 1 || channelMode === 2;
      if (useMidSide) {
        encodeMidSide(channels[0], channels[1], frameCount, this.midBuffer, this.sideBuffer);
        const targetBuffer = channelMode === 1 ? [this.midBuffer] : [this.sideBuffer];
        const targetSidechain = sidechainEnabled && sidechain ? [sidechain[0]] : null;
        this.processBands(targetBuffer, frameCount, targetSidechain, params, soloBand, maskingEnabled);
        this.applySoftSat(targetBuffer, frameCount, softSat);
        decodeMidSide(this.midBuffer, this.sideBuffer, frameCount, channels[0], channels[1]);
      } else {
        this.processBands(channels, frameCount, sidechainEnabled ? sidechain : null, params, soloBand, maskingEnabled);
        this.applySoftSat(channels, frameCount, softSat);
      }
    }
    reset() {
      for (const band of this.bands) {
        resetBiquad(band.filter);
        resetBiquad(band.filter2);
        resetBiquad(band.detector);
        resetBiquad(band.dynFilter);
        band.coeffsInit = false;
        band.envelope.reset();
        band.currentGainDb = 0;
        band.dirty = true;
      }
      this.bandLevels.fill(-100);
      this.bandGainReduction.fill(0);
      this.maskingData = null;
      this.maskingMeter.reset();
    }
    getMeters() {
      return {
        bandLevels: [...this.bandLevels],
        bandGainReduction: [...this.bandGainReduction],
        masking: this.maskingData ? [...this.maskingData] : null
      };
    }
    // ── Internal helpers ───────────────────────────────────────
    ensureBuffers(requiredSize) {
      if (this.midBuffer.length < requiredSize) {
        this.midBuffer = new Float32Array(requiredSize);
        this.sideBuffer = new Float32Array(requiredSize);
        this.tempL = new Float32Array(requiredSize);
        this.tempR = new Float32Array(requiredSize);
        this.dynGainBuf = new Float32Array(requiredSize);
      }
    }
    // ── Internal processing ────────────────────────────────────
    processBands(channels, frameCount, sidechain, params, soloBand, maskingEnabled) {
      if (soloBand < 0) {
        let anyEnabled = false;
        for (let i = 0; i < EQ_MAX_BANDS2; i++) {
          if ((params[this.bandKeys[i].enabled] ?? 0) >= 0.5) {
            anyEnabled = true;
            break;
          }
        }
        if (!anyEnabled) {
          for (let i = 0; i < EQ_MAX_BANDS2; i++) {
            this.bandLevels[i] = this.bandLevels[i] * 0.8 + -100 * 0.2;
            this.bandGainReduction[i] = this.bandGainReduction[i] * 0.8;
          }
          this.maskingData = null;
          return;
        }
      }
      for (let bandIdx = 0; bandIdx < EQ_MAX_BANDS2; bandIdx++) {
        const band = this.bands[bandIdx];
        const keys = this.bandKeys[bandIdx];
        const bandEnabled = (params[keys.enabled] ?? 0) >= 0.5;
        if (!bandEnabled && soloBand < 0) {
          this.bandLevels[bandIdx] = this.bandLevels[bandIdx] * 0.8 + -100 * 0.2;
          this.bandGainReduction[bandIdx] = this.bandGainReduction[bandIdx] * 0.8;
          continue;
        }
        if (soloBand >= 0 && bandIdx !== soloBand) {
          this.bandLevels[bandIdx] = this.bandLevels[bandIdx] * 0.8 + -100 * 0.2;
          this.bandGainReduction[bandIdx] = this.bandGainReduction[bandIdx] * 0.8;
          continue;
        }
        const freq = params[keys.freqHz] ?? 1e3;
        const gain = clamp(params[keys.gainDb] ?? 0, -24, 24);
        const q = params[keys.q] ?? 1;
        const shape = Math.round(params[keys.shape] ?? 0);
        const mode = Math.round(params[keys.mode] ?? 0);
        const dynRange = params[keys.dynamicRangeDb] ?? 6;
        const dynThreshold = params[keys.dynamicThresholdDb] ?? -24;
        const bandSidechain = (params[keys.sidechainEnabled] ?? 0) >= 0.5;
        const dynAttack = params[keys.dynamicAttackMs] ?? 15;
        const dynRelease = params[keys.dynamicReleaseMs] ?? 150;
        const dynRatio = clamp(params[keys.dynamicRatio] ?? 3, 1, 20);
        const dynKnee = params[keys.dynamicKneeDb] ?? 0;
        if (band.cachedFreq !== freq || band.cachedGain !== gain || band.cachedQ !== q || band.cachedShape !== shape || band.dirty) {
          this.updateBandCoefficients(band, freq, gain, q, shape);
          band.cachedFreq = freq;
          band.cachedGain = gain;
          band.cachedQ = q;
          band.cachedShape = shape;
          band.dirty = false;
        }
        if (band.cachedAttackMs !== dynAttack || band.cachedReleaseMs !== dynRelease) {
          band.envelope.setAttack(dynAttack, this.sampleRate);
          band.envelope.setRelease(dynRelease, this.sampleRate);
          band.cachedAttackMs = dynAttack;
          band.cachedReleaseMs = dynRelease;
        }
        const isDynamic = mode === 1 || shape >= 9;
        const isSidechain = mode === 2 && bandSidechain && sidechain;
        if (isDynamic || isSidechain) {
          this.processDynamicBand(
            band,
            channels,
            frameCount,
            freq,
            q,
            dynRange,
            dynThreshold,
            dynRatio,
            dynKnee,
            dynAttack,
            dynRelease,
            isSidechain ? sidechain : null
          );
        } else {
          processBiquadSmoothed(band.filter, band.filterSmooth, channels, frameCount);
          if (band.cachedShape === 6) {
            processBiquadSmoothed(band.filter2, band.filter2Smooth, channels, frameCount);
          }
        }
        this.measureBandLevel(band, channels, frameCount, bandIdx);
      }
      for (let ch = 0; ch < channels.length; ch++) {
        const data = channels[ch];
        for (let i = 0; i < frameCount; i++) {
          data[i] = sanitizeSample(data[i]);
        }
      }
      if (maskingEnabled && sidechain) {
        this.computeMasking(channels, sidechain, frameCount);
      } else {
        this.maskingData = null;
      }
    }
    updateBandCoefficients(band, freq, gainDb, q, shape) {
      const sr = this.sampleRate;
      const effectiveGain = shape >= 9 ? 0 : gainDb;
      const effectiveShape = shape >= 9 ? shape - 9 : shape;
      const baseShape = EQ_SHAPES2[effectiveShape] ?? "bell";
      switch (baseShape) {
        case "bell":
          setBell(band.filter.coeffs, freq, effectiveGain, q, sr);
          break;
        case "highShelf":
          setHighShelf(band.filter.coeffs, freq, effectiveGain, q, sr);
          break;
        case "lowShelf":
          setLowShelf(band.filter.coeffs, freq, effectiveGain, q, sr);
          break;
        case "highPass":
          setHighPass(band.filter.coeffs, freq, q, sr);
          break;
        case "lowPass":
          setLowPass(band.filter.coeffs, freq, q, sr);
          break;
        case "notch":
          setNotch(band.filter.coeffs, freq, q, sr);
          break;
        case "tilt": {
          const halfGain = effectiveGain * 0.5;
          setLowShelf(band.filter.coeffs, freq, -halfGain, q * 0.7, sr);
          setHighShelf(band.filter2.coeffs, freq, halfGain, q * 0.7, sr);
          band.filter2Smooth.setTarget(band.filter2.coeffs);
          break;
        }
        case "bandPass":
          setBandPass(band.filter.coeffs, freq, q, sr);
          break;
        case "flat":
          band.filter.coeffs.b0 = 1;
          band.filter.coeffs.b1 = 0;
          band.filter.coeffs.b2 = 0;
          band.filter.coeffs.a1 = 0;
          band.filter.coeffs.a2 = 0;
          break;
        default:
          setBell(band.filter.coeffs, freq, effectiveGain, q, sr);
      }
      band.filterSmooth.setTarget(band.filter.coeffs);
      setBandPass(band.detector.coeffs, freq, q * 2, sr);
      setBandPass(band.dynFilter.coeffs, freq, q, sr);
      band.dynSmooth.setTarget(band.dynFilter.coeffs);
      if (!band.coeffsInit) {
        band.filterSmooth.snap();
        band.filter2Smooth.snap();
        band.dynSmooth.snap();
        band.coeffsInit = true;
      }
    }
    /**
     * Compute compressor gain reduction (dB) using threshold, ratio, and knee.
     *
     * Implements the standard feed-forward compressor gain computer
     * with optional soft knee:
     *
     *   For input level x_db:
     *     - If x_db < threshold - knee/2:  reduction = 0 dB
     *     - If x_db > threshold + knee/2:  reduction = -(x_db - threshold) * (1 - 1/ratio)
     *     - In knee zone: quadratic interpolation
     *
     * The result is clamped to [-dynRangeDb, 0].
     *
     * @returns signed gain in dB (always ≤ 0; negative = attenuation)
     */
    computeGainReduction(inputDb, thresholdDb, ratio, kneeDb, dynRangeDb) {
      if (inputDb <= thresholdDb - kneeDb * 0.5) {
        return 0;
      }
      let gainReduction;
      const slope = 1 - 1 / ratio;
      if (kneeDb > 0 && inputDb < thresholdDb + kneeDb * 0.5) {
        const kneeBottom = thresholdDb - kneeDb * 0.5;
        const x = inputDb - kneeBottom;
        gainReduction = slope * (x * x) / (2 * kneeDb);
      } else {
        gainReduction = slope * (inputDb - thresholdDb);
      }
      return Math.max(-dynRangeDb, -gainReduction);
    }
    processDynamicBand(band, channels, frameCount, _freq, _q, dynRangeDb, thresholdDb, ratio, kneeDb, attackMs, releaseMs, sidechain) {
      processBiquadSmoothed(band.filter, band.filterSmooth, channels, frameCount);
      if (band.cachedShape === 11) {
        processBiquadSmoothed(band.filter2, band.filter2Smooth, channels, frameCount);
      }
      const detectSource = sidechain ?? channels;
      const numCh = Math.min(detectSource.length, 2);
      const attackCoef = attackMs > 0 ? 1 - Math.exp(-1 / (attackMs / 1e3 * this.sampleRate)) : 1;
      const releaseCoef = releaseMs > 0 ? 1 - Math.exp(-1 / (releaseMs / 1e3 * this.sampleRate)) : 1;
      for (let ch = 0; ch < numCh; ch++) {
        const src = detectSource[ch];
        const tempBuf = ch === 0 ? this.tempL : this.tempR;
        tempBuf.set(src.subarray(0, frameCount));
        const { b0, b1, b2, a1, a2 } = band.detector.coeffs;
        let z1 = band.detector.z1[ch];
        let z2 = band.detector.z2[ch];
        for (let i = 0; i < frameCount; i++) {
          const x = tempBuf[i];
          const y = b0 * x + z1;
          z1 = b1 * x - a1 * y + z2;
          z2 = b2 * x - a2 * y;
          tempBuf[i] = y;
        }
        band.detector.z1[ch] = z1;
        band.detector.z2[ch] = z2;
      }
      let gainReduction = band.currentGainDb;
      for (let i = 0; i < frameCount; i++) {
        let maxAbs = 0;
        for (let ch = 0; ch < numCh; ch++) {
          const val = Math.abs(ch === 0 ? this.tempL[i] : this.tempR[i]);
          if (val > maxAbs) maxAbs = val;
        }
        const detected = band.envelope.process(maxAbs);
        const detectedDb = 20 * Math.log10(Math.max(1e-10, detected));
        const targetReduction = this.computeGainReduction(
          detectedDb,
          thresholdDb,
          ratio,
          kneeDb,
          dynRangeDb
        );
        const coef = targetReduction < gainReduction ? attackCoef : releaseCoef;
        gainReduction += coef * (targetReduction - gainReduction);
        band.currentGainDb = gainReduction;
        this.dynGainBuf[i] = 1 - Math.pow(10, gainReduction / 20);
      }
      const numOutCh = Math.min(channels.length, 2);
      for (let i = 0; i < frameCount; i++) {
        const c = band.dynSmooth.tick();
        const bandAmount = this.dynGainBuf[i];
        for (let ch = 0; ch < numOutCh; ch++) {
          const data = channels[ch];
          const x = data[i];
          const bp = c.b0 * x + band.dynFilter.z1[ch];
          band.dynFilter.z1[ch] = c.b1 * x - c.a1 * bp + band.dynFilter.z2[ch];
          band.dynFilter.z2[ch] = c.b2 * x - c.a2 * bp;
          data[i] = sanitizeSample(x - bp * bandAmount);
        }
      }
      const bandIdx = this.bands.indexOf(band);
      if (bandIdx >= 0) {
        this.bandGainReduction[bandIdx] = -band.currentGainDb;
      }
    }
    measureBandLevel(_band, channels, frameCount, bandIdx) {
      let peak = 0;
      for (let i = 0; i < frameCount; i++) {
        const abs = Math.abs(channels[0][i]);
        if (abs > peak) peak = abs;
      }
      const db = 20 * Math.log10(Math.max(1e-10, peak));
      this.bandLevels[bandIdx] = this.bandLevels[bandIdx] * 0.9 + db * 0.1;
    }
    applySoftSat(channels, frameCount, enabled) {
      if (!enabled) return;
      for (let ch = 0; ch < channels.length; ch++) {
        const data = channels[ch];
        for (let i = 0; i < frameCount; i++) {
          const x = data[i];
          const saturated = fastTanh(x * 2) * 0.5;
          data[i] = sanitizeSample(x * 0.85 + saturated * 0.15);
        }
      }
    }
    computeMasking(channels, sidechain, frameCount) {
      const result = this.maskingMeter.analyze(
        channels[0],
        sidechain[0] ?? channels[0],
        frameCount
      );
      this.maskingData = result.levels;
    }
  };

  // src/effects/ultina-core/dsp/multiband.ts
  var LR4_Q = 1 / Math.SQRT2;
  var MAX_BANDS = 3;
  var CrossoverNetwork = class {
    splits = [];
    bandCount = 1;
    channelCount = 2;
    maxBlockSize = 8192;
    // ── Pre-allocated LR4 work buffers (allocation-free process) ──
    lr4Work = [];
    lr4LpOut = [];
    lr4MidLp = [];
    // ── FIR (linear-phase / hybrid) state ──────────────────────
    crossoverMode = "analog";
    firNumTaps = DEFAULT_FIR_TAPS;
    firLatency = (DEFAULT_FIR_TAPS - 1) / 2;
    /** FIR filters indexed [splitIndex][channelIndex]. */
    firFilters = [];
    /** Cached frequencies so we only re-design when changed. */
    firFreqCache = [];
    /** Scratch buffer for LP outputs during splitFir (one per split). */
    firLpOuts = [0, 0];
    prepare(channelCount, bandCount, maxBlockSize = 8192) {
      this.channelCount = Math.max(1, channelCount);
      this.bandCount = bandCount;
      this.maxBlockSize = Math.max(1, maxBlockSize);
      const splitCount = MAX_BANDS - 1;
      this.splits = [];
      for (let i = 0; i < splitCount; i++) {
        this.splits.push({
          lp: [createBiquad(this.channelCount), createBiquad(this.channelCount)],
          hp: [createBiquad(this.channelCount), createBiquad(this.channelCount)]
        });
      }
      this.lr4Work = [];
      this.lr4LpOut = [];
      this.lr4MidLp = [];
      for (let ch = 0; ch < this.channelCount; ch++) {
        this.lr4Work.push(new Float32Array(this.maxBlockSize));
        this.lr4LpOut.push(new Float32Array(this.maxBlockSize));
        this.lr4MidLp.push(new Float32Array(this.maxBlockSize));
      }
      this.firNumTaps = DEFAULT_FIR_TAPS;
      this.firLatency = (this.firNumTaps - 1) / 2;
      this.firFilters = [];
      this.firFreqCache = [];
      for (let s = 0; s < splitCount; s++) {
        const channelFilters = [];
        for (let c = 0; c < this.channelCount; c++) {
          channelFilters.push(createFirFilter(this.firNumTaps));
        }
        this.firFilters.push(channelFilters);
        this.firFreqCache.push(0);
      }
    }
    setCrossoverMode(mode) {
      if (mode !== this.crossoverMode) {
        this.crossoverMode = mode;
        this.reset();
      }
    }
    getCrossoverMode() {
      return this.crossoverMode;
    }
    /** Latency introduced by the crossover in samples (0 for analog, M for hybrid). */
    getLatency() {
      return this.crossoverMode === "hybrid" && this.bandCount > 1 ? this.firLatency : 0;
    }
    setBandCount(bandCount) {
      if (bandCount !== this.bandCount) {
        this.bandCount = bandCount;
        this.reset();
      }
    }
    setFrequency(splitIndex, freqHz, sampleRate2) {
      if (splitIndex >= 0 && splitIndex < this.splits.length) {
        const split = this.splits[splitIndex];
        for (const bq of split.lp) setLowPass(bq.coeffs, freqHz, LR4_Q, sampleRate2);
        for (const bq of split.hp) setHighPass(bq.coeffs, freqHz, LR4_Q, sampleRate2);
      }
      if (splitIndex >= 0 && splitIndex < this.firFilters.length) {
        if (this.firFreqCache[splitIndex] !== freqHz) {
          this.firFreqCache[splitIndex] = freqHz;
          for (const fir of this.firFilters[splitIndex]) {
            designLowPassFir(fir, freqHz, sampleRate2, this.firNumTaps);
          }
        }
      }
    }
    reset() {
      for (const split of this.splits) {
        for (const bq of split.lp) resetBiquad(bq);
        for (const bq of split.hp) resetBiquad(bq);
      }
      for (const splitFirs of this.firFilters) {
        for (const fir of splitFirs) resetFirFilter(fir);
      }
    }
    getBandCount() {
      return this.bandCount;
    }
    /**
     * Split input into N bands. Writes into bandOut[0..N-1].
     * Each bandOut element is an array of Float32Array (per channel).
     * Dispatches between LR4 (analog) and FIR (hybrid) modes.
     */
    split(input, bandOut, frameCount, _sampleRate) {
      const chCount = Math.min(this.channelCount, input.length);
      if (this.bandCount === 1) {
        for (let ch = 0; ch < chCount; ch++) {
          bandOut[0][ch].set(input[ch].subarray(0, frameCount));
        }
        return;
      }
      if (this.crossoverMode === "hybrid") {
        this.splitFir(input, bandOut, frameCount);
      } else {
        this.splitLr4(input, bandOut, frameCount);
      }
    }
    /**
     * FIR (linear-phase) crossover split.
     *
     * Uses complementary FIR LP/HP pairs designed via windowed-sinc.
     * Perfect reconstruction: low + mid + high = delayed input.
     *
     * Band assignment (all share the same group delay M):
     *   - Low  = LP(freq1, input)
     *   - High = input_delayed - LP(freq2, input)  [or LP(freq1) for 2-band]
     *   - Mid  = LP(freq2) - LP(freq1)              [3-band only]
     */
    splitFir(input, bandOut, frameCount) {
      const N = this.firNumTaps;
      const M = this.firLatency;
      const ch = Math.min(this.channelCount, input.length);
      const numSplits = Math.min(this.firFilters.length, this.bandCount - 1);
      for (let i = 0; i < frameCount; i++) {
        for (let c = 0; c < ch; c++) {
          const lpOuts = this.firLpOuts;
          for (let s = 0; s < numSplits; s++) {
            lpOuts[s] = processFirSample(this.firFilters[s][c], input[c][i]);
          }
          const fir0 = this.firFilters[0][c];
          let delayIdx = fir0.writePos - 1 - M;
          if (delayIdx < 0) delayIdx += N;
          const delayed = fir0.delayLine[delayIdx];
          if (this.bandCount === 2) {
            bandOut[0][c][i] = sanitizeSample(lpOuts[0]);
            bandOut[1][c][i] = sanitizeSample(delayed - lpOuts[0]);
          } else {
            bandOut[0][c][i] = sanitizeSample(lpOuts[0]);
            bandOut[2][c][i] = sanitizeSample(delayed - lpOuts[1]);
            bandOut[1][c][i] = sanitizeSample(lpOuts[1] - lpOuts[0]);
          }
        }
      }
    }
    /**
     * LR4 (analog, zero-latency) crossover split.
     * Uses cascaded Butterworth biquad sections.
     * All work buffers are pre-allocated — no allocations in this path.
     */
    splitLr4(input, bandOut, frameCount) {
      if (this.bandCount === 1) {
        for (let ch = 0; ch < this.channelCount; ch++) {
          bandOut[0][ch].set(input[ch].subarray(0, frameCount));
        }
        return;
      }
      const chCount = Math.min(this.channelCount, input.length);
      for (let ch = 0; ch < chCount; ch++) {
        this.lr4Work[ch].set(input[ch].subarray(0, frameCount));
      }
      const activeSplits = this.bandCount - 1;
      for (let s = 0; s < activeSplits; s++) {
        const split = this.splits[s];
        for (let ch = 0; ch < chCount; ch++) {
          this.lr4LpOut[ch].set(this.lr4Work[ch].subarray(0, frameCount));
        }
        for (const bq of split.lp) {
          for (let ch = 0; ch < this.channelCount; ch++) {
            processBiquadInPlace(bq, this.lr4LpOut[ch], ch, frameCount);
          }
        }
        for (const bq of split.hp) {
          for (let ch = 0; ch < chCount; ch++) {
            processBiquadInPlace(bq, this.lr4Work[ch], ch, frameCount);
          }
        }
        if (s === 0) {
          for (let ch = 0; ch < chCount; ch++) {
            bandOut[0][ch].set(this.lr4LpOut[ch].subarray(0, frameCount));
          }
        }
        if (s === activeSplits - 1) {
          const lastBand = this.bandCount - 1;
          for (let ch = 0; ch < chCount; ch++) {
            bandOut[lastBand][ch].set(this.lr4Work[ch].subarray(0, frameCount));
          }
        } else {
          if (this.bandCount === 3 && s === 0) {
            for (let ch = 0; ch < chCount; ch++) {
              bandOut[1][ch].set(this.lr4Work[ch].subarray(0, frameCount));
            }
          }
        }
      }
      if (this.bandCount === 3 && this.splits.length === 2) {
        const split1 = this.splits[1];
        for (let ch = 0; ch < chCount; ch++) {
          this.lr4MidLp[ch].set(bandOut[1][ch].subarray(0, frameCount));
        }
        for (const bq of split1.lp) {
          for (let ch = 0; ch < this.channelCount; ch++) {
            processBiquadInPlace(bq, this.lr4MidLp[ch], ch, frameCount);
          }
        }
        for (let ch = 0; ch < this.channelCount; ch++) {
          bandOut[1][ch].set(this.lr4MidLp[ch].subarray(0, frameCount));
        }
      }
    }
    /**
     * Sum bands back together. LR4 crossovers sum to flat (allpass).
     */
    sum(bands, output, frameCount) {
      const chCount = Math.min(this.channelCount, output.length);
      for (let ch = 0; ch < chCount; ch++) {
        output[ch].fill(0, 0, frameCount);
        for (let b = 0; b < this.bandCount; b++) {
          for (let i = 0; i < frameCount; i++) {
            output[ch][i] += bands[b][ch][i];
          }
        }
        for (let i = 0; i < frameCount; i++) {
          output[ch][i] = sanitizeSample(output[ch][i]);
        }
      }
    }
  };
  function processBiquadInPlace(bq, data, ch, frameCount) {
    const { b0, b1, b2, a1, a2 } = bq.coeffs;
    let z1 = bq.z1[ch];
    let z2 = bq.z2[ch];
    for (let i = 0; i < frameCount; i++) {
      const x = data[i];
      const y = b0 * x + z1;
      z1 = b1 * x - a1 * y + z2;
      z2 = b2 * x - a2 * y;
      data[i] = y;
    }
    bq.z1[ch] = z1;
    bq.z2[ch] = z2;
    if (!Number.isFinite(z1) || !Number.isFinite(z2)) {
      bq.z1[ch] = 0;
      bq.z2[ch] = 0;
    }
  }
  var MidSideProcessor = class {
    midBuf = new Float32Array(0);
    sideBuf = new Float32Array(0);
    prepare(maxBlockSize, _channelCount) {
      this.midBuf = new Float32Array(maxBlockSize);
      this.sideBuf = new Float32Array(maxBlockSize);
    }
    /**
     * If channel mode is mid or side, encode to M/S.
     * Returns the active channel buffer for processing.
     * If stereo mode, returns null (process both channels directly).
     */
    encode(left, right, frameCount, channelMode) {
      if (channelMode !== "mid" && channelMode !== "side") return null;
      if (frameCount > this.midBuf.length) {
        this.midBuf = new Float32Array(frameCount);
        this.sideBuf = new Float32Array(frameCount);
      }
      encodeMidSide(left, right, frameCount, this.midBuf, this.sideBuf);
      return { mid: this.midBuf, side: this.sideBuf };
    }
    /**
     * Decode M/S back to L/R and write into output buffers.
     * Only called when channel mode was mid or side.
     */
    decode(mid, side, frameCount, leftOut, rightOut) {
      decodeMidSide(mid, side, frameCount, leftOut, rightOut);
    }
  };
  var TransientSustainSeparator = class {
    transientEnv = [0, 0];
    sustainEnv = [0, 0];
    fastCoef = 0;
    slowCoef = 0;
    prepare(sampleRate2) {
      this.fastCoef = 1 - Math.exp(-1 / (5 / 1e3 * sampleRate2));
      this.slowCoef = 1 - Math.exp(-1 / (200 / 1e3 * sampleRate2));
      this.transientEnv = [0, 0];
      this.sustainEnv = [0, 0];
    }
    reset() {
      this.transientEnv.fill(0);
      this.sustainEnv.fill(0);
    }
    separate(input, frameCount, transientBuf, sustainBuf, channelIndex = 0) {
      const ch = channelIndex & 1;
      let tEnv = this.transientEnv[ch];
      let sEnv = this.sustainEnv[ch];
      for (let i = 0; i < frameCount; i++) {
        const abs = Math.abs(input[i]);
        if (abs > tEnv) {
          tEnv += this.fastCoef * (abs - tEnv);
        } else {
          tEnv += this.slowCoef * (abs - tEnv);
        }
        sEnv += this.slowCoef * (abs - sEnv);
        const tAmount = sEnv > 1e-8 ? clamp(tEnv / sEnv - 1, 0, 1) : 0;
        transientBuf[i] = input[i] * tAmount;
        sustainBuf[i] = input[i] * (1 - tAmount);
      }
      this.transientEnv[ch] = tEnv;
      this.sustainEnv[ch] = sEnv;
    }
  };
  var MultibandProcessor = class {
    crossover = new CrossoverNetwork();
    midSide = new MidSideProcessor();
    tsSeparator = new TransientSustainSeparator();
    // Preallocated band buffers
    bandBuffers = [];
    transientBuf = new Float32Array(0);
    sustainBuf = new Float32Array(0);
    sampleRate = 48e3;
    channelCount = 2;
    maxBlockSize = 8192;
    prepared = false;
    prepare(sampleRate2, channelCount, maxBlockSize, bandCount) {
      this.sampleRate = sampleRate2;
      this.channelCount = Math.max(1, channelCount);
      this.maxBlockSize = Math.max(1, maxBlockSize);
      this.crossover.prepare(this.channelCount, bandCount, this.maxBlockSize);
      this.midSide.prepare(this.maxBlockSize, this.channelCount);
      this.tsSeparator.prepare(sampleRate2);
      this.bandBuffers = [];
      for (let b = 0; b < MAX_BANDS; b++) {
        const channels = [];
        for (let ch = 0; ch < this.channelCount; ch++) {
          channels.push(new Float32Array(this.maxBlockSize));
        }
        this.bandBuffers.push(channels);
      }
      this.transientBuf = new Float32Array(this.maxBlockSize);
      this.sustainBuf = new Float32Array(this.maxBlockSize);
      this.prepared = true;
    }
    setCrossover(splitIndex, freqHz) {
      this.crossover.setFrequency(splitIndex, freqHz, this.sampleRate);
    }
    setCrossoverMode(mode) {
      this.crossover.setCrossoverMode(mode);
    }
    getCrossoverLatency() {
      return this.crossover.getLatency();
    }
    setBandCount(bandCount) {
      if (bandCount !== this.crossover.getBandCount()) {
        this.crossover.setBandCount(bandCount);
      }
    }
    reset() {
      this.crossover.reset();
      this.tsSeparator.reset();
      for (const band of this.bandBuffers) {
        for (const ch of band) ch.fill(0);
      }
    }
    /**
     * Process audio through the multiband chain.
     * The bandProcessFn is called for each band.
     */
    process(channels, frameCount, bandProcessFn, channelMode = "stereo") {
      if (!this.prepared) return;
      if (frameCount > this.maxBlockSize) {
        frameCount = this.maxBlockSize;
      }
      if (channelMode === "mid" || channelMode === "side") {
        if (this.channelCount < 2) {
          this.processBands(channels, frameCount, bandProcessFn);
          return;
        }
        const msResult = this.midSide.encode(channels[0], channels[1], frameCount, channelMode);
        if (!msResult) return;
        const target = channelMode === "mid" ? msResult.mid : msResult.side;
        const singleChannel = [target];
        this.processBands(singleChannel, frameCount, bandProcessFn);
        this.midSide.decode(msResult.mid, msResult.side, frameCount, channels[0], channels[1]);
      } else if (channelMode === "transient" || channelMode === "sustain") {
        if (this.channelCount < 2) {
          this.tsSeparator.separate(
            channels[0],
            frameCount,
            this.transientBuf,
            this.sustainBuf,
            0
          );
          const target = channelMode === "transient" ? this.transientBuf : this.sustainBuf;
          const singleChannel = [target];
          this.processBands(singleChannel, frameCount, bandProcessFn);
          const otherBuf = channelMode === "transient" ? this.sustainBuf : this.transientBuf;
          for (let i = 0; i < frameCount; i++) {
            channels[0][i] = target[i] + otherBuf[i];
          }
        } else {
          for (let ch = 0; ch < this.channelCount; ch++) {
            this.tsSeparator.separate(
              channels[ch],
              frameCount,
              this.transientBuf,
              this.sustainBuf,
              ch
            );
            const target = channelMode === "transient" ? this.transientBuf : this.sustainBuf;
            const otherBuf = channelMode === "transient" ? this.sustainBuf : this.transientBuf;
            const singleChannel = [target];
            this.processBands(singleChannel, frameCount, bandProcessFn);
            for (let i = 0; i < frameCount; i++) {
              channels[ch][i] = target[i] + otherBuf[i];
            }
          }
        }
      } else {
        this.processBands(channels, frameCount, bandProcessFn);
      }
    }
    processBands(channels, frameCount, bandProcessFn) {
      const bandCount = this.crossover.getBandCount();
      if (bandCount === 1) {
        bandProcessFn(0, channels, frameCount);
        return;
      }
      this.crossover.split(channels, this.bandBuffers, frameCount, this.sampleRate);
      for (let b = 0; b < bandCount; b++) {
        bandProcessFn(b, this.bandBuffers[b], frameCount);
      }
      this.crossover.sum(this.bandBuffers, channels, frameCount);
    }
  };

  // src/effects/ultina-core/contracts/channelModes.ts
  var CHANNEL_MODES = [
    "stereo",
    "mid",
    "side",
    "transient",
    "sustain"
  ];
  function channelModeFromValue(v) {
    const idx = Math.round(v);
    return CHANNEL_MODES[Math.max(0, Math.min(CHANNEL_MODES.length - 1, idx))];
  }

  // src/effects/ultina-core/dsp/modules/compModule.ts
  var COMP_MAX_BANDS = 3;
  var COMP_MODES = ["punch", "modern", "vintage", "opto", "fet"];
  var DETECTION_MODES = ["peak", "rms", "trueEnvelope"];
  var CompModuleProcessor = class {
    sampleRate = 44100;
    maxBlockSize = 512;
    bands = [];
    multiband = new MultibandProcessor();
    // Sidechain HPF
    scHpf = createBiquad(2);
    // Per-band detector HPF (comp.detectorHpfHz) — filters each band's own
    // detection signal so low-frequency energy does not trigger compression.
    // Two cascaded 2-pole stages = 24 dB/oct, so a 120 Hz setting really
    // rejects a 60 Hz rumble (a single 2-pole only manages −12 dB there,
    // which still crosses the threshold). Inactive at the 20 Hz default;
    // has no effect while an external sidechain drives the detector
    // (comp.sidechainHpfHz covers that path).
    detHpf = [];
    detHpfBufs = [];
    // Dry buffer for mix
    dryL = new Float32Array(0);
    dryR = new Float32Array(0);
    // Meter state
    gainReduction = new Array(COMP_MAX_BANDS).fill(0);
    outputLevels = new Array(COMP_MAX_BANDS).fill(-100);
    autoMakeupDb = 0;
    autoMakeupSmoother = new OnePoleSmoother();
    // Cached config
    cachedBandCount = -1;
    cachedXover1 = -1;
    cachedXover2 = -1;
    prepare(ctx) {
      this.sampleRate = ctx.sampleRate;
      this.maxBlockSize = ctx.maxBlockSize;
      this.ensureBuffers(this.maxBlockSize);
      this.scHpf = createBiquad(2);
      this.detHpf = [];
      this.detHpfBufs = [];
      for (let i = 0; i < COMP_MAX_BANDS; i++) {
        this.detHpf.push([createBiquad(1), createBiquad(1)]);
        this.detHpfBufs.push(new Float32Array(this.maxBlockSize));
      }
      this.bands = [];
      for (let i = 0; i < COMP_MAX_BANDS; i++) {
        this.bands.push({
          peakEnv: new EnvelopeFollower(),
          rmsDetector: new RmsDetector(),
          gainReductionDb: 0,
          targetGrDb: 0,
          attackCoef: 0,
          releaseCoef: 0,
          holdCounter: 0,
          prevDetected: 0,
          optoMemory: 0
        });
        this.bands[i].peakEnv.prepare(10, 100, this.sampleRate);
        this.bands[i].rmsDetector.prepare(10, this.sampleRate);
      }
      this.autoMakeupSmoother.setTimeConstant(50, this.sampleRate);
      this.multiband.prepare(this.sampleRate, 2, this.maxBlockSize, 1);
    }
    process(args) {
      const { channels, frameCount, sidechain, params } = args;
      if (channels.length < 2) return;
      const enabled = (params["comp.enabled"] ?? 0) >= 0.5;
      if (!enabled) return;
      this.ensureBuffers(frameCount);
      const mode = COMP_MODES[Math.round(clamp(params["comp.mode"] ?? 1, 0, COMP_MODES.length - 1))];
      const detectionMode = DETECTION_MODES[Math.round(clamp(params["comp.detectionMode"] ?? 1, 0, 2))];
      const thresholdDb = params["comp.thresholdDb"] ?? -20;
      const ratio = clamp(params["comp.ratio"] ?? 3, 1, 20);
      const attackMs = clamp(params["comp.attackMs"] ?? 10, 0.1, 200);
      const releaseMs = clamp(params["comp.releaseMs"] ?? 100, 5, 2e3);
      const kneeDb = clamp(params["comp.kneeDb"] ?? 0, 0, 24);
      const makeupDb = params["comp.makeupDb"] ?? 0;
      const makeupAuto = (params["comp.makeupAuto"] ?? 0) >= 0.5;
      const autoRelease = (params["comp.autoRelease"] ?? 0) >= 0.5;
      const mixPercent = clamp(params["comp.mix"] ?? 100, 0, 100);
      const scEnabled = (params["comp.sidechainEnabled"] ?? 0) >= 0.5;
      const scHpfHz = clamp(params["comp.sidechainHpfHz"] ?? 20, 20, 2e3);
      const detHpfHz = clamp(params["comp.detectorHpfHz"] ?? 20, 20, 1e3);
      const bandCount = Math.round(clamp(params["comp.bandCount"] ?? 1, 1, 3));
      const xover1 = clamp(params["comp.crossoverHz1"] ?? 250, 20, 2e4);
      const xover2 = clamp(params["comp.crossoverHz2"] ?? 2500, 20, 2e4);
      const channelModeRaw = Math.round(clamp(params["comp.channelMode"] ?? 0, 0, 4));
      const channelMode = channelModeFromValue(channelModeRaw);
      const deltaListen = (params["comp.delta"] ?? 0) >= 0.5;
      const { effectiveAttack, effectiveRelease, effectiveRatio, effectiveKneeDb } = this.applyMode(mode, attackMs, releaseMs, ratio, kneeDb);
      this.updateMultiband(bandCount, xover1, xover2);
      const xoverMode = (params["comp.crossoverMode"] ?? 0) >= 0.5 ? "hybrid" : "analog";
      this.multiband.setCrossoverMode(xoverMode);
      this.dryL.set(channels[0].subarray(0, frameCount));
      this.dryR.set(channels[1].subarray(0, frameCount));
      let detectSource = channels;
      if (scEnabled && sidechain && sidechain.length >= 2) {
        setHighPass(this.scHpf.coeffs, scHpfHz, 0.707, this.sampleRate);
        this.scHpfBufferL.set(sidechain[0].subarray(0, frameCount));
        this.scHpfBufferR.set(sidechain[1].subarray(0, frameCount));
        const scChannels = [this.scHpfBufferL, this.scHpfBufferR];
        processBiquad(this.scHpf, scChannels, frameCount);
        detectSource = scChannels;
      }
      const bandThresholdDb = [
        params["comp.band0.thresholdDb"] ?? thresholdDb,
        params["comp.band1.thresholdDb"] ?? thresholdDb,
        params["comp.band2.thresholdDb"] ?? thresholdDb
      ];
      const scActive = scEnabled && sidechain && sidechain.length >= 2;
      const detHpfActive = detHpfHz > 20.5 && !scActive && this.detHpf.length === COMP_MAX_BANDS;
      if (detHpfActive) {
        for (let b = 0; b < COMP_MAX_BANDS; b++) {
          for (const stage of this.detHpf[b]) {
            setHighPass(stage.coeffs, detHpfHz, 0.707, this.sampleRate);
          }
        }
      }
      this.multiband.process(
        channels,
        frameCount,
        (bandIdx, bandChannels, bandFrames) => {
          this.processBand(
            bandIdx,
            bandChannels,
            bandFrames,
            scActive ? detectSource : null,
            bandThresholdDb[bandIdx],
            effectiveAttack,
            effectiveRelease,
            effectiveRatio,
            effectiveKneeDb,
            detectionMode,
            autoRelease,
            mode,
            detHpfActive
          );
        },
        channelMode
      );
      let finalMakeup = makeupDb;
      if (makeupAuto) {
        let avgGr = 0;
        for (let b = 0; b < bandCount; b++) {
          avgGr += this.gainReduction[b];
        }
        avgGr /= bandCount;
        const targetMakeup = avgGr * 0.5;
        for (let i = 0; i < frameCount; i++) {
          this.autoMakeupDb = this.autoMakeupSmoother.process(targetMakeup);
        }
        finalMakeup += this.autoMakeupDb;
      }
      if (finalMakeup !== 0) {
        const makeupLinear = dbToLinear(finalMakeup);
        for (let ch = 0; ch < 2; ch++) {
          const data = channels[ch];
          for (let i = 0; i < frameCount; i++) {
            data[i] = sanitizeSample(data[i] * makeupLinear);
          }
        }
      }
      if (mode === "fet") {
        let avgGr = 0;
        for (let b = 0; b < bandCount; b++) {
          avgGr += this.gainReduction[b];
        }
        avgGr /= Math.max(1, bandCount);
        const bite = clamp(avgGr / 10, 0, 1) * 0.25;
        if (bite > 1e-3) {
          for (let ch = 0; ch < 2; ch++) {
            const data = channels[ch];
            for (let i = 0; i < frameCount; i++) {
              const x = data[i];
              const sat = fastTanh(x * 1.7) / 1.7;
              data[i] = sanitizeSample(x * (1 - bite) + sat * bite);
            }
          }
        }
      }
      if (mode === "vintage" && finalMakeup > 0.5) {
        const warmth = clamp(finalMakeup / 12, 0, 0.3);
        for (let ch = 0; ch < 2; ch++) {
          const data = channels[ch];
          for (let i = 0; i < frameCount; i++) {
            const x = data[i];
            const sat = fastTanh(x * 1.5) / 1.5;
            data[i] = sanitizeSample(x * (1 - warmth) + sat * warmth);
          }
        }
      }
      const mix = mixPercent / 100;
      if (mix < 0.999) {
        for (let i = 0; i < frameCount; i++) {
          const wetL = channels[0][i];
          const wetR = channels[1][i];
          channels[0][i] = sanitizeSample(wetL * mix + this.dryL[i] * (1 - mix));
          channels[1][i] = sanitizeSample(wetR * mix + this.dryR[i] * (1 - mix));
        }
      }
      if (deltaListen) {
        for (let i = 0; i < frameCount; i++) {
          channels[0][i] = sanitizeSample(channels[0][i] - this.dryL[i]);
          channels[1][i] = sanitizeSample(channels[1][i] - this.dryR[i]);
        }
      }
    }
    reset() {
      for (const band of this.bands) {
        band.peakEnv.reset();
        band.rmsDetector.reset();
        band.gainReductionDb = 0;
        band.targetGrDb = 0;
        band.holdCounter = 0;
        band.prevDetected = 0;
        band.optoMemory = 0;
      }
      this.gainReduction.fill(0);
      this.outputLevels.fill(-100);
      this.autoMakeupDb = 0;
      this.autoMakeupSmoother.reset(0);
      this.multiband.reset();
      resetBiquad(this.scHpf);
      for (const stages of this.detHpf) {
        for (const stage of stages) resetBiquad(stage);
      }
    }
    getMeters() {
      return {
        gainReduction: [...this.gainReduction],
        outputLevels: [...this.outputLevels],
        autoMakeupDb: this.autoMakeupDb
      };
    }
    /** Hybrid crossover group delay (samples). */
    getLatency() {
      return this.multiband.getCrossoverLatency();
    }
    // ── Internal ──────────────────────────────────────────────
    scHpfBufferL = new Float32Array(0);
    scHpfBufferR = new Float32Array(0);
    ensureBuffers(requiredSize) {
      if (this.dryL.length < requiredSize) {
        this.dryL = new Float32Array(requiredSize);
        this.dryR = new Float32Array(requiredSize);
        this.scHpfBufferL = new Float32Array(requiredSize);
        this.scHpfBufferR = new Float32Array(requiredSize);
      }
      for (let b = 0; b < this.detHpfBufs.length; b++) {
        if (this.detHpfBufs[b].length < requiredSize) {
          this.detHpfBufs[b] = new Float32Array(requiredSize);
        }
      }
    }
    applyMode(mode, attackMs, releaseMs, ratio, kneeDb) {
      switch (mode) {
        case "punch":
          return {
            effectiveAttack: attackMs * 0.5,
            // Faster attack
            effectiveRelease: releaseMs * 0.7,
            effectiveRatio: ratio * 1.15,
            // Slightly more aggressive
            effectiveKneeDb: kneeDb
          };
        case "vintage":
          return {
            effectiveAttack: attackMs * 2,
            // Slower attack
            effectiveRelease: releaseMs * 1.5,
            // Clamp to ≥1: the ratio knob at 1–1.17 would otherwise invert
            // the slope and turn compression into upward gain.
            effectiveRatio: Math.max(1, ratio * 0.85),
            // Gentler
            effectiveKneeDb: kneeDb
          };
        case "opto":
          return {
            effectiveAttack: clamp(attackMs * 1.5, 8, 40),
            effectiveRelease: clamp(releaseMs * 2.5, 50, 2e3),
            effectiveRatio: Math.max(1, ratio * 0.8),
            effectiveKneeDb: Math.max(kneeDb, 6)
          };
        case "fet":
          return {
            effectiveAttack: clamp(attackMs * 0.05, 0.02, 1),
            effectiveRelease: clamp(releaseMs * 0.8, 5, 1100),
            effectiveRatio: clamp(ratio * 1.3, 1, 20),
            effectiveKneeDb: kneeDb
          };
        case "modern":
        default:
          return {
            effectiveAttack: attackMs,
            effectiveRelease: releaseMs,
            effectiveRatio: ratio,
            effectiveKneeDb: kneeDb
          };
      }
    }
    updateMultiband(bandCount, xover1, xover2) {
      if (this.cachedBandCount !== bandCount) {
        this.multiband.setBandCount(bandCount);
        this.cachedBandCount = bandCount;
        this.cachedXover1 = -1;
        this.cachedXover2 = -1;
      }
      if (this.cachedXover1 !== xover1) {
        this.multiband.setCrossover(0, xover1);
        this.cachedXover1 = xover1;
      }
      if (this.cachedXover2 !== xover2 && bandCount >= 3) {
        this.multiband.setCrossover(1, xover2);
        this.cachedXover2 = xover2;
      }
    }
    processBand(bandIdx, channels, frameCount, sidechainSource, thresholdDb, attackMs, releaseMs, ratio, kneeDb, detectionMode, autoRelease, mode, detHpfActive) {
      const band = this.bands[bandIdx];
      band.attackCoef = smoothCoef(attackMs, this.sampleRate);
      band.releaseCoef = smoothCoef(releaseMs, this.sampleRate);
      const autoReleaseFastCoef = autoRelease ? smoothCoef(releaseMs * 0.2, this.sampleRate) : 0;
      const optoMemCoef = mode === "opto" ? smoothCoef(600, this.sampleRate) : 0;
      let detectCh = sidechainSource ? sidechainSource[0] ?? channels[0] : channels[0];
      if (!sidechainSource && detHpfActive) {
        const buf = this.detHpfBufs[bandIdx];
        buf.set(channels[0].subarray(0, frameCount));
        processBiquad(this.detHpf[bandIdx][0], [buf], frameCount);
        processBiquad(this.detHpf[bandIdx][1], [buf], frameCount);
        detectCh = buf;
      }
      for (let i = 0; i < frameCount; i++) {
        let detected;
        switch (detectionMode) {
          case "peak": {
            const sample = Math.abs(detectCh[i] ?? channels[0][i]);
            detected = band.peakEnv.process(sample);
            break;
          }
          case "rms": {
            const sample = Math.abs(detectCh[i] ?? channels[0][i]);
            detected = band.rmsDetector.process(sample);
            break;
          }
          case "trueEnvelope": {
            const s1 = Math.abs(detectCh[i] ?? channels[0][i]);
            if (i + 1 >= frameCount) {
              detected = band.peakEnv.process(s1);
              band.prevDetected = s1;
              break;
            }
            const s0 = Math.abs(band.prevDetected);
            const s2 = Math.abs(detectCh[i + 1] ?? channels[0][i + 1]);
            const denom = s0 - 2 * s1 + s2;
            let truePeak = s1;
            if (Math.abs(denom) > 1e-10) {
              const offset = 0.5 * (s0 - s2) / denom;
              if (Math.abs(offset) <= 0.5) {
                truePeak = s1 - 0.25 * (s0 - s2) * offset;
              }
            }
            band.prevDetected = s1;
            detected = band.peakEnv.process(truePeak);
            break;
          }
          default: {
            detected = band.peakEnv.process(Math.abs(detectCh[i] ?? channels[0][i]));
          }
        }
        const detectedDb = linearToDb(Math.max(1e-10, detected));
        let grDb = this.computeGainReduction(detectedDb, thresholdDb, ratio, kneeDb);
        let releaseCoef = band.releaseCoef;
        if (mode === "opto") {
          const target = clamp(band.gainReductionDb / 10, 0, 1);
          band.optoMemory += (target - band.optoMemory) * optoMemCoef;
          releaseCoef = band.releaseCoef / (1 + band.optoMemory * 3);
        } else if (autoRelease) {
          const grFraction = clamp(band.gainReductionDb / 12, 0, 1);
          releaseCoef = band.releaseCoef * (1 - grFraction) + autoReleaseFastCoef * grFraction;
        }
        if (grDb > 0.1) {
          band.holdCounter = Math.round(attackMs / 1e3 * this.sampleRate);
        } else if (band.holdCounter > 0) {
          band.holdCounter--;
          grDb = band.targetGrDb;
        }
        band.targetGrDb = grDb;
        const coef = grDb > band.gainReductionDb ? band.attackCoef : releaseCoef;
        band.gainReductionDb += coef * (grDb - band.gainReductionDb);
        const gainLinear = dbToLinear(-band.gainReductionDb);
        for (let ch = 0; ch < channels.length; ch++) {
          channels[ch][i] = sanitizeSample(channels[ch][i] * gainLinear);
        }
      }
      this.gainReduction[bandIdx] = this.gainReduction[bandIdx] * 0.7 + band.gainReductionDb * 0.3;
      let bandPeak = 0;
      for (let i = 0; i < frameCount; i++) {
        const a = Math.abs(channels[0][i]);
        if (a > bandPeak) bandPeak = a;
      }
      const bandPeakDb = linearToDb(Math.max(1e-10, bandPeak));
      this.outputLevels[bandIdx] = this.outputLevels[bandIdx] * 0.8 + bandPeakDb * 0.2;
    }
    /**
     * Compute gain reduction from detected level.
     * Implements soft knee compression curve.
     * Returns positive dB for reduction.
     */
    computeGainReduction(detectedDb, thresholdDb, ratio, kneeDb) {
      if (kneeDb <= 0) {
        if (detectedDb <= thresholdDb) return 0;
        return (detectedDb - thresholdDb) * (1 - 1 / ratio);
      }
      const kneeStart = thresholdDb - kneeDb / 2;
      const kneeEnd = thresholdDb + kneeDb / 2;
      if (detectedDb <= kneeStart) {
        return 0;
      }
      if (detectedDb >= kneeEnd) {
        return (detectedDb - thresholdDb) * (1 - 1 / ratio);
      }
      const x = detectedDb - kneeStart;
      const w = kneeDb;
      const fullGr = (detectedDb - thresholdDb) * (1 - 1 / ratio);
      const kneeFraction = x / w;
      return fullGr * kneeFraction * kneeFraction;
    }
  };

  // src/effects/ultina-core/dsp/modules/gateModule.ts
  var GATE_MAX_BANDS = 3;
  var GateModuleProcessor = class {
    sampleRate = 44100;
    maxBlockSize = 512;
    bands = [];
    multiband = new MultibandProcessor();
    // Sidechain HPF
    scHpf = createBiquad(2);
    // Dry buffer for delta
    dryL = new Float32Array(0);
    dryR = new Float32Array(0);
    // Sidechain HPF buffers
    scHpfBufferL = new Float32Array(0);
    scHpfBufferR = new Float32Array(0);
    // Meter state
    bandGain = new Array(GATE_MAX_BANDS).fill(0);
    bandState = new Array(GATE_MAX_BANDS).fill(0 /* Closed */);
    bandReductionDb = new Array(GATE_MAX_BANDS).fill(0);
    // Cached config
    cachedBandCount = -1;
    cachedXover1 = -1;
    cachedXover2 = -1;
    prepare(ctx) {
      this.sampleRate = ctx.sampleRate;
      this.maxBlockSize = ctx.maxBlockSize;
      this.ensureBuffers(this.maxBlockSize);
      this.scHpf = createBiquad(2);
      this.bands = [];
      for (let i = 0; i < GATE_MAX_BANDS; i++) {
        this.bands.push({
          state: 0 /* Closed */,
          currentGain: 0,
          holdCounter: 0,
          detector: new EnvelopeFollower(),
          attackCoef: 0,
          releaseCoef: 0
        });
        this.bands[i].detector.prepare(2, 20, this.sampleRate);
      }
      this.multiband.prepare(this.sampleRate, 2, this.maxBlockSize, 1);
    }
    process(args) {
      const { channels, frameCount, sidechain, params } = args;
      if (channels.length < 2) return;
      const enabled = (params["gate.enabled"] ?? 0) >= 0.5;
      if (!enabled) return;
      this.ensureBuffers(frameCount);
      const rangeDb = clamp(params["gate.rangeDb"] ?? -20, -80, 0);
      const attackMs = clamp(params["gate.attackMs"] ?? 1, 0.1, 100);
      const holdMs = clamp(params["gate.holdMs"] ?? 50, 0, 1e3);
      const releaseMs = clamp(params["gate.releaseMs"] ?? 100, 5, 5e3);
      const hysteresisDb = clamp(params["gate.hysteresisDb"] ?? 6, 0, 24);
      const scHpfHz = clamp(params["gate.sidechainHpfHz"] ?? 20, 20, 2e3);
      const bandCount = Math.round(clamp(params["gate.bandCount"] ?? 1, 1, 3));
      const xover1 = clamp(params["gate.crossoverHz1"] ?? 250, 20, 2e4);
      const xover2 = clamp(params["gate.crossoverHz2"] ?? 2500, 20, 2e4);
      const channelModeRaw = Math.round(clamp(params["gate.channelMode"] ?? 0, 0, 4));
      const channelMode = channelModeFromValue(channelModeRaw);
      const deltaListen = (params["gate.delta"] ?? 0) >= 0.5;
      const mixPercent = clamp(params["gate.mix"] ?? 100, 0, 100);
      const closedGainLinear = dbToLinear(rangeDb);
      const openThresholdDb = [
        params["gate.band0.openThresholdDb"] ?? -40,
        params["gate.band1.openThresholdDb"] ?? -40,
        params["gate.band2.openThresholdDb"] ?? -40
      ];
      const closeThresholdDb = [
        params["gate.band0.closeThresholdDb"] ?? openThresholdDb[0] - hysteresisDb,
        params["gate.band1.closeThresholdDb"] ?? openThresholdDb[1] - hysteresisDb,
        params["gate.band2.closeThresholdDb"] ?? openThresholdDb[2] - hysteresisDb
      ];
      for (let b = 0; b < closeThresholdDb.length; b++) {
        if (closeThresholdDb[b] > openThresholdDb[b]) {
          closeThresholdDb[b] = openThresholdDb[b];
        }
      }
      this.updateMultiband(bandCount, xover1, xover2);
      const xoverMode = (params["gate.crossoverMode"] ?? 0) >= 0.5 ? "hybrid" : "analog";
      this.multiband.setCrossoverMode(xoverMode);
      this.dryL.set(channels[0].subarray(0, frameCount));
      this.dryR.set(channels[1].subarray(0, frameCount));
      let detectSource = channels;
      const scEnabled = sidechain && sidechain.length >= 2;
      if (scEnabled) {
        setHighPass(this.scHpf.coeffs, scHpfHz, 0.707, this.sampleRate);
        this.scHpfBufferL.set(sidechain[0].subarray(0, frameCount));
        this.scHpfBufferR.set(sidechain[1].subarray(0, frameCount));
        const scChannels = [this.scHpfBufferL, this.scHpfBufferR];
        processBiquad(this.scHpf, scChannels, frameCount);
        detectSource = scChannels;
      }
      const scActive = scEnabled && sidechain && sidechain.length >= 2;
      this.multiband.process(
        channels,
        frameCount,
        (bandIdx, bandChannels, bandFrames) => {
          this.processBand(
            bandIdx,
            bandChannels,
            bandFrames,
            scActive ? detectSource : null,
            openThresholdDb[bandIdx],
            closeThresholdDb[bandIdx],
            attackMs,
            holdMs,
            releaseMs,
            closedGainLinear
          );
        },
        channelMode
      );
      const mix = mixPercent / 100;
      if (mix < 0.999) {
        for (let i = 0; i < frameCount; i++) {
          channels[0][i] = sanitizeSample(channels[0][i] * mix + this.dryL[i] * (1 - mix));
          channels[1][i] = sanitizeSample(channels[1][i] * mix + this.dryR[i] * (1 - mix));
        }
      }
      if (deltaListen) {
        for (let i = 0; i < frameCount; i++) {
          channels[0][i] = sanitizeSample(channels[0][i] - this.dryL[i]);
          channels[1][i] = sanitizeSample(channels[1][i] - this.dryR[i]);
        }
      }
    }
    reset() {
      for (const band of this.bands) {
        band.state = 0 /* Closed */;
        band.currentGain = 0;
        band.holdCounter = 0;
        band.detector.reset();
      }
      this.bandGain.fill(0);
      this.bandState.fill(0 /* Closed */);
      this.bandReductionDb.fill(0);
      this.multiband.reset();
      resetBiquad(this.scHpf);
    }
    getMeters() {
      return {
        bandGain: [...this.bandGain],
        bandState: [...this.bandState],
        bandReductionDb: [...this.bandReductionDb]
      };
    }
    /** Hybrid crossover group delay (samples). */
    getLatency() {
      return this.multiband.getCrossoverLatency();
    }
    // ── Internal ──────────────────────────────────────────────
    ensureBuffers(requiredSize) {
      if (this.dryL.length < requiredSize) {
        this.dryL = new Float32Array(requiredSize);
        this.dryR = new Float32Array(requiredSize);
        this.scHpfBufferL = new Float32Array(requiredSize);
        this.scHpfBufferR = new Float32Array(requiredSize);
      }
    }
    updateMultiband(bandCount, xover1, xover2) {
      if (this.cachedBandCount !== bandCount) {
        this.multiband.setBandCount(bandCount);
        this.cachedBandCount = bandCount;
        this.cachedXover1 = -1;
        this.cachedXover2 = -1;
      }
      if (this.cachedXover1 !== xover1) {
        this.multiband.setCrossover(0, xover1);
        this.cachedXover1 = xover1;
      }
      if (this.cachedXover2 !== xover2 && bandCount >= 3) {
        this.multiband.setCrossover(1, xover2);
        this.cachedXover2 = xover2;
      }
    }
    processBand(bandIdx, channels, frameCount, sidechainSource, openThresholdDb, closeThresholdDb, attackMs, holdMs, releaseMs, closedGainLinear) {
      const band = this.bands[bandIdx];
      band.attackCoef = smoothCoef(attackMs, this.sampleRate);
      band.releaseCoef = smoothCoef(releaseMs, this.sampleRate);
      const holdSamples = Math.round(holdMs / 1e3 * this.sampleRate);
      const detectCh = sidechainSource ? sidechainSource[0] ?? channels[0] : channels[0];
      const openThresholdLin = dbToLinear(openThresholdDb);
      const closeThresholdLin = dbToLinear(closeThresholdDb);
      for (let i = 0; i < frameCount; i++) {
        const sample = Math.abs(detectCh[i] ?? channels[0][i]);
        const detected = band.detector.process(sample);
        switch (band.state) {
          case 0 /* Closed */:
            if (detected >= openThresholdLin) {
              band.state = 1 /* Opening */;
            }
            break;
          case 1 /* Opening */:
            if (detected < closeThresholdLin) {
              band.state = 4 /* Closing */;
            } else if (band.currentGain >= 0.999) {
              band.state = 2 /* Open */;
            }
            break;
          case 2 /* Open */:
            if (detected < closeThresholdLin) {
              band.state = 3 /* Holding */;
              band.holdCounter = holdSamples;
            }
            break;
          case 3 /* Holding */:
            if (detected >= openThresholdLin) {
              band.state = 2 /* Open */;
            } else if (band.holdCounter <= 0) {
              band.state = 4 /* Closing */;
            } else {
              band.holdCounter--;
            }
            break;
          case 4 /* Closing */:
            if (detected >= openThresholdLin) {
              band.state = 1 /* Opening */;
            }
            break;
        }
        let targetGain;
        switch (band.state) {
          case 0 /* Closed */:
            targetGain = closedGainLinear;
            break;
          case 1 /* Opening */:
          case 2 /* Open */:
          case 3 /* Holding */:
            targetGain = 1;
            break;
          case 4 /* Closing */:
            targetGain = closedGainLinear;
            break;
          default:
            targetGain = 1;
        }
        const coef = targetGain > band.currentGain ? band.attackCoef : band.releaseCoef;
        band.currentGain += coef * (targetGain - band.currentGain);
        band.currentGain = clamp(band.currentGain, 0, 1);
        if (band.state === 4 /* Closing */ && band.currentGain <= closedGainLinear * 1.001) {
          band.state = 0 /* Closed */;
        }
        const g = band.currentGain;
        for (let ch = 0; ch < channels.length; ch++) {
          channels[ch][i] = sanitizeSample(channels[ch][i] * g);
        }
      }
      this.bandGain[bandIdx] = this.bandGain[bandIdx] * 0.7 + band.currentGain * 0.3;
      this.bandState[bandIdx] = band.state;
      const reductionDb = -linearToDb(Math.max(1e-10, band.currentGain));
      this.bandReductionDb[bandIdx] = this.bandReductionDb[bandIdx] * 0.7 + reductionDb * 0.3;
    }
  };

  // src/effects/ultina-core/dsp/oversampler.ts
  var OS_FACTOR = 4;
  var HB_TAPS = 15;
  var HB_COEFS_RAW = [
    -258e-5,
    -667e-5,
    -0.01139,
    0,
    0.04818,
    0.13163,
    0.21483,
    0.25,
    0.21483,
    0.13163,
    0.04818,
    0,
    -0.01139,
    -667e-5,
    -258e-5
  ];
  var HB_COEFS = (() => {
    const sum = HB_COEFS_RAW.reduce((a, b) => a + b, 0);
    return Float32Array.from(HB_COEFS_RAW, (v) => v / sum);
  })();
  var OS_LATENCY_SAMPLES = (HB_TAPS - 1 + 2) / OS_FACTOR;
  function createOsChannelState(maxOsFrames) {
    return {
      upHist: new Float32Array(HB_TAPS),
      upHistWrite: 0,
      downHist: new Float32Array(HB_TAPS),
      downHistWrite: 0,
      osBuffer: new Float32Array(maxOsFrames),
      dsD1: 0,
      dsD2: 0
    };
  }
  function resetOsChannelState(s) {
    s.upHist.fill(0);
    s.upHistWrite = 0;
    s.downHist.fill(0);
    s.downHistWrite = 0;
    s.osBuffer.fill(0);
    s.dsD1 = 0;
    s.dsD2 = 0;
  }
  function upsample(input, osState, frames) {
    const out = osState.osBuffer;
    const osFrames = frames * OS_FACTOR;
    const hist = osState.upHist;
    let hw = osState.upHistWrite;
    const tapLen = HB_TAPS;
    for (let i = 0; i < osFrames; i++) out[i] = 0;
    for (let i = 0; i < frames; i++) {
      out[i * OS_FACTOR] = input[i] * OS_FACTOR;
    }
    for (let i = 0; i < osFrames; i++) {
      hist[hw] = out[i];
      const nextHw = (hw + 1) % tapLen;
      let sum = 0;
      let histIdx = nextHw;
      for (let j = 0; j < tapLen; j++) {
        sum += hist[histIdx] * HB_COEFS[j];
        histIdx = (histIdx + 1) % tapLen;
      }
      out[i] = sum;
      hw = nextHw;
    }
    osState.upHistWrite = hw;
  }
  function downsample(osState, output, frames) {
    const input = osState.osBuffer;
    const osFrames = frames * OS_FACTOR;
    const hist = osState.downHist;
    let hw = osState.downHistWrite;
    const tapLen = HB_TAPS;
    for (let i = 0; i < osFrames; i++) {
      hist[hw] = input[i];
      const nextHw = (hw + 1) % tapLen;
      let sum = 0;
      let histIdx = (hw + 1) % tapLen;
      for (let j = 0; j < tapLen; j++) {
        sum += hist[histIdx] * HB_COEFS[j];
        histIdx = (histIdx + 1) % tapLen;
      }
      const delayed = osState.dsD2;
      osState.dsD2 = osState.dsD1;
      osState.dsD1 = sum;
      if (i % OS_FACTOR === 0) {
        output[i / OS_FACTOR] = delayed;
      }
      hw = nextHw;
    }
    osState.downHistWrite = hw;
  }

  // src/effects/ultina-core/dsp/modules/exciterModule.ts
  var EXCITER_MAX_BANDS = 3;
  var ExciterModuleProcessor = class {
    sampleRate = 44100;
    maxBlockSize = 512;
    // Multiband
    multiband = new MultibandProcessor();
    // Per-band oversampling state (2 channels × max bands)
    osStates = [];
    // Dry buffer for mix
    dryL = new Float32Array(0);
    dryR = new Float32Array(0);
    // Tone filter state (per channel, per band)
    toneLowState = [];
    toneHighState = [];
    // Meter state
    harmonicContent = new Array(EXCITER_MAX_BANDS).fill(0);
    outputPeaks = new Array(EXCITER_MAX_BANDS).fill(-100);
    osActive = false;
    // Cached config
    cachedBandCount = -1;
    cachedXover1 = -1;
    cachedXover2 = -1;
    prepare(ctx) {
      this.sampleRate = ctx.sampleRate;
      this.maxBlockSize = ctx.maxBlockSize;
      const maxOsFrames = this.maxBlockSize * OS_FACTOR;
      this.dryL = new Float32Array(this.maxBlockSize);
      this.dryR = new Float32Array(this.maxBlockSize);
      this.osStates = [];
      for (let b = 0; b < EXCITER_MAX_BANDS; b++) {
        const chStates = [];
        for (let ch = 0; ch < 2; ch++) {
          chStates.push(createOsChannelState(maxOsFrames));
        }
        this.osStates.push(chStates);
      }
      this.toneLowState = new Array(EXCITER_MAX_BANDS * 2).fill(0);
      this.toneHighState = new Array(EXCITER_MAX_BANDS * 2).fill(0);
      this.multiband.prepare(this.sampleRate, 2, this.maxBlockSize, 1);
    }
    process(args) {
      const { channels, frameCount, params } = args;
      if (channels.length < 2) return;
      const enabled = (params["exciter.enabled"] ?? 0) >= 0.5;
      if (!enabled) return;
      this.ensureBuffers(frameCount);
      const trashMode = (params["exciter.trashMode"] ?? 0) >= 0.5;
      const tubeAmt = clamp(params["exciter.tubeAmount"] ?? 0, 0, 100) / 100;
      const tubeAsymAmt = clamp(params["exciter.tubeAsymAmount"] ?? 0, 0, 100) / 100;
      const warmAmt = clamp(params["exciter.warmAmount"] ?? 30, 0, 100) / 100;
      const tapeAmt = clamp(params["exciter.tapeAmount"] ?? 0, 0, 100) / 100;
      const retroAmt = clamp(params["exciter.retroAmount"] ?? 0, 0, 100) / 100;
      const odAmt = clamp(params["exciter.overdriveAmount"] ?? 0, 0, 100) / 100;
      const screamAmt = clamp(params["exciter.screamAmount"] ?? 0, 0, 100) / 100;
      const clipAmt = clamp(params["exciter.clipperAmount"] ?? 0, 0, 100) / 100;
      const scratchAmt = clamp(params["exciter.scratchAmount"] ?? 0, 0, 100) / 100;
      const toneSlider = clamp(params["exciter.toneSlider"] ?? 0, -100, 100) / 100;
      const bandCount = Math.round(clamp(params["exciter.bandCount"] ?? 1, 1, 3));
      const xover1 = clamp(params["exciter.crossoverHz1"] ?? 2e3, 20, 2e4);
      const xover2 = clamp(params["exciter.crossoverHz2"] ?? 8e3, 20, 2e4);
      const channelModeRaw = Math.round(clamp(params["exciter.channelMode"] ?? 0, 0, 4));
      const channelMode = channelModeFromValue(channelModeRaw);
      const oversampling = (params["exciter.oversampling"] ?? 1) >= 0.5;
      const mixPercent = clamp(params["exciter.mix"] ?? 50, 0, 100);
      const deltaListen = (params["exciter.delta"] ?? 0) >= 0.5;
      this.osActive = oversampling;
      const amounts = {
        tubeAmt,
        tubeAsymAmt,
        warmAmt,
        tapeAmt,
        retroAmt,
        odAmt,
        screamAmt,
        clipAmt,
        scratchAmt
      };
      this.updateMultiband(bandCount, xover1, xover2);
      const xoverMode = (params["exciter.crossoverMode"] ?? 0) >= 0.5 ? "hybrid" : "analog";
      this.multiband.setCrossoverMode(xoverMode);
      this.dryL.set(channels[0].subarray(0, frameCount));
      this.dryR.set(channels[1].subarray(0, frameCount));
      this.multiband.process(
        channels,
        frameCount,
        (bandIdx, bandChannels, bandFrames) => {
          this.processBand(bandIdx, bandChannels, bandFrames, trashMode, amounts, toneSlider, oversampling);
        },
        channelMode
      );
      const mix = mixPercent / 100;
      if (mix < 0.999) {
        for (let i = 0; i < frameCount; i++) {
          channels[0][i] = sanitizeSample(channels[0][i] * mix + this.dryL[i] * (1 - mix));
          channels[1][i] = sanitizeSample(channels[1][i] * mix + this.dryR[i] * (1 - mix));
        }
      }
      if (deltaListen) {
        for (let i = 0; i < frameCount; i++) {
          channels[0][i] = sanitizeSample(channels[0][i] - this.dryL[i]);
          channels[1][i] = sanitizeSample(channels[1][i] - this.dryR[i]);
        }
      }
      for (let ch = 0; ch < channels.length; ch++) {
        const data = channels[ch];
        for (let i = 0; i < frameCount; i++) {
          data[i] = sanitizeSample(data[i]);
        }
      }
    }
    reset() {
      for (const bandStates of this.osStates) {
        for (const s of bandStates) {
          resetOsChannelState(s);
        }
      }
      this.toneLowState.fill(0);
      this.toneHighState.fill(0);
      this.multiband.reset();
      this.harmonicContent.fill(0);
      this.outputPeaks.fill(-100);
    }
    getMeters() {
      return {
        harmonicContentDb: [...this.harmonicContent],
        outputPeakDb: [...this.outputPeaks],
        oversampling: this.osActive
      };
    }
    /** Hybrid crossover group delay + oversampler latency (samples). */
    getLatency() {
      let lat = this.multiband.getCrossoverLatency();
      if (this.osActive) {
        lat += OS_LATENCY_SAMPLES;
      }
      return lat;
    }
    // ── Internal methods ──────────────────────────────────────
    ensureBuffers(size) {
      if (this.dryL.length < size) {
        this.dryL = new Float32Array(size);
        this.dryR = new Float32Array(size);
      }
    }
    updateMultiband(bandCount, xover1, xover2) {
      if (bandCount !== this.cachedBandCount) {
        this.multiband.setBandCount(bandCount);
        this.cachedBandCount = bandCount;
        this.cachedXover1 = -1;
        this.cachedXover2 = -1;
      }
      if (xover1 !== this.cachedXover1) {
        this.multiband.setCrossover(0, xover1);
        this.cachedXover1 = xover1;
      }
      if (xover2 !== this.cachedXover2 && bandCount >= 3) {
        this.multiband.setCrossover(1, xover2);
        this.cachedXover2 = xover2;
      }
    }
    processBand(bandIdx, bandChannels, bandFrames, trashMode, amounts, toneSlider, oversampling) {
      const chL = bandChannels[0];
      const chR = bandChannels.length >= 2 ? bandChannels[1] : bandChannels[0];
      let inEnergy = 0;
      for (let i = 0; i < bandFrames; i++) {
        inEnergy += chL[i] * chL[i];
      }
      if (oversampling && bandFrames > 0) {
        const osStateL = this.osStates[bandIdx][0];
        const osStateR = this.osStates[bandIdx][1];
        const osFrames = bandFrames * OS_FACTOR;
        this.upsample(chL, osStateL, bandFrames);
        this.upsample(chR, osStateR, bandFrames);
        this.applySaturation(osStateL.osBuffer, osFrames, trashMode, amounts);
        this.applySaturation(osStateR.osBuffer, osFrames, trashMode, amounts);
        this.applyTone(osStateL.osBuffer, osStateR.osBuffer, osFrames, toneSlider, bandIdx);
        this.downsample(osStateL, chL, bandFrames);
        this.downsample(osStateR, chR, bandFrames);
      } else {
        this.applySaturation(chL, bandFrames, trashMode, amounts);
        this.applySaturation(chR, bandFrames, trashMode, amounts);
        this.applyTone(chL, chR, bandFrames, toneSlider, bandIdx);
      }
      let outEnergy = 0;
      for (let i = 0; i < bandFrames; i++) {
        outEnergy += chL[i] * chL[i];
      }
      if (inEnergy > 1e-10 && bandFrames > 0) {
        const ratio = outEnergy / inEnergy;
        this.harmonicContent[bandIdx] = 10 * Math.log10(Math.max(1e-10, ratio));
      } else {
        this.harmonicContent[bandIdx] *= 0.8;
      }
      let peak = 0;
      for (let i = 0; i < bandFrames; i++) {
        const a = Math.abs(chL[i]);
        if (a > peak) peak = a;
      }
      this.outputPeaks[bandIdx] = peak > 1e-10 ? 20 * Math.log10(peak) : -100;
    }
    /**
     * Apply all active saturation/distortion types.
     * Saturation types are blended in parallel; distortion types
     * are applied in series.
     */
    applySaturation(buf, frames, trashMode, a) {
      const hasSat = a.tubeAmt > 0 || a.tubeAsymAmt > 0 || a.warmAmt > 0 || a.tapeAmt > 0 || a.retroAmt > 0;
      const hasDist = trashMode && (a.odAmt > 0 || a.screamAmt > 0 || a.clipAmt > 0 || a.scratchAmt > 0);
      if (!hasSat && !hasDist) return;
      for (let i = 0; i < frames; i++) {
        let x = buf[i];
        let wet = 0;
        let blendCount = 0;
        if (a.tubeAmt > 0) {
          wet += tubeSaturation(x, 1 + a.tubeAmt * 2);
          blendCount++;
        }
        if (a.tubeAsymAmt > 0) {
          wet += tubeAsymSaturation(x, 1 + a.tubeAsymAmt * 2);
          blendCount++;
        }
        if (a.warmAmt > 0) {
          wet += warmSaturation(x, 1 + a.warmAmt * 1.5, a.warmAmt * 0.7);
          blendCount++;
        }
        if (a.tapeAmt > 0) {
          wet += tapeSaturation(x, 1 + a.tapeAmt * 2);
          blendCount++;
        }
        if (a.retroAmt > 0) {
          const d = x * (1 + a.retroAmt * 2);
          const pos = d > 0 ? fastTanh(d * 1.3) * 0.8 : fastTanh(d * 0.7) * 0.9;
          wet += pos;
          blendCount++;
        }
        if (blendCount > 0) {
          wet /= blendCount;
          const satBlend = Math.max(a.tubeAmt, a.tubeAsymAmt, a.warmAmt, a.tapeAmt, a.retroAmt);
          x = x * (1 - satBlend * 0.5) + wet * (satBlend * 0.5);
        }
        if (trashMode) {
          if (a.odAmt > 0) {
            const drive = 1 + a.odAmt * 5;
            x = softClip(x, drive) / Math.sqrt(drive);
          }
          if (a.screamAmt > 0) {
            const drive = 1 + a.screamAmt * 8;
            const d = x * drive;
            x = fastTanh(Math.tan(d * 0.3) * 0.8) * 0.7 * a.screamAmt + x * (1 - a.screamAmt);
          }
          if (a.clipAmt > 0) {
            const threshold = 1 - a.clipAmt * 0.8;
            x = hardClip(x, threshold);
          }
          if (a.scratchAmt > 0) {
            const bits = Math.max(2, 12 - a.scratchAmt * 10);
            const levels = Math.pow(2, bits);
            const quantized = Math.round(x * levels) / levels;
            x = quantized * a.scratchAmt + x * (1 - a.scratchAmt);
          }
        }
        buf[i] = sanitizeSample(x);
      }
    }
    /**
     * Apply tone shelving via one-pole crossover.
     * toneSlider: -1 = boost lows, +1 = boost highs.
     */
    applyTone(chL, chR, frames, toneSlider, bandIdx) {
      if (Math.abs(toneSlider) < 1e-3) return;
      const lowGainDb = toneSlider < 0 ? -toneSlider * 6 : 0;
      const highGainDb = toneSlider > 0 ? toneSlider * 6 : 0;
      const lowGain = dbToLinear(lowGainDb);
      const highGain = dbToLinear(highGainDb);
      const alpha = 0.98;
      let lowL = this.toneLowState[bandIdx * 2];
      let lowR = this.toneLowState[bandIdx * 2 + 1];
      for (let i = 0; i < frames; i++) {
        const sL = chL[i];
        lowL = lowL + alpha * (sL - lowL);
        chL[i] = sanitizeSample(lowL * lowGain + (sL - lowL) * highGain);
        const sR = chR[i];
        lowR = lowR + alpha * (sR - lowR);
        chR[i] = sanitizeSample(lowR * lowGain + (sR - lowR) * highGain);
      }
      this.toneLowState[bandIdx * 2] = lowL;
      this.toneLowState[bandIdx * 2 + 1] = lowR;
    }
    // ── Oversampling (shared half-band FIR) ──────────────────
    upsample(input, osState, frames) {
      upsample(input, osState, frames);
    }
    downsample(osState, output, frames) {
      downsample(osState, output, frames);
    }
  };

  // src/effects/ultina-core/dsp/modules/transientModule.ts
  var TRANSIENT_MAX_BANDS = 3;
  var GLOBAL_MODES = {
    0: { attackMult: 0.5, releaseMult: 0.7, envSpeed: 1.5 },
    // Precise: fast
    1: { attackMult: 1, releaseMult: 1, envSpeed: 1 },
    // Balanced: medium
    2: { attackMult: 2, releaseMult: 1.5, envSpeed: 0.6 }
    // Loose: slow
  };
  var CONTOUR_SHAPES = {
    0: { sharpness: 0.9, thresholdDb: -40 },
    // Sharp
    1: { sharpness: 0.5, thresholdDb: -50 },
    // Medium
    2: { sharpness: 0.2, thresholdDb: -60 }
    // Smooth
  };
  var TransientModuleProcessor = class {
    sampleRate = 44100;
    maxBlockSize = 512;
    // Per-band state
    bands = [];
    // Multiband
    multiband = new MultibandProcessor();
    // Dry buffer
    dryL = new Float32Array(0);
    dryR = new Float32Array(0);
    // Meter state
    transientLevels = new Array(TRANSIENT_MAX_BANDS).fill(0);
    outputPeaks = new Array(TRANSIENT_MAX_BANDS).fill(-100);
    // Cached multiband config
    cachedBandCount = -1;
    cachedXover1 = -1;
    cachedXover2 = -1;
    prepare(ctx) {
      this.sampleRate = ctx.sampleRate;
      this.maxBlockSize = ctx.maxBlockSize;
      this.ensureBuffers(this.maxBlockSize);
      this.bands = [];
      for (let i = 0; i < TRANSIENT_MAX_BANDS; i++) {
        this.bands.push({
          fastEnv: 0,
          slowEnv: 0,
          transientGain: 1,
          sustainGain: 1,
          prevTransientGain: 1,
          prevSustainGain: 1
        });
      }
      this.multiband.prepare(this.sampleRate, 2, this.maxBlockSize, 1);
    }
    process(args) {
      const { channels, frameCount, params } = args;
      if (channels.length < 2) return;
      const enabled = (params["transient.enabled"] ?? 0) >= 0.5;
      if (!enabled) return;
      this.ensureBuffers(frameCount);
      const globalMode = Math.round(clamp(params["transient.globalMode"] ?? 1, 0, 2));
      const contourShape = Math.round(clamp(params["transient.contourShape"] ?? 1, 0, 2));
      const attackAmount = clamp(params["transient.attackAmount"] ?? 0, -100, 100) / 100;
      const sustainAmount = clamp(params["transient.sustainAmount"] ?? 0, -100, 100) / 100;
      const bandCount = Math.round(clamp(params["transient.bandCount"] ?? 1, 1, 3));
      const xover1 = clamp(params["transient.crossoverHz1"] ?? 250, 20, 2e4);
      const xover2 = clamp(params["transient.crossoverHz2"] ?? 2500, 20, 2e4);
      const channelModeRaw = Math.round(clamp(params["transient.channelMode"] ?? 0, 0, 4));
      const channelMode = channelModeFromValue(channelModeRaw);
      const deltaListen = (params["transient.delta"] ?? 0) >= 0.5;
      const mixPercent = clamp(params["transient.mix"] ?? 100, 0, 100);
      const modeCfg = GLOBAL_MODES[globalMode] ?? GLOBAL_MODES[1];
      const contourCfg = CONTOUR_SHAPES[contourShape] ?? CONTOUR_SHAPES[1];
      this.updateMultiband(bandCount, xover1, xover2);
      const xoverMode = (params["transient.crossoverMode"] ?? 0) >= 0.5 ? "hybrid" : "analog";
      this.multiband.setCrossoverMode(xoverMode);
      this.dryL.set(channels[0].subarray(0, frameCount));
      this.dryR.set(channels[1].subarray(0, frameCount));
      this.multiband.process(
        channels,
        frameCount,
        (bandIdx, bandChannels, bandFrames) => {
          this.processBand(
            bandIdx,
            bandChannels,
            bandFrames,
            modeCfg,
            contourCfg,
            attackAmount,
            sustainAmount
          );
        },
        channelMode
      );
      const mix = mixPercent / 100;
      if (mix < 0.999) {
        for (let i = 0; i < frameCount; i++) {
          channels[0][i] = sanitizeSample(channels[0][i] * mix + this.dryL[i] * (1 - mix));
          channels[1][i] = sanitizeSample(channels[1][i] * mix + this.dryR[i] * (1 - mix));
        }
      }
      if (deltaListen) {
        for (let i = 0; i < frameCount; i++) {
          channels[0][i] = sanitizeSample(channels[0][i] - this.dryL[i]);
          channels[1][i] = sanitizeSample(channels[1][i] - this.dryR[i]);
        }
      }
    }
    reset() {
      for (const band of this.bands) {
        band.fastEnv = 0;
        band.slowEnv = 0;
        band.transientGain = 1;
        band.sustainGain = 1;
        band.prevTransientGain = 1;
        band.prevSustainGain = 1;
      }
      this.multiband.reset();
      this.transientLevels.fill(0);
      this.outputPeaks.fill(-100);
    }
    getMeters() {
      return {
        transientLevel: [...this.transientLevels],
        outputPeakDb: [...this.outputPeaks]
      };
    }
    /** Hybrid crossover group delay (samples). */
    getLatency() {
      return this.multiband.getCrossoverLatency();
    }
    // ── Internal methods ──────────────────────────────────────
    ensureBuffers(size) {
      if (this.dryL.length < size) {
        this.dryL = new Float32Array(size);
        this.dryR = new Float32Array(size);
      }
    }
    updateMultiband(bandCount, xover1, xover2) {
      if (bandCount !== this.cachedBandCount) {
        this.multiband.setBandCount(bandCount);
        this.cachedBandCount = bandCount;
        this.cachedXover1 = -1;
        this.cachedXover2 = -1;
      }
      if (xover1 !== this.cachedXover1) {
        this.multiband.setCrossover(0, xover1);
        this.cachedXover1 = xover1;
      }
      if (xover2 !== this.cachedXover2 && bandCount >= 3) {
        this.multiband.setCrossover(1, xover2);
        this.cachedXover2 = xover2;
      }
    }
    processBand(bandIdx, bandChannels, bandFrames, modeCfg, contourCfg, attackAmount, sustainAmount) {
      const band = this.bands[bandIdx];
      const chL = bandChannels[0];
      const chR = bandChannels.length >= 2 ? bandChannels[1] : bandChannels[0];
      const fastAttackMs = 0.5 * modeCfg.attackMult;
      const fastReleaseMs = 20 * modeCfg.releaseMult;
      const slowAttackMs = 10 * modeCfg.attackMult;
      const slowReleaseMs = 150 * modeCfg.releaseMult;
      const fastAttackCoef = Math.exp(-1 / (fastAttackMs * 1e-3 * this.sampleRate));
      const fastReleaseCoef = Math.exp(-1 / (fastReleaseMs * 1e-3 * this.sampleRate));
      const slowAttackCoef = Math.exp(-1 / (slowAttackMs * 1e-3 * this.sampleRate));
      const slowReleaseCoef = Math.exp(-1 / (slowReleaseMs * 1e-3 * this.sampleRate));
      const gainSmoothCoef = Math.exp(-1 / (2 * 1e-3 * this.sampleRate));
      const transientGainBoost = attackAmount > 0 ? dbToLinear(attackAmount * 12) : dbToLinear(attackAmount * 6);
      const sustainGainBoost = sustainAmount > 0 ? dbToLinear(sustainAmount * 8) : dbToLinear(sustainAmount * 12);
      let maxTransientLevel = 0;
      for (let i = 0; i < bandFrames; i++) {
        const input = Math.abs(chL[i]);
        if (input > band.fastEnv) {
          band.fastEnv = fastAttackCoef * band.fastEnv + (1 - fastAttackCoef) * input;
        } else {
          band.fastEnv = fastReleaseCoef * band.fastEnv + (1 - fastReleaseCoef) * input;
        }
        if (input > band.slowEnv) {
          band.slowEnv = slowAttackCoef * band.slowEnv + (1 - slowAttackCoef) * input;
        } else {
          band.slowEnv = slowReleaseCoef * band.slowEnv + (1 - slowReleaseCoef) * input;
        }
        let transientAmount = 0;
        if (band.slowEnv > 1e-8) {
          transientAmount = band.fastEnv / band.slowEnv;
          transientAmount = Math.max(0, Math.min(1, (transientAmount - 1) * contourCfg.sharpness * 2));
        }
        maxTransientLevel = Math.max(maxTransientLevel, transientAmount);
        const targetTransientGain = 1 + (transientGainBoost - 1) * transientAmount;
        const sustainAmountNorm = 1 - transientAmount;
        const targetSustainGain = 1 + (sustainGainBoost - 1) * sustainAmountNorm;
        band.transientGain = gainSmoothCoef * band.transientGain + (1 - gainSmoothCoef) * targetTransientGain;
        band.sustainGain = gainSmoothCoef * band.sustainGain + (1 - gainSmoothCoef) * targetSustainGain;
        const combinedGain = band.transientGain * transientAmount + band.sustainGain * (1 - transientAmount);
        chL[i] = sanitizeSample(chL[i] * combinedGain);
        if (bandChannels.length >= 2) {
          chR[i] = sanitizeSample(chR[i] * combinedGain);
        }
      }
      this.transientLevels[bandIdx] = maxTransientLevel;
      let peak = 0;
      for (let i = 0; i < bandFrames; i++) {
        const a = Math.abs(chL[i]);
        if (a > peak) peak = a;
      }
      this.outputPeaks[bandIdx] = peak > 1e-10 ? 20 * Math.log10(peak) : -100;
    }
  };

  // src/effects/ultina-core/dsp/modules/clipperModule.ts
  var CLIPPER_MAX_BANDS = 3;
  function createBandMeterState() {
    return {
      inputPeak: 0,
      outputPeak: 0,
      outputRmsSum: 0,
      outputRmsCount: 0,
      clippingReduction: 0
    };
  }
  function resetBandMeterState(s) {
    s.inputPeak = 0;
    s.outputPeak = 0;
    s.outputRmsSum = 0;
    s.outputRmsCount = 0;
    s.clippingReduction = 0;
  }
  var ClipperModuleProcessor = class {
    sampleRate = 44100;
    maxBlockSize = 512;
    // Multiband
    multiband = new MultibandProcessor();
    // Per-band oversampling state (2 channels × max bands)
    osStates = [];
    /** Whether oversampling is currently active (for latency reporting). */
    osActive = false;
    // Dry buffer for delta listen
    dryL = new Float32Array(0);
    dryR = new Float32Array(0);
    // Per-band meter state
    bandMeters = [];
    // Cached config
    cachedBandCount = -1;
    cachedXover1 = -1;
    cachedXover2 = -1;
    prepare(ctx) {
      this.sampleRate = ctx.sampleRate;
      this.maxBlockSize = ctx.maxBlockSize;
      const maxOsFrames = this.maxBlockSize * OS_FACTOR;
      this.dryL = new Float32Array(this.maxBlockSize);
      this.dryR = new Float32Array(this.maxBlockSize);
      this.osStates = [];
      for (let b = 0; b < CLIPPER_MAX_BANDS; b++) {
        const chStates = [];
        for (let ch = 0; ch < 2; ch++) {
          chStates.push(createOsChannelState(maxOsFrames));
        }
        this.osStates.push(chStates);
      }
      this.bandMeters = [];
      for (let b = 0; b < CLIPPER_MAX_BANDS; b++) {
        this.bandMeters.push(createBandMeterState());
      }
      this.multiband.prepare(this.sampleRate, 2, this.maxBlockSize, 1);
    }
    process(args) {
      const { channels, frameCount, params } = args;
      if (channels.length < 2) return;
      const enabled = (params["clipper.enabled"] ?? 0) >= 0.5;
      if (!enabled) return;
      const ceilingDb = clamp(params["clipper.ceilingDb"] ?? -1, -24, 0);
      const kneeDb = clamp(params["clipper.kneeDb"] ?? 2, 0, 12);
      const driveDb = clamp(params["clipper.driveDb"] ?? 0, 0, 24);
      const bandCount = Math.round(clamp(params["clipper.bandCount"] ?? 1, 1, 3));
      const xover1 = clamp(params["clipper.crossoverHz1"] ?? 250, 20, 2e4);
      const xover2 = clamp(params["clipper.crossoverHz2"] ?? 2500, 20, 2e4);
      const channelModeRaw = Math.round(clamp(params["clipper.channelMode"] ?? 0, 0, 4));
      const channelMode = channelModeFromValue(channelModeRaw);
      const oversampling = (params["clipper.oversampling"] ?? 1) >= 0.5;
      this.osActive = oversampling;
      const deltaListen = (params["clipper.delta"] ?? 0) >= 0.5;
      const mixPercent = clamp(params["clipper.mix"] ?? 100, 0, 100);
      const driveLin = dbToLinear(driveDb);
      const ceilingLin = dbToLinear(ceilingDb);
      const kneeLin = Math.min(
        dbToLinear(ceilingDb + kneeDb) - ceilingLin,
        ceilingLin
      );
      this.updateMultiband(bandCount, xover1, xover2);
      const xoverMode = (params["clipper.crossoverMode"] ?? 0) >= 0.5 ? "hybrid" : "analog";
      this.multiband.setCrossoverMode(xoverMode);
      for (let b = 0; b < bandCount; b++) {
        this.bandMeters[b].clippingReduction = 0;
        this.bandMeters[b].outputRmsSum = 0;
        this.bandMeters[b].outputRmsCount = 0;
      }
      const chunkSize = this.maxBlockSize;
      for (let offset = 0; offset < frameCount; offset += chunkSize) {
        const remaining = Math.min(chunkSize, frameCount - offset);
        const chunkChannels = [
          channels[0].subarray(offset, offset + remaining),
          channels[1].subarray(offset, offset + remaining)
        ];
        this.ensureBuffers(remaining);
        this.dryL.set(chunkChannels[0]);
        this.dryR.set(chunkChannels[1]);
        this.multiband.process(
          chunkChannels,
          remaining,
          (bandIdx, bandChannels, bandFrames) => {
            this.processBand(
              bandIdx,
              bandChannels,
              bandFrames,
              driveLin,
              ceilingLin,
              kneeLin,
              kneeDb,
              oversampling
            );
          },
          channelMode
        );
        for (let i = 0; i < remaining; i++) {
          const s0 = chunkChannels[0][i];
          if (s0 > ceilingLin) chunkChannels[0][i] = ceilingLin;
          else if (s0 < -ceilingLin) chunkChannels[0][i] = -ceilingLin;
          const s1 = chunkChannels[1][i];
          if (s1 > ceilingLin) chunkChannels[1][i] = ceilingLin;
          else if (s1 < -ceilingLin) chunkChannels[1][i] = -ceilingLin;
        }
        const mix = mixPercent / 100;
        if (mix < 0.999) {
          for (let i = 0; i < remaining; i++) {
            chunkChannels[0][i] = sanitizeSample(chunkChannels[0][i] * mix + this.dryL[i] * (1 - mix));
            chunkChannels[1][i] = sanitizeSample(chunkChannels[1][i] * mix + this.dryR[i] * (1 - mix));
          }
        }
        if (deltaListen) {
          for (let i = 0; i < remaining; i++) {
            chunkChannels[0][i] = sanitizeSample(chunkChannels[0][i] - this.dryL[i]);
            chunkChannels[1][i] = sanitizeSample(chunkChannels[1][i] - this.dryR[i]);
          }
        }
      }
    }
    reset() {
      for (const bandStates of this.osStates) {
        for (const s of bandStates) {
          resetOsChannelState(s);
        }
      }
      for (const bm of this.bandMeters) {
        resetBandMeterState(bm);
      }
      this.multiband.reset();
    }
    getMeters() {
      const bands = this.bandMeters.map((bm) => ({
        inputPeakDb: ampToDb(bm.inputPeak),
        outputPeakDb: ampToDb(bm.outputPeak),
        gainReductionDb: bm.clippingReduction,
        outputRmsDb: bm.outputRmsCount > 0 ? ampToDb(Math.sqrt(bm.outputRmsSum / bm.outputRmsCount)) : -100
      }));
      const clippingReductionDb = this.bandMeters.map((bm) => bm.clippingReduction);
      return { bands, clippingReductionDb };
    }
    /** Hybrid crossover group delay + oversampler latency (samples). */
    getLatency() {
      let lat = this.multiband.getCrossoverLatency();
      if (this.osActive) {
        lat += OS_LATENCY_SAMPLES;
      }
      return lat;
    }
    // ── Internal methods ──────────────────────────────────────
    ensureBuffers(size) {
      if (this.dryL.length < size) {
        this.dryL = new Float32Array(size);
        this.dryR = new Float32Array(size);
      }
    }
    updateMultiband(bandCount, xover1, xover2) {
      if (bandCount !== this.cachedBandCount) {
        this.multiband.setBandCount(bandCount);
        this.cachedBandCount = bandCount;
        this.cachedXover1 = -1;
        this.cachedXover2 = -1;
      }
      if (xover1 !== this.cachedXover1) {
        this.multiband.setCrossover(0, xover1);
        this.cachedXover1 = xover1;
      }
      if (xover2 !== this.cachedXover2 && bandCount >= 3) {
        this.multiband.setCrossover(1, xover2);
        this.cachedXover2 = xover2;
      }
    }
    processBand(bandIdx, bandChannels, bandFrames, driveLin, ceilingLin, kneeLin, _kneeDb, oversampling) {
      const chL = bandChannels[0];
      const chR = bandChannels.length >= 2 ? bandChannels[1] : bandChannels[0];
      const bm = this.bandMeters[bandIdx];
      let inPeak = 0;
      for (let i = 0; i < bandFrames; i++) {
        const a = Math.abs(chL[i]);
        if (a > inPeak) inPeak = a;
      }
      if (inPeak > bm.inputPeak) bm.inputPeak = inPeak;
      bm.inputPeak = Math.max(inPeak, bm.inputPeak * 0.95);
      let maxReductionDb = 0;
      if (oversampling && bandFrames > 0) {
        const osStateL = this.osStates[bandIdx][0];
        const osStateR = this.osStates[bandIdx][1];
        this.upsample(chL, osStateL, bandFrames);
        this.upsample(chR, osStateR, bandFrames);
        const osFrames = bandFrames * OS_FACTOR;
        const redL = this.applyClipping(osStateL.osBuffer, osFrames, driveLin, ceilingLin, kneeLin);
        const redR = this.applyClipping(osStateR.osBuffer, osFrames, driveLin, ceilingLin, kneeLin);
        maxReductionDb = Math.max(redL, redR);
        this.downsample(osStateL, chL, bandFrames);
        this.downsample(osStateR, chR, bandFrames);
      } else {
        const redL = this.applyClipping(chL, bandFrames, driveLin, ceilingLin, kneeLin);
        const redR = this.applyClipping(chR, bandFrames, driveLin, ceilingLin, kneeLin);
        maxReductionDb = Math.max(redL, redR);
      }
      let outPeak = 0;
      let rmsSum = 0;
      for (let i = 0; i < bandFrames; i++) {
        const a = Math.abs(chL[i]);
        if (a > outPeak) outPeak = a;
        rmsSum += chL[i] * chL[i];
      }
      bm.outputPeak = Math.max(outPeak, bm.outputPeak * 0.95);
      bm.outputRmsSum += rmsSum;
      bm.outputRmsCount += bandFrames;
      bm.clippingReduction = Math.max(maxReductionDb, bm.clippingReduction);
    }
    /**
     * Apply soft-knee clipping curve to a buffer.
     * Returns the maximum gain reduction in dB observed.
     */
    applyClipping(buf, frames, driveLin, ceilingLin, kneeLin) {
      let maxReductionDb = 0;
      const kneeStart = ceilingLin - kneeLin;
      for (let i = 0; i < frames; i++) {
        const x = buf[i] * driveLin;
        const absX = Math.abs(x);
        const sign = x < 0 ? -1 : 1;
        let out;
        if (absX <= kneeStart) {
          out = x;
        } else if (absX >= ceilingLin) {
          out = sign * ceilingLin;
          if (absX > ceilingLin * 1.001) {
            const redDb = 20 * Math.log10(absX / ceilingLin);
            if (redDb > maxReductionDb) maxReductionDb = redDb;
          }
        } else {
          const t = (absX - kneeStart) / kneeLin;
          const smooth = 3 * t * t - 2 * t * t * t;
          out = sign * (kneeStart + kneeLin * smooth);
          if (absX > 1e-6) {
            const redDb = 20 * Math.log10(absX / Math.abs(out));
            if (redDb > maxReductionDb) maxReductionDb = redDb;
          }
        }
        buf[i] = sanitizeSample(out);
      }
      return maxReductionDb;
    }
    // ── Oversampling (shared half-band FIR) ──────────────────
    upsample(input, osState, frames) {
      upsample(input, osState, frames);
    }
    downsample(osState, output, frames) {
      downsample(osState, output, frames);
    }
  };

  // src/effects/ultina-core/dsp/modules/densityModule.ts
  var DENSITY_MAX_BANDS = 3;
  function createBandState() {
    return {
      peakEnv: { process: (t) => t },
      rmsDetector: { process: (i) => i },
      gainLinear: 1,
      targetGainDb: 0,
      attackCoef: 1e-3,
      releaseCoef: 1e-3
    };
  }
  function createBandMeterState2() {
    return {
      inputPeak: 0,
      outputPeak: 0,
      outputRmsSum: 0,
      outputRmsCount: 0,
      upwardGain: 0
    };
  }
  function resetBandMeterState2(s) {
    s.inputPeak = 0;
    s.outputPeak = 0;
    s.outputRmsSum = 0;
    s.outputRmsCount = 0;
    s.upwardGain = 0;
  }
  var DensityModuleProcessor = class {
    sampleRate = 44100;
    maxBlockSize = 512;
    // Multiband
    multiband = new MultibandProcessor();
    // Per-band envelope followers and state
    bands = [];
    // Per-band meter state
    bandMeters = [];
    // Dry buffer for delta listen
    dryL = new Float32Array(0);
    dryR = new Float32Array(0);
    // Cached config
    cachedBandCount = -1;
    cachedXover1 = -1;
    cachedXover2 = -1;
    prepare(ctx) {
      this.sampleRate = ctx.sampleRate;
      this.maxBlockSize = ctx.maxBlockSize;
      this.dryL = new Float32Array(this.maxBlockSize);
      this.dryR = new Float32Array(this.maxBlockSize);
      this.bands = [];
      for (let b = 0; b < DENSITY_MAX_BANDS; b++) {
        this.bands.push(createBandState());
      }
      this.bandMeters = [];
      for (let b = 0; b < DENSITY_MAX_BANDS; b++) {
        this.bandMeters.push(createBandMeterState2());
      }
      this.multiband.prepare(this.sampleRate, 2, this.maxBlockSize, 1);
      this.initEnvelopeFollowers();
    }
    initEnvelopeFollowers() {
      for (const band of this.bands) {
        band.peakEnv = this.createPeakEnv();
        band.rmsDetector = this.createRmsDetector();
      }
    }
    createPeakEnv() {
      let state = 0;
      let attackCoef = smoothCoef(10, this.sampleRate);
      let releaseCoef = smoothCoef(150, this.sampleRate);
      return {
        process(target) {
          const absTarget = Math.abs(target);
          const coef = absTarget > state ? attackCoef : releaseCoef;
          state += coef * (absTarget - state);
          return state;
        }
      };
    }
    createRmsDetector() {
      let sumSq = 0;
      const coef = smoothCoef(50, this.sampleRate);
      return {
        process(input) {
          const sq = input * input;
          sumSq += coef * (sq - sumSq);
          return Math.sqrt(Math.max(0, sumSq));
        }
      };
    }
    process(args) {
      const { channels, frameCount, params } = args;
      if (channels.length < 2) return;
      const enabled = (params["density.enabled"] ?? 0) >= 0.5;
      if (!enabled) return;
      const thresholdDb = clamp(params["density.thresholdDb"] ?? -30, -60, 0);
      const rangeDb = clamp(params["density.rangeDb"] ?? 6, 0, 24);
      const ratio = clamp(params["density.ratio"] ?? 2, 1, 10);
      const attackMs = clamp(params["density.attackMs"] ?? 10, 0.5, 200);
      const releaseMs = clamp(params["density.releaseMs"] ?? 150, 10, 2e3);
      const bandCount = Math.round(clamp(params["density.bandCount"] ?? 1, 1, 3));
      const xover1 = clamp(params["density.crossoverHz1"] ?? 250, 20, 2e4);
      const xover2 = clamp(params["density.crossoverHz2"] ?? 2500, 20, 2e4);
      const channelModeRaw = Math.round(clamp(params["density.channelMode"] ?? 0, 0, 2));
      const channelMode = channelModeFromValue(channelModeRaw);
      const deltaListen = (params["density.delta"] ?? 0) >= 0.5;
      const mixPercent = clamp(params["density.mix"] ?? 100, 0, 100);
      this.updateMultiband(bandCount, xover1, xover2);
      const xoverMode = (params["density.crossoverMode"] ?? 0) >= 0.5 ? "hybrid" : "analog";
      this.multiband.setCrossoverMode(xoverMode);
      for (let b = 0; b < bandCount; b++) {
        resetBandMeterState2(this.bandMeters[b]);
      }
      let offset = 0;
      while (offset < frameCount) {
        const remaining = frameCount - offset;
        const chunkSize = Math.min(remaining, this.maxBlockSize);
        const chunkChannels = [
          channels[0].subarray(offset, offset + chunkSize),
          channels[1].subarray(offset, offset + chunkSize)
        ];
        this.ensureBuffers(chunkSize);
        this.dryL.set(chunkChannels[0]);
        this.dryR.set(chunkChannels[1]);
        this.multiband.process(
          chunkChannels,
          chunkSize,
          (bandIdx, bandChannels, bandFrames) => {
            this.processBand(
              bandIdx,
              bandChannels,
              bandFrames,
              thresholdDb,
              rangeDb,
              ratio,
              attackMs,
              releaseMs
            );
          },
          channelMode
        );
        const mix = mixPercent / 100;
        if (mix < 0.999) {
          for (let i = 0; i < chunkSize; i++) {
            chunkChannels[0][i] = sanitizeSample(chunkChannels[0][i] * mix + this.dryL[i] * (1 - mix));
            chunkChannels[1][i] = sanitizeSample(chunkChannels[1][i] * mix + this.dryR[i] * (1 - mix));
          }
        }
        if (deltaListen) {
          for (let i = 0; i < chunkSize; i++) {
            chunkChannels[0][i] = sanitizeSample(chunkChannels[0][i] - this.dryL[i]);
            chunkChannels[1][i] = sanitizeSample(chunkChannels[1][i] - this.dryR[i]);
          }
        }
        offset += chunkSize;
      }
    }
    reset() {
      for (const band of this.bands) {
        band.gainLinear = 1;
        band.targetGainDb = 0;
      }
      this.initEnvelopeFollowers();
      this.multiband.reset();
      for (const bm of this.bandMeters) {
        resetBandMeterState2(bm);
      }
    }
    getMeters() {
      const bands = this.bandMeters.map((bm) => ({
        inputPeakDb: ampToDb(bm.inputPeak),
        outputPeakDb: ampToDb(bm.outputPeak),
        outputRmsDb: bm.outputRmsCount > 0 ? ampToDb(Math.sqrt(bm.outputRmsSum / bm.outputRmsCount)) : -100,
        gainReductionDb: -bm.upwardGain
        // negative of upward gain for consistent display
      }));
      return {
        bands,
        upwardGainDb: this.bandMeters.map((bm) => bm.upwardGain)
      };
    }
    /** Hybrid crossover group delay (samples). */
    getLatency() {
      return this.multiband.getCrossoverLatency();
    }
    // ── Internal methods ──────────────────────────────────────
    ensureBuffers(size) {
      if (this.dryL.length < size) {
        this.dryL = new Float32Array(size);
        this.dryR = new Float32Array(size);
      }
    }
    updateMultiband(bandCount, xover1, xover2) {
      if (bandCount !== this.cachedBandCount) {
        this.multiband.setBandCount(bandCount);
        this.cachedBandCount = bandCount;
        this.cachedXover1 = -1;
        this.cachedXover2 = -1;
      }
      if (xover1 !== this.cachedXover1) {
        this.multiband.setCrossover(0, xover1);
        this.cachedXover1 = xover1;
      }
      if (xover2 !== this.cachedXover2 && bandCount >= 3) {
        this.multiband.setCrossover(1, xover2);
        this.cachedXover2 = xover2;
      }
    }
    processBand(bandIdx, channels, frameCount, thresholdDb, rangeDb, ratio, attackMs, releaseMs) {
      const band = this.bands[bandIdx];
      const bm = this.bandMeters[bandIdx];
      band.attackCoef = smoothCoef(attackMs, this.sampleRate);
      band.releaseCoef = smoothCoef(releaseMs, this.sampleRate);
      const detectCh = channels[0] ?? channels[0];
      let maxUpwardGain = 0;
      for (let i = 0; i < frameCount; i++) {
        const detected = band.rmsDetector.process(detectCh[i]);
        const detectedDb = linearToDb(Math.max(1e-10, detected));
        let targetGainDb = 0;
        if (detectedDb < thresholdDb) {
          const distance = thresholdDb - detectedDb;
          targetGainDb = distance * (1 - 1 / ratio);
          targetGainDb = Math.min(rangeDb, targetGainDb);
        }
        const coef = targetGainDb > band.targetGainDb ? band.attackCoef : band.releaseCoef;
        band.targetGainDb += coef * (targetGainDb - band.targetGainDb);
        if (band.targetGainDb > maxUpwardGain) maxUpwardGain = band.targetGainDb;
        const gainLinear = dbToLinear(band.targetGainDb);
        for (let ch = 0; ch < channels.length; ch++) {
          channels[ch][i] = sanitizeSample(channels[ch][i] * gainLinear);
        }
        const inAbs = Math.abs(detectCh[i]);
        if (inAbs > bm.inputPeak) bm.inputPeak = inAbs;
        const outAbs = Math.abs(channels[0][i]);
        if (outAbs > bm.outputPeak) bm.outputPeak = outAbs;
        bm.outputRmsSum += channels[0][i] * channels[0][i];
        bm.outputRmsCount++;
      }
      bm.upwardGain = bm.upwardGain * 0.7 + maxUpwardGain * 0.3;
    }
  };

  // src/effects/ultina-core/dsp/modules/sculptorModule.ts
  var NUM_BANDS = 8;
  var MAX_CORRECTION_DB = 12;
  var BELL_Q = 1.5;
  var ANALYSIS_Q2 = 2;
  var BAND_FREQS = [80, 170, 350, 700, 1400, 2800, 5600, 11200];
  var TARGET_CURVES = [
    // Guitar: warm mids, controlled highs
    [-1, 2, 3, 2, 1, 0, -1, -2],
    // Bass: strong lows, rolled-off highs
    [4, 3, 2, 0, -2, -3, -4, -5],
    // Kick: deep low end, slight click
    [5, 2, -1, -2, 0, 1, 0, -1],
    // Piano: nearly flat, slight presence
    [0, 0, 1, 1, 1, 1, 0, -1],
    // Snare: punchy mids and crack
    [-2, 3, 2, 0, 1, 3, 4, 2],
    // Speech: presence boost, roll-off extremes
    [-4, -2, 0, 1, 2, 2, 0, -3]
  ];
  var SculptorModuleProcessor = class {
    sampleRate = 44100;
    maxBlockSize = 512;
    // Analysis: bandpass biquads (single channel — ch 0 only)
    analysisBiquads = [];
    // Processing: bell biquads (2 channels for stereo, ch 0 for M/S)
    bellBiquads = [];
    // Per-band envelope followers
    envFollowers = new Array(NUM_BANDS).fill(0);
    // Per-band smoothed gain (dB)
    smoothedGainDb = new Array(NUM_BANDS).fill(0);
    // Envelope follower coefficients
    envAtkCoef = 0;
    envRelCoef = 0;
    // Gain smoothing coefficients
    gainAtkCoef = 0;
    gainRelCoef = 0;
    // Mid/Side processor
    midSide = new MidSideProcessor();
    // Dry buffers
    dryL = new Float32Array(0);
    dryR = new Float32Array(0);
    // Meter data (current curves for display)
    currentTargetCurve = new Float32Array(NUM_BANDS);
    currentSpectralCurve = new Float32Array(NUM_BANDS);
    prepare(ctx) {
      this.sampleRate = ctx.sampleRate;
      this.maxBlockSize = ctx.maxBlockSize;
      this.dryL = new Float32Array(this.maxBlockSize);
      this.dryR = new Float32Array(this.maxBlockSize);
      this.analysisBiquads = [];
      for (let b = 0; b < NUM_BANDS; b++) {
        this.analysisBiquads.push(createBiquad(1));
      }
      this.bellBiquads = [];
      for (let b = 0; b < NUM_BANDS; b++) {
        this.bellBiquads.push(createBiquad(2));
      }
      for (let b = 0; b < NUM_BANDS; b++) {
        setBandPass(this.analysisBiquads[b].coeffs, BAND_FREQS[b], ANALYSIS_Q2, this.sampleRate);
      }
      for (let b = 0; b < NUM_BANDS; b++) {
        setBell(this.bellBiquads[b].coeffs, BAND_FREQS[b], 0, BELL_Q, this.sampleRate);
      }
      this.envAtkCoef = smoothCoef(5, this.sampleRate);
      this.envRelCoef = smoothCoef(50, this.sampleRate);
      this.gainAtkCoef = 1 - Math.exp(-this.maxBlockSize / (0.02 * this.sampleRate));
      this.gainRelCoef = 1 - Math.exp(-this.maxBlockSize / (0.1 * this.sampleRate));
      this.midSide.prepare(this.maxBlockSize, 2);
    }
    process(args) {
      const { channels, frameCount, params } = args;
      if (channels.length < 2) return;
      const enabled = (params["sculptor.enabled"] ?? 0) >= 0.5;
      if (!enabled) return;
      const targetProfile = Math.round(clamp(params["sculptor.targetProfile"] ?? 5, 0, 5));
      const amount = clamp(params["sculptor.amount"] ?? 50, 0, 100) / 100;
      const lowFreq = clamp(params["sculptor.lowFreqBoundaryHz"] ?? 20, 20, 1e3);
      const highFreq = clamp(params["sculptor.highFreqBoundaryHz"] ?? 16e3, 1e3, 2e4);
      const dryWet = clamp(params["sculptor.dryWet"] ?? 100, 0, 100) / 100;
      const channelModeRaw = Math.round(clamp(params["sculptor.channelMode"] ?? 0, 0, 2));
      const channelMode = channelModeFromValue(channelModeRaw);
      const deltaListen = (params["sculptor.delta"] ?? 0) >= 0.5;
      const targetCurve = TARGET_CURVES[targetProfile];
      for (let b = 0; b < NUM_BANDS; b++) {
        this.currentTargetCurve[b] = targetCurve[b];
      }
      let offset = 0;
      while (offset < frameCount) {
        const remaining = frameCount - offset;
        const chunkSize = Math.min(remaining, this.maxBlockSize);
        const chunkL = channels[0].subarray(offset, offset + chunkSize);
        const chunkR = channels[1].subarray(offset, offset + chunkSize);
        this.ensureBuffers(chunkSize);
        this.dryL.set(chunkL);
        this.dryR.set(chunkR);
        let analysisSignal;
        let processChannels;
        let msResult = null;
        if (channelMode === "mid" || channelMode === "side") {
          msResult = this.midSide.encode(chunkL, chunkR, chunkSize, channelMode);
          if (msResult) {
            const target = channelMode === "mid" ? msResult.mid : msResult.side;
            analysisSignal = target;
            processChannels = [target];
          } else {
            analysisSignal = chunkL;
            processChannels = [chunkL, chunkR];
          }
        } else {
          analysisSignal = chunkL;
          processChannels = [chunkL, chunkR];
        }
        for (let b = 0; b < NUM_BANDS; b++) {
          this.runBandpassAnalysis(b, analysisSignal, chunkSize);
        }
        let sumDb = 0;
        let validBands = 0;
        for (let b = 0; b < NUM_BANDS; b++) {
          if (BAND_FREQS[b] >= lowFreq && BAND_FREQS[b] <= highFreq) {
            sumDb += ampToDb(this.envFollowers[b]);
            validBands++;
          }
        }
        const avgDb = validBands > 0 ? sumDb / validBands : -60;
        for (let b = 0; b < NUM_BANDS; b++) {
          let targetGainDb;
          if (BAND_FREQS[b] < lowFreq || BAND_FREQS[b] > highFreq) {
            targetGainDb = 0;
          } else {
            if (this.envFollowers[b] < 1e-6) {
              targetGainDb = 0;
            } else {
              const measuredDb = ampToDb(this.envFollowers[b]);
              const relativeLevel = measuredDb - avgDb;
              targetGainDb = (targetCurve[b] - relativeLevel) * amount;
              targetGainDb = clamp(targetGainDb, -MAX_CORRECTION_DB, MAX_CORRECTION_DB);
            }
          }
          const coef = targetGainDb > this.smoothedGainDb[b] ? this.gainAtkCoef : this.gainRelCoef;
          this.smoothedGainDb[b] += coef * (targetGainDb - this.smoothedGainDb[b]);
        }
        for (let b = 0; b < NUM_BANDS; b++) {
          const g = this.smoothedGainDb[b];
          setBell(this.bellBiquads[b].coeffs, BAND_FREQS[b], g, BELL_Q, this.sampleRate);
          this.currentSpectralCurve[b] = g;
        }
        for (let b = 0; b < NUM_BANDS; b++) {
          if (Math.abs(this.smoothedGainDb[b]) < 1e-3) continue;
          const { b0, b1, b2, a1, a2 } = this.bellBiquads[b].coeffs;
          for (let ch = 0; ch < processChannels.length; ch++) {
            let z1 = this.bellBiquads[b].z1[ch];
            let z2 = this.bellBiquads[b].z2[ch];
            const data = processChannels[ch];
            for (let i = 0; i < chunkSize; i++) {
              const x = data[i];
              const y = b0 * x + z1;
              z1 = b1 * x - a1 * y + z2;
              z2 = b2 * x - a2 * y;
              data[i] = sanitizeSample(y);
            }
            this.bellBiquads[b].z1[ch] = z1;
            this.bellBiquads[b].z2[ch] = z2;
          }
        }
        if (msResult) {
          this.midSide.decode(msResult.mid, msResult.side, chunkSize, chunkL, chunkR);
        }
        if (deltaListen) {
          for (let i = 0; i < chunkSize; i++) {
            chunkL[i] = sanitizeSample(chunkL[i] - this.dryL[i]);
            chunkR[i] = sanitizeSample(chunkR[i] - this.dryR[i]);
          }
        } else {
          const wet = dryWet;
          const dry = 1 - dryWet;
          for (let i = 0; i < chunkSize; i++) {
            chunkL[i] = sanitizeSample(chunkL[i] * wet + this.dryL[i] * dry);
            chunkR[i] = sanitizeSample(chunkR[i] * wet + this.dryR[i] * dry);
          }
        }
        offset += chunkSize;
      }
    }
    reset() {
      for (const bq of this.analysisBiquads) resetBiquad(bq);
      for (const bq of this.bellBiquads) resetBiquad(bq);
      this.envFollowers.fill(0);
      this.smoothedGainDb.fill(0);
      this.currentTargetCurve.fill(0);
      this.currentSpectralCurve.fill(0);
      for (let b = 0; b < NUM_BANDS; b++) {
        setBell(this.bellBiquads[b].coeffs, BAND_FREQS[b], 0, BELL_Q, this.sampleRate);
      }
    }
    getMeters() {
      const bandLevelDb = new Float32Array(NUM_BANDS);
      let totalActiveGain = 0;
      for (let b = 0; b < NUM_BANDS; b++) {
        bandLevelDb[b] = linearToDb(Math.max(1e-10, this.envFollowers[b]));
        totalActiveGain += Math.abs(this.smoothedGainDb[b]);
      }
      const amountActive = clamp(
        totalActiveGain / (NUM_BANDS * MAX_CORRECTION_DB),
        0,
        1
      );
      return {
        spectralCurveDb: new Float32Array(this.currentSpectralCurve),
        targetCurveDb: new Float32Array(this.currentTargetCurve),
        bandLevelDb,
        amountActive
      };
    }
    // ── Internal methods ──────────────────────────────────────
    ensureBuffers(size) {
      if (this.dryL.length < size) {
        this.dryL = new Float32Array(size);
        this.dryR = new Float32Array(size);
      }
    }
    /**
     * Run bandpass analysis on a signal and update the per-band envelope follower.
     * Does NOT modify the input buffer.
     */
    runBandpassAnalysis(bandIdx, input, frameCount) {
      const bq = this.analysisBiquads[bandIdx];
      const { b0, b1, b2, a1, a2 } = bq.coeffs;
      let z1 = bq.z1[0];
      let z2 = bq.z2[0];
      let env = this.envFollowers[bandIdx];
      const atkCoef = this.envAtkCoef;
      const relCoef = this.envRelCoef;
      for (let i = 0; i < frameCount; i++) {
        const x = input[i];
        const y = b0 * x + z1;
        z1 = b1 * x - a1 * y + z2;
        z2 = b2 * x - a2 * y;
        const abs = Math.abs(y);
        const coef = abs > env ? atkCoef : relCoef;
        env += coef * (abs - env);
      }
      bq.z1[0] = z1;
      bq.z2[0] = z2;
      this.envFollowers[bandIdx] = env;
    }
  };

  // src/effects/ultina-core/dsp/modules/phaseModule.ts
  var PhaseModuleProcessor = class {
    sampleRate = 44100;
    maxBlockSize = 512;
    // DC blocker state (per channel)
    dcPrevX = [0, 0];
    dcPrevY = [0, 0];
    dcCoef = 0.995;
    // All-pass filter state (per channel) — single-state TDF-II
    allPassState = [0, 0];
    // Delay buffers (per channel) — sized in prepare() to cover the
    // requested shift at the active sample rate (50 ms max + 1 sample
    // headroom). The previous fixed-size buffer silently capped at ~93 ms
    // regardless of sampleRate, breaking >48 kHz operation.
    delayBufL = new Float32Array(0);
    delayBufR = new Float32Array(0);
    delayBufferSize = 0;
    delayWritePos = 0;
    // Dry buffers
    dryL = new Float32Array(0);
    dryR = new Float32Array(0);
    // ── Analysis state ──
    // Peak envelopes for asymmetry
    posPeakEnv = 0;
    negPeakEnv = 0;
    peakAtkCoef = 0;
    peakRelCoef = 0;
    // Running correlation accumulators
    corrSum = 0;
    // Σ(L*R)
    corrLsq = 0;
    // Σ(L²)
    corrRsq = 0;
    // Σ(R²)
    corrCount = 0;
    // Cross-correlation buffer for sidechain offset detection
    xcorrMaxOffsetSamples = 0;
    detectedOffsetMs = 0;
    // Smoothed meter values
    smoothAsymmetry = 0;
    smoothCorrelation = 1;
    // Learn result (suggested rotation)
    learnedRotation = 0;
    prepare(ctx) {
      this.sampleRate = ctx.sampleRate;
      this.maxBlockSize = ctx.maxBlockSize;
      this.dryL = new Float32Array(this.maxBlockSize);
      this.dryR = new Float32Array(this.maxBlockSize);
      this.delayBufferSize = Math.max(
        Math.ceil(50 * this.sampleRate / 1e3) + 1,
        this.maxBlockSize
      );
      this.delayBufL = new Float32Array(this.delayBufferSize);
      this.delayBufR = new Float32Array(this.delayBufferSize);
      this.delayWritePos = 0;
      this.dcCoef = 1 - 2 * Math.PI * 20 / this.sampleRate;
      if (this.dcCoef < 0.9) this.dcCoef = 0.9;
      this.peakAtkCoef = smoothCoef(5, this.sampleRate);
      this.peakRelCoef = smoothCoef(200, this.sampleRate);
      this.xcorrMaxOffsetSamples = Math.min(
        Math.floor(50 * this.sampleRate / 1e3),
        this.delayBufferSize - 1
      );
      this.reset();
    }
    process(args) {
      const { channels, frameCount, sidechain, params } = args;
      if (channels.length < 2) return;
      const enabled = (params["phase.enabled"] ?? 0) >= 0.5;
      if (!enabled) return;
      const learnActive = (params["phase.learnActive"] ?? 0) >= 0.5;
      const rotationDegrees = clamp(params["phase.rotationDegrees"] ?? 0, -180, 180);
      const timeShiftMs = clamp(params["phase.timeShiftMs"] ?? 0, -50, 50);
      const sidechainEnabled = (params["phase.sidechainEnabled"] ?? 0) >= 0.5;
      const autoAlignActive = (params["phase.autoAlignActive"] ?? 0) >= 0.5;
      const deltaListen = (params["phase.delta"] ?? 0) >= 0.5;
      const mixPercent = clamp(params["phase.mix"] ?? 100, 0, 100);
      const effectiveRotation = learnActive ? this.learnedRotation : rotationDegrees;
      let effectiveShiftMs = timeShiftMs;
      if (autoAlignActive && sidechainEnabled) {
        effectiveShiftMs = this.detectedOffsetMs;
      }
      const c = clamp(effectiveRotation / 180, -0.99, 0.99);
      const shiftSamples = Math.round(effectiveShiftMs * this.sampleRate / 1e3);
      const delayL = Math.min(Math.max(0, shiftSamples), this.delayBufferSize - 1);
      const delayR = Math.min(Math.max(0, -shiftSamples), this.delayBufferSize - 1);
      let offset = 0;
      while (offset < frameCount) {
        const remaining = frameCount - offset;
        const chunkSize = Math.min(remaining, this.maxBlockSize);
        const chunkL = channels[0].subarray(offset, offset + chunkSize);
        const chunkR = channels[1].subarray(offset, offset + chunkSize);
        this.ensureBuffers(chunkSize);
        this.dryL.set(chunkL);
        this.dryR.set(chunkR);
        for (let i = 0; i < chunkSize; i++) {
          let xL = chunkL[i];
          let xR = chunkR[i];
          const yL_dc = xL - this.dcPrevX[0] + this.dcCoef * this.dcPrevY[0];
          const yR_dc = xR - this.dcPrevX[1] + this.dcCoef * this.dcPrevY[1];
          this.dcPrevX[0] = xL;
          this.dcPrevX[1] = xR;
          this.dcPrevY[0] = yL_dc;
          this.dcPrevY[1] = yR_dc;
          xL = sanitizeSample(yL_dc);
          xR = sanitizeSample(yR_dc);
          const sL0 = this.allPassState[0];
          const sR0 = this.allPassState[1];
          const yL_ap = c * xL + sL0;
          const yR_ap = c * xR + sR0;
          this.allPassState[0] = xL - c * yL_ap;
          this.allPassState[1] = xR - c * yR_ap;
          xL = sanitizeSample(yL_ap);
          xR = sanitizeSample(yR_ap);
          this.delayBufL[this.delayWritePos] = xL;
          this.delayBufR[this.delayWritePos] = xR;
          const readPosL = (this.delayWritePos - delayL + this.delayBufferSize) % this.delayBufferSize;
          const readPosR = (this.delayWritePos - delayR + this.delayBufferSize) % this.delayBufferSize;
          const yL = this.delayBufL[readPosL];
          const yR = this.delayBufR[readPosR];
          this.delayWritePos = (this.delayWritePos + 1) % this.delayBufferSize;
          chunkL[i] = yL;
          chunkR[i] = yR;
          const mid = (this.dryL[i] + this.dryR[i]) * 0.5;
          const posPeak = mid > 0 ? mid : 0;
          const negPeak = mid < 0 ? -mid : 0;
          const posCoef = posPeak > this.posPeakEnv ? this.peakAtkCoef : this.peakRelCoef;
          const negCoef = negPeak > this.negPeakEnv ? this.peakAtkCoef : this.peakRelCoef;
          this.posPeakEnv += posCoef * (posPeak - this.posPeakEnv);
          this.negPeakEnv += negCoef * (negPeak - this.negPeakEnv);
          this.corrSum += this.dryL[i] * this.dryR[i];
          this.corrLsq += this.dryL[i] * this.dryL[i];
          this.corrRsq += this.dryR[i] * this.dryR[i];
          this.corrCount++;
        }
        const peakSum = this.posPeakEnv + this.negPeakEnv;
        const asymmetry = peakSum > 1e-8 ? Math.abs(this.posPeakEnv - this.negPeakEnv) / peakSum : 0;
        const asymCoef = smoothCoef(50, this.sampleRate) * chunkSize;
        this.smoothAsymmetry += Math.min(1, asymCoef) * (asymmetry - this.smoothAsymmetry);
        let corr = 1;
        if (this.corrLsq > 1e-8 && this.corrRsq > 1e-8 && this.corrCount > 0) {
          corr = this.corrSum / (Math.sqrt(this.corrLsq) * Math.sqrt(this.corrRsq));
          corr = clamp(corr, -1, 1);
        }
        const corrCoef = Math.min(1, smoothCoef(100, this.sampleRate) * chunkSize);
        this.smoothCorrelation += corrCoef * (corr - this.smoothCorrelation);
        const decay = 0.99;
        this.corrSum *= decay;
        this.corrLsq *= decay;
        this.corrRsq *= decay;
        if (learnActive) {
          const asym = this.posPeakEnv > this.negPeakEnv ? this.smoothAsymmetry : -this.smoothAsymmetry;
          const targetRot = asym * 90;
          const learnCoef = Math.min(1, smoothCoef(500, this.sampleRate) * chunkSize);
          this.learnedRotation += learnCoef * (targetRot - this.learnedRotation);
        }
        if (sidechainEnabled && sidechain && sidechain.length >= 1) {
          const sc = sidechain[0];
          let bestLag = 0;
          let bestCorr = -Infinity;
          const maxLag = Math.min(this.xcorrMaxOffsetSamples, chunkSize - 1);
          for (let lag = -maxLag; lag <= maxLag; lag++) {
            let sum = 0;
            let count = 0;
            for (let i = Math.max(0, lag); i < Math.min(chunkSize, chunkSize + lag); i++) {
              const scIdx = i - lag;
              if (scIdx >= 0 && scIdx < sc.length) {
                sum += this.dryL[i] * sc[scIdx];
                count++;
              }
            }
            if (count > 0) {
              const avg = sum / count;
              if (avg > bestCorr) {
                bestCorr = avg;
                bestLag = lag;
              }
            }
          }
          const detectedMs = bestLag / this.sampleRate * 1e3;
          const detectCoef = Math.min(1, smoothCoef(200, this.sampleRate) * chunkSize);
          this.detectedOffsetMs += detectCoef * (detectedMs - this.detectedOffsetMs);
        }
        const mix = mixPercent / 100;
        if (mix < 0.999) {
          for (let i = 0; i < chunkSize; i++) {
            chunkL[i] = sanitizeSample(chunkL[i] * mix + this.dryL[i] * (1 - mix));
            chunkR[i] = sanitizeSample(chunkR[i] * mix + this.dryR[i] * (1 - mix));
          }
        }
        if (deltaListen) {
          for (let i = 0; i < chunkSize; i++) {
            chunkL[i] = sanitizeSample(chunkL[i] - this.dryL[i]);
            chunkR[i] = sanitizeSample(chunkR[i] - this.dryR[i]);
          }
        }
        offset += chunkSize;
      }
    }
    reset() {
      this.dcPrevX = [0, 0];
      this.dcPrevY = [0, 0];
      this.allPassState = [0, 0];
      this.delayBufL.fill(0);
      this.delayBufR.fill(0);
      this.delayWritePos = 0;
      this.posPeakEnv = 0;
      this.negPeakEnv = 0;
      this.corrSum = 0;
      this.corrLsq = 0;
      this.corrRsq = 0;
      this.corrCount = 0;
      this.detectedOffsetMs = 0;
      this.smoothAsymmetry = 0;
      this.smoothCorrelation = 1;
      this.learnedRotation = 0;
    }
    getMeters() {
      return {
        asymmetry: clamp(this.smoothAsymmetry, 0, 1),
        correlation: clamp(this.smoothCorrelation, -1, 1),
        detectedOffsetMs: this.detectedOffsetMs
      };
    }
    // ── Internal ──
    ensureBuffers(size) {
      if (this.dryL.length < size) {
        this.dryL = new Float32Array(size);
        this.dryR = new Float32Array(size);
      }
    }
  };

  // src/effects/ultina-core/dsp/modules/unmaskModule.ts
  var NUM_BANDS2 = 32;
  var MAX_REDUCTION_DB = 12;
  var BELL_Q2 = 2.5;
  var ANALYSIS_Q3 = 3;
  var LOG_MIN = Math.log(40);
  var LOG_MAX = Math.log(16e3);
  var BAND_FREQS2 = [];
  for (let i = 0; i < NUM_BANDS2; i++) {
    const t = i / (NUM_BANDS2 - 1);
    BAND_FREQS2.push(Math.round(Math.exp(LOG_MIN + t * (LOG_MAX - LOG_MIN))));
  }
  var UnmaskModuleProcessor = class _UnmaskModuleProcessor {
    sampleRate = 44100;
    maxBlockSize = 256;
    channelCount = 2;
    // Analysis filter banks (bandpass)
    mainAnalysisFilters = [];
    scAnalysisFilters = [];
    // Processing filter chain (bell EQs applied to main signal)
    processFilters = [];
    // Envelope followers
    mainEnv = new Float32Array(NUM_BANDS2);
    scEnv = new Float32Array(NUM_BANDS2);
    // Smoothed gain values
    gainDb = new Float32Array(NUM_BANDS2);
    gainSmoothed = new Float32Array(NUM_BANDS2);
    // Smoothing coefficient
    gainSmoothCoef = 1e-3;
    // Metering state
    maskingPerBandDb = new Float32Array(NUM_BANDS2);
    gainReductionPerBandDb = new Float32Array(NUM_BANDS2);
    maskingScore = 0;
    /** Learn mode: auto-calibrated masking threshold (persists after learn). */
    learnedThresholdDb = -15;
    learnSmoothCoef = 0.02;
    // Cross-instance ecosystem: aggregate masker from all other Ultina instances
    ecosystemMaskerDb = new Float32Array(SPECTRAL_BANDS);
    instanceId = "";
    // M/S processor
    midSide = new MidSideProcessor();
    // Buffers
    dryBuf = [];
    analysisBuf = new Float32Array(0);
    scAnalysisBuf = new Float32Array(0);
    // Cached meter arrays (to avoid reallocation)
    meterMaskingDb = new Float32Array(NUM_BANDS2);
    meterGainRedDb = new Float32Array(NUM_BANDS2);
    /** Set the instance ID for cross-instance spectral communication. */
    setInstanceId(id) {
      this.instanceId = id;
    }
    prepare(ctx) {
      this.sampleRate = ctx.sampleRate;
      this.maxBlockSize = ctx.maxBlockSize;
      this.channelCount = ctx.channelCount;
      this.gainSmoothCoef = 1 - Math.exp(-this.maxBlockSize / (0.05 * this.sampleRate));
      this.mainAnalysisFilters = [];
      this.scAnalysisFilters = [];
      for (let i = 0; i < NUM_BANDS2; i++) {
        const freq = BAND_FREQS2[i];
        const clampedFreq = Math.min(freq, this.sampleRate / 2 - 1);
        const mainBq = createBiquad(1);
        setBandPass(mainBq.coeffs, clampedFreq, ANALYSIS_Q3, this.sampleRate);
        this.mainAnalysisFilters.push(mainBq);
        const scBq = createBiquad(1);
        setBandPass(scBq.coeffs, clampedFreq, ANALYSIS_Q3, this.sampleRate);
        this.scAnalysisFilters.push(scBq);
      }
      this.processFilters = [];
      for (let i = 0; i < NUM_BANDS2; i++) {
        const freq = BAND_FREQS2[i];
        const clampedFreq = Math.min(freq, this.sampleRate / 2 - 1);
        const bq = createBiquad(this.channelCount);
        setBell(bq.coeffs, clampedFreq, 0, BELL_Q2, this.sampleRate);
        this.processFilters.push(bq);
      }
      this.midSide.prepare(this.maxBlockSize, this.channelCount);
      this.ensureBuffers(this.maxBlockSize);
    }
    process(args) {
      const { channels, frameCount, sidechain, params } = args;
      const enabled = params["unmask.enabled"] ?? 0;
      const amount = params["unmask.amount"] ?? 50;
      const scEnabled = params["unmask.sidechainEnabled"] ?? 0;
      const thresholdDb = params["unmask.maskingThresholdDb"] ?? -15;
      const responseHz = params["unmask.responseSpeedHz"] ?? 5;
      const channelModeRaw = params["unmask.channelMode"] ?? 0;
      const delta = params["unmask.delta"] ?? 0;
      const mixPercent = clamp(params["unmask.mix"] ?? 100, 0, 100);
      const ecosystemEnabled = (params["unmask.ecosystemEnabled"] ?? 0) >= 0.5;
      if (enabled < 0.5) return;
      if (channels.length < 2) return;
      this.ensureBuffers(frameCount);
      const n = Math.min(channels[0].length, frameCount);
      for (let ch = 0; ch < channels.length; ch++) {
        if (!this.dryBuf[ch] || this.dryBuf[ch].length < n) {
          this.dryBuf[ch] = new Float32Array(n);
        }
        this.dryBuf[ch].set(channels[ch].subarray(0, n));
      }
      const envCoef = smoothCoef(1e3 / Math.max(0.5, responseHz), this.sampleRate);
      this.runAnalysis(channels, sidechain, scEnabled, frameCount, envCoef);
      if (ecosystemEnabled && this.instanceId) {
        const hasData = SpectralRegistry.getInstance().getAggregateMasker(
          this.instanceId,
          this.ecosystemMaskerDb
        );
        if (hasData) {
          for (let b = 0; b < NUM_BANDS2; b++) {
            const ecoAmp = Math.pow(10, this.ecosystemMaskerDb[b] / 20);
            if (ecoAmp > this.scEnv[b]) {
              this.scEnv[b] = ecoAmp;
            }
          }
        }
      }
      const learnActive = (params["unmask.learnActive"] ?? 0) >= 0.5;
      this.computeMaskingAndGain(amount, thresholdDb, envCoef, learnActive);
      this.applyGainChain(channels, frameCount, channelModeRaw);
      const mix = mixPercent / 100;
      if (mix < 0.999) {
        for (let ch = 0; ch < channels.length; ch++) {
          for (let i = 0; i < n; i++) {
            channels[ch][i] = sanitizeSample(channels[ch][i] * mix + this.dryBuf[ch][i] * (1 - mix));
          }
        }
      }
      if (delta >= 0.5) {
        for (let ch = 0; ch < channels.length; ch++) {
          for (let i = 0; i < n; i++) {
            channels[ch][i] = sanitizeSample(channels[ch][i] - this.dryBuf[ch][i]);
          }
        }
      } else {
        for (let ch = 0; ch < channels.length; ch++) {
          for (let i = 0; i < n; i++) {
            channels[ch][i] = sanitizeSample(channels[ch][i]);
          }
        }
      }
    }
    /**
     * Analysis cadence: the 32-band analysis filters run at the FULL
     * sample rate (a stride-N feed rescales each filter's response by N
     * and aliases — the old stride-4 version read frequency-warped
     * levels). To bound CPU, the analysis runs every 4th block; the
     * smoothed envelopes make the ~10–40 ms update granularity
     * inaudible at the module's response speeds.
     */
    static ANALYSIS_INTERVAL = 4;
    /** Starts at INTERVAL so the very first block is analyzed immediately. */
    analysisBlockCounter = _UnmaskModuleProcessor.ANALYSIS_INTERVAL;
    runAnalysis(channels, sidechain, scEnabled, frameCount, envCoef) {
      const mainSrc = channels[0];
      const scSrc = scEnabled >= 0.5 && sidechain && sidechain.length > 0 ? sidechain[0] : null;
      if (scSrc) {
        const n = Math.min(frameCount, this.scAnalysisBuf.length, scSrc.length);
        for (let i = 0; i < n; i++) {
          this.scAnalysisBuf[i] = scSrc[i];
        }
      } else {
        const decayFactor = 1 - envCoef;
        for (let b = 0; b < NUM_BANDS2; b++) {
          this.scEnv[b] *= decayFactor;
        }
      }
      this.analysisBlockCounter++;
      if (this.analysisBlockCounter < _UnmaskModuleProcessor.ANALYSIS_INTERVAL) {
        return;
      }
      this.analysisBlockCounter = 0;
      const nMain = Math.min(frameCount, this.analysisBuf.length);
      for (let i = 0; i < nMain; i++) {
        this.analysisBuf[i] = mainSrc[i];
      }
      for (let b = 0; b < NUM_BANDS2; b++) {
        const bq = this.mainAnalysisFilters[b];
        const c = bq.coeffs;
        let z1 = bq.z1[0];
        let z2 = bq.z2[0];
        let env = this.mainEnv[b];
        for (let i = 0; i < nMain; i++) {
          const x = this.analysisBuf[i];
          const y = c.b0 * x + z1;
          z1 = c.b1 * x - c.a1 * y + z2;
          z2 = c.b2 * x - c.a2 * y;
          const abs = y < 0 ? -y : y;
          env += envCoef * (abs - env);
        }
        bq.z1[0] = z1;
        bq.z2[0] = z2;
        this.mainEnv[b] = env;
      }
      if (scSrc) {
        for (let b = 0; b < NUM_BANDS2; b++) {
          const bq = this.scAnalysisFilters[b];
          const c = bq.coeffs;
          let z1 = bq.z1[0];
          let z2 = bq.z2[0];
          let env = this.scEnv[b];
          for (let i = 0; i < nMain; i++) {
            const x = this.scAnalysisBuf[i];
            const y = c.b0 * x + z1;
            z1 = c.b1 * x - c.a1 * y + z2;
            z2 = c.b2 * x - c.a2 * y;
            const abs = y < 0 ? -y : y;
            env += envCoef * (abs - env);
          }
          bq.z1[0] = z1;
          bq.z2[0] = z2;
          this.scEnv[b] = env;
        }
      }
    }
    computeMaskingAndGain(amount, thresholdDb, _envCoef, learnActive) {
      const amountFactor = clamp(amount / 100, 0, 1);
      if (learnActive) {
        let sum = 0;
        let count = 0;
        for (let b = 0; b < NUM_BANDS2; b++) {
          if (this.maskingPerBandDb[b] > -6) {
            sum += this.maskingPerBandDb[b];
            count++;
          }
        }
        if (count > 0) {
          const target = clamp(sum / count - 6, -40, 0);
          this.learnedThresholdDb += this.learnSmoothCoef * (target - this.learnedThresholdDb);
        }
      }
      const effectiveThreshold = learnActive ? this.learnedThresholdDb : thresholdDb;
      let totalMasking = 0;
      let activeBands = 0;
      for (let b = 0; b < NUM_BANDS2; b++) {
        const mainDb = ampToDb(this.mainEnv[b]);
        const scDb = ampToDb(this.scEnv[b]);
        const maskingDb = this.mainEnv[b] < 1e-9 && this.scEnv[b] < 1e-9 ? -200 : scDb - mainDb;
        this.maskingPerBandDb[b] = maskingDb;
        let targetGain = 0;
        if (maskingDb > effectiveThreshold) {
          const excess = maskingDb - effectiveThreshold;
          targetGain = -clamp(excess * amountFactor, 0, MAX_REDUCTION_DB);
        }
        this.gainDb[b] = targetGain;
        const gs = this.gainSmoothed[b];
        this.gainSmoothed[b] = gs + this.gainSmoothCoef * (targetGain - gs);
        this.gainReductionPerBandDb[b] = this.gainSmoothed[b];
        if (maskingDb > effectiveThreshold) {
          totalMasking += clamp(maskingDb - effectiveThreshold, 0, MAX_REDUCTION_DB);
          activeBands++;
        }
      }
      const maxPossibleMasking = NUM_BANDS2 * MAX_REDUCTION_DB;
      this.maskingScore = totalMasking / maxPossibleMasking;
    }
    applyGainChain(channels, frameCount, channelModeRaw) {
      const channelMode = channelModeFromValue(channelModeRaw);
      const activeBands = [];
      for (let b = 0; b < NUM_BANDS2; b++) {
        const gain = this.gainSmoothed[b];
        if (gain < -0.01 || gain > 0.01) {
          const freq = Math.min(BAND_FREQS2[b], this.sampleRate / 2 - 1);
          setBell(this.processFilters[b].coeffs, freq, gain, BELL_Q2, this.sampleRate);
          activeBands.push(b);
        }
      }
      if (activeBands.length === 0) return;
      if (channelMode === "stereo") {
        this.processChannelsDirect(channels, frameCount, activeBands);
      } else {
        const msResult = this.midSide.encode(channels[0], channels[1], frameCount, channelMode);
        if (msResult) {
          const target = [
            channelMode === "mid" ? msResult.mid : msResult.side
          ];
          this.processChannelsDirect(target, frameCount, activeBands);
          const chunkL = channels[0];
          const chunkR = channels[1] ?? channels[0];
          this.midSide.decode(msResult.mid, msResult.side, frameCount, chunkL, chunkR);
        } else {
          this.processChannelsDirect(channels, frameCount, activeBands);
        }
      }
    }
    /**
     * Apply active bell filters in series to channels.
     * Optimized: no per-sample sanitizeSample calls (NaN is handled
     * at the final output stage), coefficient references cached.
     */
    processChannelsDirect(channels, frameCount, activeBands) {
      const n = Math.min(channels[0].length, frameCount);
      const numCh = channels.length;
      for (const b of activeBands) {
        const bq = this.processFilters[b];
        const c = bq.coeffs;
        for (let ch = 0; ch < numCh; ch++) {
          const data = channels[ch];
          let z1 = bq.z1[ch];
          let z2 = bq.z2[ch];
          for (let i = 0; i < n; i++) {
            const x = data[i];
            const y = c.b0 * x + z1;
            z1 = c.b1 * x - c.a1 * y + z2;
            z2 = c.b2 * x - c.a2 * y;
            data[i] = y;
          }
          bq.z1[ch] = z1;
          bq.z2[ch] = z2;
        }
      }
    }
    reset() {
      for (let b = 0; b < NUM_BANDS2; b++) {
        resetBiquad(this.mainAnalysisFilters[b]);
        resetBiquad(this.scAnalysisFilters[b]);
        resetBiquad(this.processFilters[b]);
        this.mainEnv[b] = 0;
        this.scEnv[b] = 0;
        this.gainDb[b] = 0;
        this.gainSmoothed[b] = 0;
        this.maskingPerBandDb[b] = 0;
        this.gainReductionPerBandDb[b] = 0;
      }
      this.maskingScore = 0;
      this.learnedThresholdDb = -15;
    }
    getMeters() {
      this.meterMaskingDb.set(this.maskingPerBandDb);
      this.meterGainRedDb.set(this.gainReductionPerBandDb);
      return {
        maskingPerBandDb: this.meterMaskingDb,
        gainReductionPerBandDb: this.meterGainRedDb,
        maskingScore: this.maskingScore,
        learnedThresholdDb: this.learnedThresholdDb
      };
    }
    ensureBuffers(size) {
      if (this.analysisBuf.length < size) {
        this.analysisBuf = new Float32Array(size);
        this.scAnalysisBuf = new Float32Array(size);
      }
      while (this.dryBuf.length < this.channelCount) {
        this.dryBuf.push(new Float32Array(size));
      }
      for (let ch = 0; ch < this.channelCount; ch++) {
        if (!this.dryBuf[ch] || this.dryBuf[ch].length < size) {
          this.dryBuf[ch] = new Float32Array(size);
        }
      }
    }
  };

  // src/effects/ultina-core/dsp/moduleFactories.ts
  function registerCoreModules(processor) {
    processor.registerModuleFactory("eq", () => new EqModuleProcessor());
    processor.registerModuleFactory("comp", () => new CompModuleProcessor());
    processor.registerModuleFactory("gate", () => new GateModuleProcessor());
    processor.registerModuleFactory("exciter", () => new ExciterModuleProcessor());
    processor.registerModuleFactory("transient", () => new TransientModuleProcessor());
    processor.registerModuleFactory("clipper", () => new ClipperModuleProcessor());
    processor.registerModuleFactory("density", () => new DensityModuleProcessor());
    processor.registerModuleFactory("sculptor", () => new SculptorModuleProcessor());
    processor.registerModuleFactory("phase", () => new PhaseModuleProcessor());
    processor.registerModuleFactory("unmask", () => new UnmaskModuleProcessor());
  }

  // src/effects/ultina-worklet.entry.js
  var MAX_BLOCK = 128;
  var CHANNELS = 2;
  var UltinaWorkletProcessor = class extends AudioWorkletProcessor {
    proc = new UltinaProcessor();
    scratch = [new Float32Array(MAX_BLOCK), new Float32Array(MAX_BLOCK)];
    lastLatencyPosted = -1;
    blockCount = 0;
    metersEnabled = true;
    // Time-stamped parameter events (setParameterAt — automation lanes and
    // offline renders), sorted ascending by `when`. Applied from process()
    // when the render clock reaches them — port messages alone have no
    // timing, so without this queue an offline export would hear every
    // automation point at the moment it was POSTED (the last one wins),
    // collapsing the lane to a constant.
    pendingParams = [];
    constructor(options) {
      super();
      registerCoreModules(this.proc);
      this.proc.prepare({
        sampleRate,
        channelCount: CHANNELS,
        maxBlockSize: MAX_BLOCK,
        qualityMode: 1
        // "mix" — matches the vector harness
      });
      const initial = options?.processorOptions?.params;
      if (initial) this.proc.loadState(initial);
      this.syncGraphFromParams();
      this.postLatency();
      this.port.onmessage = (event) => {
        const msg = event.data;
        if (!msg) return;
        if (msg.type === "params") {
          this.proc.loadState(msg.params);
          this.syncGraphFromParams();
          this.postLatency();
        } else if (msg.type === "param") {
          if (this.pendingParams.length > 0) {
            const now = currentTime;
            this.pendingParams = this.pendingParams.filter(
              (ev) => ev.id !== msg.id || ev.when <= now
            );
          }
          this.proc.setParameter(msg.id, msg.value);
          if (typeof msg.id === "string" && msg.id.endsWith(".enabled")) {
            this.syncGraphFromParams();
            this.postLatency();
          }
        } else if (msg.type === "paramAt") {
          const when = Number(msg.when);
          if (!Number.isFinite(when)) {
            this.proc.setParameter(msg.id, msg.value);
            if (typeof msg.id === "string" && msg.id.endsWith(".enabled")) {
              this.syncGraphFromParams();
            }
            this.postLatency();
            return;
          }
          const q = this.pendingParams;
          let i = q.length;
          while (i > 0 && q[i - 1].when > when) i--;
          q.splice(i, 0, { id: msg.id, value: msg.value, when });
        } else if (msg.type === "reset") {
          this.pendingParams.length = 0;
          this.proc.reset();
        } else if (msg.type === "setMeters") {
          this.metersEnabled = msg.enabled !== false;
          this.proc.setMetersEnabled(this.metersEnabled);
        } else if (msg.type === "dispose") {
          this.proc.dispose();
        }
      };
    }
    /** Mirror the host-facing "<module>.enabled" params into the module graph.
     * The graph boots ALL-DISABLED and nothing else syncs it — without this,
     * the active chain stays empty forever and every module (EQ/Comp/Gate/…)
     * is silent DSP while the UI happily reports it on. */
    syncGraphFromParams() {
      const graph = this.proc.getGraphRuntime();
      for (const type of MODULE_TYPES) {
        graph.setModuleEnabled(type, this.proc.getParameter(`${type}.enabled`) >= 0.5);
      }
    }
    postLatency() {
      const samples = this.proc.getLatencySamples();
      if (samples !== this.lastLatencyPosted) {
        this.lastLatencyPosted = samples;
        this.port.postMessage({ type: "latency", samples });
      }
    }
    /** Apply every queued event whose project time has arrived (the render
     *  clock granularity is one 128-frame quantum ≈ 2.7 ms). */
    applyDueParams(horizon) {
      const q = this.pendingParams;
      if (q.length === 0 || q[0].when > horizon) return;
      let applied = 0;
      let enabledToggled = false;
      while (q.length > 0 && q[0].when <= horizon) {
        const ev = q.shift();
        this.proc.setParameter(ev.id, ev.value);
        if (typeof ev.id === "string" && ev.id.endsWith(".enabled")) {
          enabledToggled = true;
        }
        applied++;
      }
      if (applied > 0) {
        if (enabledToggled) this.syncGraphFromParams();
        this.postLatency();
      }
    }
    process(inputs, outputs) {
      const output = outputs[0];
      if (!output || !output[0] || !output[1]) return true;
      const input = inputs[0];
      const total = output[0].length;
      this.applyDueParams(currentTime + total / sampleRate);
      for (let offset = 0; offset < total; offset += MAX_BLOCK) {
        const frames = Math.min(MAX_BLOCK, total - offset);
        for (let c = 0; c < CHANNELS; c++) {
          const buf = this.scratch[c];
          const inCh = input && input[c];
          if (inCh && inCh.length >= offset + frames) {
            buf.set(inCh.subarray(offset, offset + frames));
          } else if (inCh && inCh.length >= frames) {
            buf.set(inCh.subarray(0, frames));
          } else {
            buf.fill(0, 0, frames);
          }
        }
        this.proc.process(this.scratch, frames);
        for (let c = 0; c < CHANNELS; c++) {
          output[c].set(this.scratch[c].subarray(0, frames), offset);
        }
      }
      if (this.metersEnabled && (this.blockCount++ & 3) === 0) {
        this.port.postMessage({ type: "meters", meters: this.proc.getMeters() });
        this.postLatency();
      }
      return true;
    }
  };
  registerProcessor("ultina-processor", UltinaWorkletProcessor);
})();
