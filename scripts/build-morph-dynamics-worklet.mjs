/**
 * Bundle the MORPH DYNAMICS worklet entry into a single classic-script file
 * served from public/morph-dynamics-worklet.js (dev) and copied into dist/
 * on build. Runs automatically via the predev/prebuild npm hooks.
 */
import { build } from "esbuild";

await build({
  entryPoints: ["src/effects/morph-dynamics-worklet.entry.js"],
  bundle: true,
  format: "iife",
  target: "es2022",
  outfile: "public/morph-dynamics-worklet.js",
  minify: false,
  logLevel: "warning",
  banner: {
    js: "/* MORPH DYNAMICS worklet — generated by scripts/build-morph-dynamics-worklet.mjs from src/effects/morph-dynamics-worklet.entry.js + morph-dynamics-core. Do not edit. */",
  },
});
console.log("[morph-dynamics] worklet bundle written to public/morph-dynamics-worklet.js");
