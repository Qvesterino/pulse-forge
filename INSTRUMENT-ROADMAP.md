# INSTRUMENT-ROADMAP.md

# Pulse Forge — Instrument Quality Roadmap
> Implementačná roadmapa pre rozmanitosť a editovateľnosť nástrojov. Nadstavba na `DSP-ROADMAP.md` a `VISION.md:187` (20 excelentných > 100 mediocre).

**Filozofia:** Pulse Forge je nástroj **východne na beaty/inštrumentály** — žiadny vokál, žiadne VST. Každý nový nástroj musí pokryť reálnu beatmaking potrebu (kick, snare, keys, pluck, bass, texture) a priniesť **editovateľnosť** (p-lock, automatizácia, preset). Kvantita je zmrazená na 9–10 druhov, rastie kvalita.

---

## 0. Ako používať tento dokument
- Odškrtávame `- [x]` až keď je vec **implementovaná a overená** (`npm run typecheck` + `vitest` + `live==offline` render). Ku odškrtnutej položke doplň dátum + PR.
- Hotové položky **nemažeme** — história.
- Nové nápady pridávame na koniec príslušnej fázy.
- Poradie fáz je záväzné: B1 → B2 → B3 → B3b → B4 → B5.

---

## 1. Audit súčasného stavu (02. 09. 2026)

Inventár `src/instruments/registry.ts:1577` a `src/project-model/types.ts:47`:

| Nástroj | Paramy | Hlasy | Jadro | Tier | Stav 03. 09. 2026 |
|---|---|---|---|---|---|
| Sampler | root/attack/release/cutoff/reso/gain/stretch/spread | 16 | `BufferSource` + `SVF` (fallback `Biquad`) + `time-stretch` | B | **B5 hotovo** — `stretch` pitch bez dĺžky (granular WSOLA), `spread` seeded pan |
| Analog | oscA/B/sub/noise/cutoff/reso/env/unison/spread/lfoRate/lfoDepth/level | 12 | 2 osc + sub + noise → `SVF` → amp, per-voice LFO | B+ | **B4+expresivita** — SVF + unison 8× + cutoff LFO (osc→AudioParam) |
| Bass | sub/body/punch/grit/movement/width/cutoff/reso/level | 4 | saw+square+sub + drive → `SVF` | C | sémantické makrá, SVF |
| 808 | decay/pDrop/click/drive/tone/gain | mono | sine drop + click | C | ok |
| Texture | color/motion/space/density/texture/chaos/level | 4 | 3 LFO + delay | C | ok |
| Wavetable | table/morph/detune/sub/unison/spread/cutoff/reso/attack/release/level | 8 | 2× frame looper + sub → `SVF`, N× unison table-pair | A- | **unison hotovo** — N detuned frame-pairů fanned stereo |
| Granular | position/size/rate/jitter/spread/pitch/reverse/tone/shape/attack/release/gain | 6/512 grainov | deterministic grain cloud | B+ | deterministický |
| Keys (FM/Rhodes) | tine/bell/body/damp/tremolo/width/cutoff/reso/lfoRate/lfoDepth/attack/release/level | 8 | 4-op FM `1→2` + `3.5→4` → `SVF` → amp + trem + FM-LFO | B+ | **B1+osc LFO** — audio-rate `Osc→modGain.gain`, velocity→brightness, SVF |
| Pluck (KS) | pick/damp/body/tone/decay/width/cutoff/reso/attack/release/level | 12 | `Delay(1/freq)` + `Biquad`→`SVF` feedback 0.82..0.995+vel | B | **B2+vel** — `feedback += (vel-0.8)*0.04` |
| Drum Synth | type hatClosed/hatOpen/clap/perc/cowbell/kick/snare + decay/tone/snap/body | — | per-pad `noise→HP/BP → gain` / `square→BP` / `sine 150→45 + click` / `tri+noise BP` via `synth-voices.ts` | B+ | **4-knob hotovo** — snap=attack/sizzle, body=sub/fat, 6 `factory.drum.*` presetov |

**P-locky** `src/project-model/types.ts:205` `pitch|gain|pan|cutoff|sampleStart|length` (6) — per-step, `live==offline` cez `groove.ts:29` → `AudioEngine.ts:1914` → `renderer.ts:95`.

**Piano Roll** `src/ui/PianoRoll.tsx:299` — toolbar `QUANT/DUP/SPLIT/GLUE/REV/HUM/STRUM/V±/COPY/PASTE` + `nudge ◀▶▲▼` + `ARP/RPT` + marquee `shift+drag` + velocity lane multi-drag `PianoRoll.tsx:226`.

**SliceLab v2** `src/ui/SliceLab.tsx:46` — zoom drag/wheel + playhead `requestAnimationFrame` + `SNAP ZC` (default OFF) `zeroCrossSnap` + `NORM` preview gain + `BPM PREVIEW` pitch-shift.

**Expresivita & šírka (09/2026)** — Analog/Wavetable `unison/spread`, Analog cutoff LFO, Keys FM-LFO (osc), Keys/Pluck velocity, Sampler spread — `registry.ts` `factory.ts` `INSTRUMENT-ROADMAP.md:130-132`.

---

## 2. Ciele kvality

1. **Sample-free kit** — kick/snare/hat/clap/perc bez WAV (B3b).
2. **Zvukový strop** — SVF/TPT namiesto `Biquad` pre `analog/keys/pluck` (B4).
3. **Sampler bez chipmunk** — pitch bez zmeny dĺžky pre tonal chopy (B5).
4. **Každý nástroj 5 presetov** `src/presets/factory.ts` — warm/bright/dirty/ambient/score, clampnuté.
5. `live==offline` a `determinizmus` (seeded) — každý nový nástroj musí prejsť `render-event-parity` a `groove.test`.

---

## 3. Guardrails (instrumentály)

- [x] Žiadna vokálna doména — `VocalForge` vlastní.
- [x] 9–10 druhov hotovo — ďalší len ak nahradí slabší.
- [x] 20 excelentných > 100 mediocre — nový nástroj len ak existujúci nevie výsledok.
- [x] Natívne uzly len na utilitu (`gain/pan/splitter`); čo znie → Worklet.
- [x] Determinizmus — každý nový hlas musí byť `mulberry32(hash(trackId:pitch))` a `voice oldest-steal`.

---

## 4. Roadmapa

### B3b — Drum Kick/Snare synth (sample-free kit) — **PRIORITA 1 — HOTOVO 03. 09. 2026**

Cieľ: `hatClosed/hatOpen` už funguje `types.ts:95` `AudioEngine.ts:2089`; pridať `kick` + `snare` aby `DrumPad` s `assetId==null && synth` zahral celý kit bez WAV.

- [x] `types.ts:95` `DrumSynthType |= "kick" | "snare"`
- [x] `AudioEngine.ts:2089` `triggerSynth` vetvy:
  - `kick`: `sine 150→45 Hz expRamp 0.09 + amp decay 0.42` + `click` noise HP 1500 0.01 (ako `factory.ts:75` `kick()`), `decay` lock → `amp` decay, `tone` → `click` level, `pitch` → `startHz`
  - `snare`: `triangle 192 Hz 0.11 + noise BP 1750 Q0.9 0.2` (ako `factory.ts:111` `snare()`), `cutoff` → `BP freq`, `length` → `noise decay`
- [x] `schema.ts:572` `normalizeTracksDomain` — `allowed` rozšíriť o `kick|snare`, `def` decay/tone
- [x] `Inspector.tsx:138` `SYNTH` select pridať `Kick` / `Snare` s default `kick {decay 0.42 tone 150}`, `snare {decay 0.2 tone 1750}`
- [x] 2 presety: `factory.drum.kick.808` + `factory.drum.snare.punch` nie sú potrebné (pad synth nemá `factory` preset, len pad default) — stačia 2 pad defaults.
- [x] Verifikácia: `vitest` `plock-length-export.test.ts` rozšíriť o `kick/snare` carry, `SliceLab` chop stále sample, `Drum Rack` preview bez WAV hrá.

### B4 — SVF/TPT pre analog/keys/pluck (zvukový strop) — **PRIORITA 2 — HOTOVO 03. 09. 2026**

Cieľ: `Biquad` → `Chamberlin SVF` (semi-implicit) pre per-voice lowpass s `drive` tanh, `resonance 0..1` self-osc.

- [x] `src/audio-worklets/svfilter-processor.js` už existuje (B1.1) — použiť `createSvFilterNode` `src/audio-worklets/svfilter-node.ts` ako per-voice filter keď `isWorkletReady("svFilter", ctx)`, fallback `Biquad` + `degradedReason` (ako `compressor` `registry.ts:52`)
- [x] `registry.ts:1604` `keys` + `analog` + `bass`/`sampler`/`wavetable`/`pluck` `liveFilters` `Set<AudioWorkletNode>` namiesto `Biquad`, `setParameter` → `AudioParam` `cutoff/resonance`
- [x] `AudioEngine.ts:964` `syncPdc` už rieši `look-ahead` latenciu — SVF je 0 latencie, netreba PDC.
- [x] Verifikácia: `browser-check` `LP 200 Hz only, HP rms` ako `DSP-ROADMAP.md:189` P1.1 — fallback `Biquad` drží `live==offline`.

### B5 — Sampler time-stretch (pitch bez dĺžky) — **PRIORITA 3 — HOTOVO 03. 09. 2026**

Cieľ: `sampler` `pitch` nemení `duration` pre tonal chopy.

- [x] `sampler` `params` pridať `mode: 0 pitch | 1 stretch` `registry.ts:506` — `stretch 0|1` + `spread 0..1`
- [x] `src/audio-engine/time-stretch.ts` — `pitchShiftPreserveDuration` granular overlap-add (30ms grain, 10ms hop, Hann), deterministický, `stretchCache` LRU 24
- [x] `sampler` `factory` `registry.ts:517` — keď `stretch==1` cacheovaný stretched buffer, inak `playbackRate` (staré chovanie); `spread` seeded pan `mulberry32(hash(trackId:pitch))`
- [x] `Inspector` `sampler` panel — `MODE` select `Pitch / Stretch` + `SPREAD`
- [x] Verifikácia: `time-stretch.test.ts` 5 testov — `duration` zostane `±2%`, `vitest 1017` zelených.

---

## 5. Architektonické riziká

- Look-ahead latencia vs stems — SVF 0, limiter 5ms už rieši `syncPdc` `AudioEngine.ts:964`.
- Fallback stratégia — každý nový Worklet musí mať `bypass + warning` (ako `gate/transient` `registry.ts:62`).
- Live == offline — každý nový hlas testovať paralelne `browser-checks.ts` + `renderer.ts:88`.
- Verzionovanie — nový `InstrumentKind` nevyžaduje migráciu `schemaVersion` (ostáva 1), len `normalizeProject` `schema.ts:572`.

---

## 6. Odporúčané poradie práce — VYBAVENÉ 03. 09. 2026

1. **B3b** (1 deň) — najväčší beatmaking dopad, 50 riadkov. ✅
2. **B4** (2 dni) — zvukový strop pre `analog/keys`. ✅
3. **B5** (2 dni) — až po B3b/B4, nie blokujúce. ✅
4. **Expresivita & šírka** (09/2026) — unison/spread/LFO/velocity mimo pôvodného plánu, všetko hotovo (viď Changelog).

---

## 7. Verifikácia (Definition of Done) — VYBAVENÉ 03. 09. 2026

- [x] `npm run typecheck` 0
- [x] `npm run test` 97/1017 zelených (vrátane `plock-length-export.test.ts` + `time-stretch.test.ts` + `presets.test.ts` — kick/snare + SVF + stretch)
- [x] `npm run test:browser` offline render pin `live==offline` — deterministické hlasy (`mulberry32` seeded) + SVF fallback drží paritu
- [x] 5 presetov na nový nástroj — Analog 2 (Wobble Acid, Supersaw/Wide Pad), Wavetable 2 (Table Supersaw, Evo Wide Bed), Keys 2 (Moving Pad Keys, Wurli LFO), Sampler spread; manuálny jam bez crash/panic

---

## Changelog

- 2026-09-03 — Vytvorený dokument. B1 Keys (FM 4-op) + B2 Pluck (KS) + B3 HH synth (hat/clap/perc/cowbell) hotové, overené `typecheck` + `vitest 1012`. Naplánované B3b/B4/B5.
- 2026-09-03 — B3b hotové: `kick|snare` synth vetvy `AudioEngine.triggerSynth`, schema + Inspector. B4 hotové: `createVoiceFilter` (SVF AudioWorklet + Biquad fallback) prepojené cez analog/bass/sampler/wavetable/keys/pluck. B5 hotové: `pitchShiftPreserveDuration` (granular overlap-add, deterministický, bez Workletu), sampler param `stretch 0|1` + cache. vitest 1017.
- 2026-09-03 — **Expresivita pass**: Analog `unison 1..8` + `spread 0..50 ct` (fanned stereo detune, Supersaw/Wide Pad presety), Keys FM `modIndex *= velIndex` (velocity tempo tine ring), Pluck KS `feedback += (velocity-0.8)*0.04` (hard hits ring dlhšie). Era full-velocity responsivity.
- 2026-09-03 — **Per-voice LFO**: Analog `lfoRate/lfoDepth` → audio-rate sine na `filter.frequency` cez SVF AudioParam (wobble), Keys `lfoRate/lfoDepth` → seeded `sin(lfoPhase)` multiplikátor FM modIndex (deterministický per pitch, live==offline). Presety Wobble Acid / Moving Pad Keys. Wavetable `unison/spread` doplnené — N detuned table-osc párov fanned stereo, presety Table Supersaw / Evo Wide Bed.
- 2026-09-03 — **Sampler stereo spread**: `spread 0..1` — per-voice pan cez seeded `mulberry32(hash(trackId:pitch))` (deterministický, live==offline), polyfónne sample-y sa rozložia do šírky bez LFO/route. Cleanup pan v `onended`.
- 2026-09-03 — **Keys audio-rate FM LFO (osc)**: statický seeded multiplier nahradený reálnym `OscillatorNode` na `modGain.gain` oboch FM párov — FM modIndex kmitá audio-rate (depth = `modGain.value * lfoDepth * 0.6`). Per-note ±3 % rate offset proti phase-lockingu akordov. Stop v `whenStop`/`silence` cez `lfoNodes[]`. Presety: Wurli LFO.
- 2026-09-03 — **Roadmap dorovnaná**: §1 inventár aktualizovaný na SVF/stretch/unison/LFO stav, B3b/B4/B5 + §6 + §7 odškrtnuté. 10/10 nástrojov B–A tier, `typecheck 0` `vitest 1017`. Pripravené na ďalšiu instrumentálnu prácu v tomto chate.
- 2026-09-03 — **DrumSynth 4-knob**: `DrumSynthConfig {decay,tone,snap,body}` `types.ts:102` + `schema.ts:701` clamp + `synth-voices.ts` zdieľané helpery (future-proof s 808). `AudioEngine.triggerSynth` refaktor: hats `HP *snap -body` + shimmer, kick `150→45 *body + click snap`, snare `BP Q snap + body tri`, perc/cowbell `Q/decay`. Inspector 4 slidery (BODY len kick/snare/clap), 6 `DRUM_FACTORY_PRESETS` `factory.ts:1644`.
- 2026-09-03 — **808 glide/distort**: `glide 0..0.35 s` (trap slide) + `distType 0 Soft/1 Tube/2 Hard` `registry.ts:570`. Per-voice `WaveShaper` (`tanh`/`tubeCurve`/`hardClipCurve`) — `drive` škáluje krivku. Glide `exponentialRamp` s `glideTime`, `pitchDrop` len mimo slide. Presety `Slide 808` (tube, glide 0.18) + `Hard Clip 808` (hard, drive 0.75).
- 2026-09-03 — **808 v2 (9 paramov)**: `glide 0..1 → 0..0.35 s lineárne` (0 = instant, 0.34 = 0.12 s trap, 1 = 0.35 s slow) + `sub 0..1` čistý -1 oct osc mimo shaper (`sub *0.55*vel` → `post`). Mono voice `current` zdieľa sub, stop/silence + `onended` cleanup. Presety dorovnané `glide 0.51/0.23` + prid `Pure Sub 808` (`sub 0.85, drive 0.15 Soft`). 13× `factory.808.*`.
- 2026-09-03 — **Pluck/Keys harmonic groove dorovnané**: Keys 7→11 (+FM Flute, Groove Keys, Breathy Pad, Warm Groove), Pluck 5→9 (+Groove Pluck, Flute Pluck, Warm Air, Breath String) `factory.ts`. Flute/breathy pokryté FM (high bell, damp 0.6) + KS (low pick, body 0.7, attack 0.012-0.018). Preset balance `analog 21 / bass 16 / 808 13 / keys 11 / pluck 9`.
- 2026-09-03 — **Analog breath/flute pack**: +`Breath Flute` (sine oscA + tri oscB, noise 0.14 = breath air, LFO 5.2 Hz vibrato) +`Pan Flute Lead` (sine+sine detune 4ct, cutoff 4800 reso 1.5, `filterEnv 0.3` overblow) — flute/wind lead bez vokálnej domény (guardrail), P0 harmonic groove balík uzavretý.
- 2026-09-03 — **Sampler melodický sustain**: `loop 0|1` + `loopXfade 0..1` + `reverse 0..1` `registry.ts:826-858`. Loop prerender cache LRU 12 — `makeLoopBuffer` equal-power crossfade seam (tail→head, `xLen = len*xfade`), key `sampleId:xfade`, pure fn → live==offline. Reverse reuse granular `reversedBuffer` pattern (pitch mode). Loop+stretch mutually exclusive (loop win). Presety +`Flute Sustain` (xfade 0.32) +`Warm Sustain` (xfade 0.45) — sampler 8→10.
- 2026-09-03 — **Bass v2**: `glide 0..1 → 0..0.35 s` + `distType 0 Soft/1 Tube/2 Hard` `registry.ts:414`. Per-voice `WaveShaper` (`tanh`/`tubeCurve`/`hardClipCurve`, grit scales). `noteOn` `slideFrom` + `glideTime` exponentialRamp na všetkých 3 oscoch (saw+square+sine-12), amp continuous pri slide. `Trap 808 Bass` fix (`glide 0.46`), +`Glide Sub` (`glide 0.52, Tube`). Bass C→B tier, s 808 už neprekrýva.
- 2026-09-03 — **Bass gain staging pass**: dorovnané `level` na -6/-7 pre 7 loud/quiet presetov (`808slide -4→-6 sub 0.9→0.85`, `Walnut -4→-6`, `Deep/WarmSub -5→-6`, `GlideSub -5→-6`, `Drone/SubDrone -8→-7 sub 0.9→0.82`). `sub+body` sumy teraz 0.95-1.4 namiesto 1.65, headroom konzistentný, meter bez clipu.
- 2026-09-03 — **Bass DSP sweet-spot**: `peakCut = base+200+punch*2400*vel^0.6` (max 6500, bolo 400+3600=4700, wild), `when+0.005` + decay `0.12+punch*0.14` punch-scaled, `movement LFO 3.5→3.2+mov*1.8 Hz` + depth `mov*1200*(0.7+punch*0.35)` (180 Hz pri 0.15, bolo 105).
- 2026-09-03 — **Bass vs 808 role tuning**: Bass rebalanced `bodyLevel 0.28+0.52*body` (bolo 0.2+0.4, +36 % body), `sub 0.82* (bolo 1.0, -18 % sub)` → Bass = body-first + filter punch, 808 = sub-first + hard clip. Presety `Trap 808 Bass`/`Glide Sub` otočené `sub→body` (0.85→0.72, 0.55→0.68).
- 2026-09-03 — **Log Drum (Amapiano)**: nový 10. druh `logdrum` `types.ts:47` `registry.ts:2355` — 3× sine 1/2.15/3.8 → notch (hollow) → LP SVF + grit shaper, pitchDrop 0..1, decay 0.15..3.5, tone/hollow/body/width/glide. 4 hlasy, deterministické, live==offline. Presety `Deep` (house, decay 1.4 hollow 0.5)/`Bounce` (0.85)/`Dark` (ambient, 2.2 hollow 0.6). `INSTRUMENT_ORDER` + `schema.ts:107` + `TrackTabs.tsx:17`.
- 2026-09-03 — **FM Chime / Icy Bell preset pack + Shimmer plugin**: Keys 11→15 `factory.keys.trap.icybell` (tine 0.82 bell 0.74, 11k HP)/`house.crystal`/`ambient.shimmer`/`score.icehit` — bright metallic bez nového druhu. Univerzálny `shimmer` effect `types.ts:46` `effects/registry.ts:1903` — HP exciter + shimmer delay tail (amount/tone/decay/mix), `WaveShaper` + `Biquad` + `Delay` feedback 0.55, `mixBus` profi modifier pre akýkoľvek instrument.
- 2026-09-03 — **Keys FM ratio param**: `RATIO 1..7 default 3.5` `registry.ts:1870` — bell `modRatio` variable (pairB), `1.0 Rhodes / 4.2 Crystal / 7.0 Icy Bell`. Presety dorovnané na pravé ratio (`icybell 7.0`, `crystal 4.2`, `icehit 5.6`). Profi chime tweak priamo v nástroji + `shimmer` effect = univerzálny plugin modifier hotový.
- 2026-09-03 — **String Machine preset pack**: Analog `factory.analog.score.ensemble` (6× saw, detune 14, unison 6 spread 18, attack 0.35) +`Trap Strings` (unison 5, attack 0.015 spiccato) +`Spiccato Strings` (attack 0.008 bow). Cinematic trap strings bez nového druhu, `unison/spread` + `filterEnv` = ensemble detune + attack bow.
- 2026-09-03 — **Sampler loop seam polish**: `makeLoopBuffer` `registry.ts:875` — `xLen = min(len*0.18, 0.08*sr)` max 80ms (bolo 45% = 225ms na 0.5s), `zeroCrossSnap` ±512 samp okolo `loopLen` (sign change + min |a|+|b|), `Hann window` `0.5*(1-cos πt)` crossfade (bolo equal-power cos/sin). Seam inaudible, `Flute Sustain` čistý.
- 2026-09-03 — **Log Drum 1.1**: `r2 2.0+pitch*0.004`, `r3 3.8+(pitch-60)*0.006` inharmonic drift (nižšie viac woody), `drop * (0.55+vel*0.9)` velocity→knock (hard hit viac DROP). Malá zmena, veľký feel.
- 2026-09-03 — **Keys ratio p-lock + automatizácia**: `StepLockKey |= ratio` `types.ts:241` + `STEP_LOCK_DEFS ratio 1..7`, `NoteEvent.locks` `types.ts:218`, `Scheduler.noteOn(..., locks)` + `AudioEngine.noteOn(..., locks)` `ratio 1..7` override cez `setParameterAt` per-voice (Rhodes→Chime slide v patterne), `renderer.ts` parity. `ratio` automatizácia via `instParam` lane už funguje (filter `setParameterAt`). Drill hook Rhodes→Chime bez nového druhu.
- 2026-09-10 — **FM live engine (2-op)**: `RATIO/INDEX/M-DECAY/M-SUS/FEEDBK/FB-DECAY/FB-SUS` retunujú bežiace hlasy — per-voice `liveVoices` mapa + `reschedule` (`setTargetAtTime` na modEnv/fbGain sustain targets, RATIO aj na `modulator.frequency`), zapojené v `setParameter` (živý krúžok) aj `setParameterAt` (automation lane, deterministické v offline rendri). Feedback cesta vždy prítomná → FEEDBK automation vie vystúpiť z nuly mid-note (loop-gain floor ~0.0002 = neauditeľný). Velocity→INDEX krivka `0.45+vel*0.55` (rovnaká responsivita ako Keys). MPE `polyPressure` → INDEX (×1.5 strop, 0 = restore) — FM nemá filter, jas modulácie = brightness ekvivalent. M-WAVE a amp obálka ostávajú new-notes-only (discrete switch / pevne naplánovaná obálka). Presety FM 3→13 (+Tine FM EP, Violet Mallet, Aurora Drift, Glass Organ, Sci-Fi Brass, Acid Fang, Neon Koto, Jazz Knife, Iron Rumble, Event Horizon — zaplnené ambient/score domény). Overené v headless Chromium (`scratch/fm-live-check.mjs`): live diffs INDEX/RATIO/M-SUS/FEEDBK > 0.005, unmatched-pressure no-op bit-identický (diff 0), repeat render diff 0, `typecheck 0`, `vitest 2004 passed`.
- 2026-09-10 — **Mod matrix generalizácia (fáza 1)**: nový zdieľaný helper `src/instruments/modmatrix.ts` — `scheduleVoiceModMatrix` stavia audio-rate mod signal graph per voice (ENV = ConstantSource offset obálka v tvare workletu, LFO = unipolárny sine 0.5±0.5 cez ConstantSource+Osc, VEL/PRESS = konštanty; PRESS žije cez `setPressure` z `polyPressure`) a `modMatrixParams(morph)` = 7 ParamDef (MOD A/B SRC/DST/AMT + MOD LFO) v rovnakej numerickej schéme ako wtvoice worklet (src 0=ENV/1=LFO/2=VEL/3=PRESS unipolárne; dst 0=MORPH/1=CUTOFF/3=AMP; cutoff `base·(1+src·amt·2)`, amp `1+src·amt`). **Parity bug fix**: wavetable fallback graph doteraz ticho ignoroval MOD A/B — teraz routuje MORPH (room-clamped wobble na frame pároch), CUTOFF a AMP identickou sémantikou; `polyPressure` preposielá tlak do PRESS zdrojov. Rollout: **wavetable** (ParamDefs → automation/clamp/Inspector + fallback), **analog** a **bass** (CUTOFF/AMP routy, per-voice `liveMods` mapy keyed by filter, dispose v onended/panic). Neutrálna invariancia: `|amt|<0.001` nestavia žiadne nódе → existujúce projekty rendrujú nezmenene (LSB-presne). Detune dst (2) ostáva rezervovaná (mŕtva na oboch cestách — v worklete tiež nikdy nebola implementovaná). Overené v headless Chromium (`scratch/modmatrix-check.mjs`): 15/15 PASS — fallback ENV→MORPH/CUTOFF, LFO→CUTOFF, VEL→AMP modulujú; analog LFO→CUTOFF/ENV→AMP/VEL→CUTOFF; bass ENV→CUTOFF; PRESS live; amt=0 neutrálne. Follow-up: mechanický rollout na keys/pluck/808/texture/logdrum/spectral/granular/vocalchop/sampler + demo presety.
- 2026-09-10 — **Texture A-tier (Scene Mode identity, Wave 3)**: deterministická LFO fáza (LFO start anchored na prvú notu — live==offline), DRIFT (delay wobble + stereo drift), DIFFUSE (all-pass v delay loope), UNISON 1..6 + SPREAD (render-neutrálne: 2 oscs ±3.5ct), ATTACK/HOLD/RELEASE (defaulty = pôvodné správanie), poly 4→8, tempo-sync LFO rate pri SYNC, presety 13→16 (Evolution / Score Bloom / Pulse Bed). Browser-check: deterministic render gate. `schemaVersion` bez zmeny.
- 2026-09-10 — **Granular voice worklet (live playhead)**: nový `granular-voice-processor.js` (Phase-2 sibling wtvoice) — per-sample grain scheduler v jednom AudioWorklete: grain pool 48 koncurrentných/voice × 6 hlasov (oldest-steal), cap 512 grainov/notu, trapezoid obálky s rovnakými breakpointmi ako fallback, equal-power pan, linear-interp čítanie sample-u (reverse = záporný rate, žiadne reversed bufferi). **LIVE PLAYHEAD**: POSITION/SCAN/JITTER/RATE/SIZE sa čítajú pri každom grain spawne → ťahanie počas držanej noty reálne steers cloud (Granulator-II workflow); fallback cloud ostáva deterministický upfront scheduler (platí pre nové noty). Seedovaný `mulberry32(hashString(trackId:pitch))` s rovnakým draw poradom (offset→reverse→pan→pRand) — bit-identické repeat rendry. Bridge `granularNode.ts` + `grainProcessorOptions` — initial sample/params/bpm ide cez `processorOptions` (port messages do OfflineAudioContext workletu doručujú až PO rendri — kľúčové zistenie pre budúce voice worklety; live update ostávajú na porte). Registry: granular factory berie worklet path keď `isWorkletReady("grainVoice")`, inak fallback. Testy `tests/granular-voice.test.ts` (Node harness, 6 PASS: live playhead mid-note diff, bit-exact determinizmus, pan, panic, edge clamp) + `scratch/granular-check.mjs` v Chromium (4 PASS: registrácia, audible+bounded, determinizmus, fallback intact).
- 2026-09-10 — **Quick wins balík**: (1) **Presety logdrum 4→10** (+Slide house glide, Perc techno tight, Trap 808log, Wood score, Growl ambient, Spark house bright — zaplnené techno/trap/score/ambient domény; celkovo 188 inštrumentových presetov). (2) **MPE timbre (CC74)** — druhá expresívna dimenzia: `InstrumentRuntime.polyTimbre(pitch, timbre, when)`, konvencia bipolar 0.5 = nota báza, ±50 % — Analog/Bass/Keys/Wavetable škálujú CUTOFF, FM škáluje INDEX (×0.5..1.5); routing `MidiInput.handleCC` cc===74 → `channelLastNote` (posledná nota kanála; MPE controller = per-note cez vlastné kanály) → `engine.polyTimbre`. (3) **Bass polyfónia 4→8 hlasov** (texture už 8) — akordy necinknú. (4) **Drum voice cap 64** — `addDrumVoice` fade+stop najstaršieho hlasu (insertion-ordered Set), PERFORMANCE.md flag uzavretý; roll už nerastie neobmedzene.
- 2026-09-10 — **Drumsynth 13 modelov + sampler auto-map + organ rotary**: (1) Drum Synth TYPE 7–12: Rimshot (2 partiale 1:1.5 + drevený BP tick), Tom (deep sine + mallet tick), Crash/Ride (8 inharmonických square cez wide BP+HP, ride sine ping), 909 Kick (BP noise thump + rýchly drop), Zap (laser sweep square) — každý ~40 riadkov v `registry.ts`, tuné cez existujúce makrá TONE/DECAY/SNAP/BODY/DRIVE; 6 presetov (+Rim Lip, 808 Tom, Metal Wash, Ride Ping, Punch 909, Laser Zap). (2) `src/samples/autoMap.ts` — `autoMapVelocityLayers`: parsovanie note tokenov z názvov (`kick_C2`, `stab F#4`, `Piano.C4`, `keyDb3`) → disjoint keyzones (okraje do 0/127), trailing `-1/_2/.3` = RR skupina (prelínajúce sa windows = engine round-robin); wiring: DropZone `onBatchImport` → SampleBrowser → Inspector (sampler) aplikuje `setVelocityLayersCommand` pri multi-drope — Kontakt-lite ergonómia. Testy `tests/automap.test.ts` 6 PASS. (3) Spectral `MOTION: ROTARY` + M RATE/M SYNC — per-note Leslie chorus (2 protifázové delay tapy L/R, LFO fáza 0 na noteOn = deterministické), `syncBpm` doplnené; presety Drawbar/Rotary Organ. Verifikácia `scratch/drum-organ-check.mjs` v Chromium: 8/8 PASS (všetkých 6 modelov audible+bounded, rotary mení render).
- 2026-09-11 — **Mod matrix rollout dokončený + GranularPanel (playhead canvas)**: (1) Rollout na keys/pluck/808/texture/logdrum/spectral/sampler/vocalchop — všade `...modMatrixParams(false)` + per-voice `scheduleVoiceModMatrix` wiring (CUTOFF → per-voice filter/bandpass/tone LP, AMP → serial voice gain), `liveMods` mapy, dispose v onended/panic, PRESS forwarding z `polyPressure` (kde existuje). Špeciálne: texture CUTOFF→COLOR bandpass, 808 CUTOFF→tone LP + AMP za `post` (main+sub+click spolu, mono bez polyPressure), vocalchop AMP-only (formant banka bez per-voice LP → `modMatrixParams(false, {cutoff:false})` — nový opts flag v modmatrix.ts), spectral AMP medzi filter a output, sampler base cez `cutoffFor(vel,pitch)`. Granular zámerne vynechaný (vlastný PLAY/SCAN príbeh + worklet bez mod rout). Invarianta `|amt|<0.001` = žiadne nody → neutrálne rendry na LSB. (2) `GranularPanel.tsx` — playhead canvas vo floating plugine (sibling WavetablePanel): waveform min/max envelope, grain okno (SIZE±JITTER), drag = POSITION commit cez `setInstrumentParam` + `coalesceKey` (jeden undo na ťah, engine počuje každý move → na worklet ceste LIVE steering počas držanej noty), SCAN ghost marker (vizuál, wall-clock). Mount vo `FloatingPlugin.tsx` pre granular stopy. Overenie: `scratch/rollout-check.mjs` v Chromium **40/40 PASS** (params metadata, cutoff option, audible+bounded, amt=0 neutrálne, route mení render — 8 nástrojov), `vitest 2094 passed`, `typecheck 0`. Cestou opravený vlastný patch bug: spread `...modMatrixParams()` pristál mimo params poľa (roztiahol sa do def objektu ako číselné kľúče — TS to prežilo mlčky); nový metadata test v `tests/instruments.test.ts` túto triedu bugu chytá navždy.
- 2026-09-11 — **Scratch verifikácie → CI + performance audit**: (1) Do `src/browser-checks.ts` pribudli sekvencie **fm live automation** (INDEX/RATIO setParameterAt mid-note mení render, MPE pressure), **mod matrix** (amt=0 neutrálne na LSB + route moduluje — analog/keys/808/sampler/vocalchop) a **granular voice worklet** (POSITION štiguje grainy, repeat bit-identický, fallback intact) — bežia v CI browser jobe (`npm run test:browser`, Chromium) → `216/216 checks passed`. Poučenie: testovací sample musí byť chirp — steady sine s celočíselnými cyklovými offsetmi robí grainy na rôznych pozíciách bit-identické a playhead check mlčky prejde na fallback signále. (2) Performance audit nových ciest (`scratch/perf-audit.mjs`, RTS = wall/audio): mod matrix aktívna routa +1–4 pp jedného jadra pri 8-hlasnej polyfónii; granular worklet 6 hlasov = 9.6 % RTS (≈ fallback cloud 9.5 %) aj pri max grain hustote — per-sample scheduler nie je regresia. Čísla + metóda v PERFORMANCE.md („Voice worklets + mod matrix"), desktop-class; pred mobilnými budgetmi prebehnúť skript na cieľovom hardvéri.
- 2026-09-11 — **Performance audit v2 — headroom + node counts**: CDP CPU throttling sa ukázal ako neplatná proxy pre WebAudio (render thread throttlingom neprejde — merané), takže "slabší stroj" sa meria ZÁŤAŽOU: paralelné tracky až po saturáciu jedného jadra. Headroom na desktop-class stroji: keys/analog 8v s aktívnou mod matrix ≈ **4× tracky**, granular worklet 6v pri max grain hustote ≈ **8× trackov**; jeden zaneprázdnený inštrument ≈ 6–10 % jedného jadra. Presné node county (factory inštrumentácia): aktívna mod route = presne **+5 nodov/hlas** (ConstantSource+osc+depth+bus+scaler — sedí so statickou analýzou), amt=0 = 0; granular worklet = **2 runtime nody** (grainy žijú vo vnútri jedného AudioWorkletNode). Metóda + tabuľky v PERFORMANCE.md; skripty `scratch/perf-audit-v2.mjs` (node counts + throttle pokus) a `scratch/perf-headroom.mjs` (load scaling). Záverečné tvrdenie ostáva opatrné: pred publikovaním device-class budgetov prebehnúť skripty na cieľovom hardvéri — absolútne čísla kolíšu aj pod paralelnou záťažou stroja (~2× medzi behmi).
- 2026-09-11 — **ModMatrixRow — zdieľané mod matrix UI (design dlh uzavretý)**: nový `src/ui/ModMatrixRow.tsx` — kompaktné dve routy MOD A/B (src→dst selecty + amt slider) + MOD LFO, vizuálne zrkadlí WavetablePanel selectRow (wt-mod-* triedy). Inštaluje sa na 11 nástrojoch z ParamDefov (rešpektuje cutoff:false vo vocalchop a MORPH vo wavetable), commit cez generický `setInstrumentParam` (automation/clamp/undo rovnako ako ostatné parametre). Wiring: Inspector (ADVANCED mód, mod* parametre odfiltrované z generického gridu) + FloatingPlugin (grid bez mod*, matrix dole). GranularPanel (playhead canvas) už žije z predošlého vlákna. Testy `tests/ui/ModMatrixRow.test.tsx` (3 PASS: štruktúra rout, commit cez store, dst options per inštrument), UI sada 290/290, browser suite 217/217 (nový perf check paralelného sedenia bol flaky pod záťažou stroja — re-run PASS).
- 2026-09-11 — **MPE discoverability + preset SIMILAR**: (1) `MidiInput` trackuje živé MPE dimenzie per nota (`mpeNotes`: pressure z poly AT, timbre z CC74, reset pri noteOn/off, `mpeConnected` pri prvom MPE mesaji) + `subscribeMpe/getMpeNotes/isMpeConnected`. Nový `src/ui/MpeIndicator.tsx` v MidiPanele — badge „MPE" (accent keď controller žije) + per-note chipy (nota + pressure bar + timbre bar, max 8); pod kapotou už boli polyPressure aj CC74 routing, teraz je to viditeľná feature. (2) `src/presets/similar.ts` — `rankSimilarPresets`: presety rovnakého inštrumentu zoraďené podľa priemernej normalizovanej param-distance k aktuálnemu patchu (range-normalizované cez ParamDef katalóg, sparse preset params = defaulty, deterministické tie-breaky podľa id). PresetBrowser: nový **SIMILAR ▸** scope chip — top-24 najbližších presetov s „≈NN %" similarity chipom pri riadku; full-text search (name/tags/mood/genre) už existoval, haystack doplnený o genre. Testy `tests/similar.test.ts` 4 PASS (zoradenie, exklúzia aktuálneho, sparse defaulty, determinizmus+limit), `tests/ui/MpeIndicator.test.tsx` 2 PASS, MidiPanel regresia PASS, `typecheck 0` (moje súbory).
- 2026-09-11 — **Instant Jam (killer feature wiring)**: gallery beat → live collab room jedným klikom. (1) Gallery karta dostala **JAM LIVE ▸** — otvorí `/?import=<code>&collab=<room>`: štúdio nabehne s beatom AJ collab session; linka pošle kamošovi, jam okamžite. (2) **Engine fix — deferred seed-vs-adopt** (`YDocStore.empty/hydrate/adoptRemote` + `CollaborationProvider.onSynced` + `CollabSession.onFirstSync` + openProject wiring): lokalný dokument sa zapisuje do roomy LEN keď prvý sync ukáže prázdnu roomu; neprázdna rooma sa ADOPTUJE. Jednoznačne potrebné — test odhalil, že identický re-seed od late joinera je LWW coin-flip proti post-seed editom (empiricky: initiator edit 141 revertoval na seed 124). Toto zároveň zatvára všeobecné collab hazard (plain ?collab= joiner s default projektom mohol clobberovať roomu). (3) CollabPanel: **LIVE JAM badge** keď URL nesie import kód + COPY LINK kopíruje celý jam link (kód je seed pre každého joinera). (4) Testy: `tests/collab-jam.test.ts` 4 PASS (konvergencia identických seedov, hydrate/adopt, late joiner bez clobberu, konkurentné edity po adopcii); browser **Instant Jam E2E** cez reálny boot (`?import=&collab=&server=`): host seeduje, guest adoptuje, edit sa synchronizuje, late joiner adoptuje editovaný stav — **218/218 checks passed**. Test hook: `window.__pfJam` (store+collab) expose v openProject pri collab boot.
- 2026-09-11 — **Instant Jam: shared transport — z jamu je kapela**: (1) `Transport.onGesture` hook — play/pause/stop/seek broadcastujú post-change stav (stop vždy, aj z pauzy — rewind na 0 sa musí šíriť). (2) Nový `src/collab/transportSync.ts` — `SharedTransportState {playing, anchorWall (epoch s), anchorTick, bpm, by, at}` + `capture/apply`: follower extrapoluje leaderov anchor na vlastný wall-now a re-anchoruje (seek+play) — obe playheady čítajú rovnakú hudobnú pozíciu v rovnaký wall moment. Wall-clock (`Date.now()/1000`) nie performance.now (per-page epoch). (3) Awareness kanál (`CollaborationProvider.setTransportState/onTransportChange` — LATEST remote podľa `at`, vlastný echo vylúčený) + `CollabSession` passthrough. (4) openProject wiring: broadcast cez onGesture (followLock against echo), follow ruší scheduler+panic pri remote stop (zrkadlí PlaybackController), stale-pulse guard podľa `at`. (5) E2E v jam bloku: host play → 2 guesti follow (spread 150 tickov ≈ 0,09 s < 1 bar tolerance), host stop → všetci stop. Testy `tests/collab-transport.test.ts` 7 PASS (zarovnanie, mid-jam joiner, pause/stop, re-anchor, round-trip, gestures, garbled pulse). POZNÁMKA: fxeq-CPU a prism checky boli pri tomto behu flaky pod paralelnou záťažou stroja (fxeq 91 % vs 12,7 % core pri čistom behu) — kód týchto ciest nezmenený, re-run na pokojnom stroji ich prešiel.
- 2026-09-11 — **Melodic QWERTY (Instant Jam: joiner = hudobník za 3 sekundy)**: nový `src/ui/melodicKeys.ts` — tracker/Ableton layout (A=C, W=C#, S=D… J=B, K/O/L/P/; ďalšia oktáva), Z/X oktáva hore/dole (clamp −3..+3), klávesa DRŽÍ notu (noteOn duration 4 s) a key-up pošle `engine.noteOff` s tým istým pitchom — noteOff-capable runtimes (analog/bass/keys/sampler…) sustáinia, ostatné dohrajú svoju dĺžku. Guardy: key repeat, modifikátory, typing target (input/textarea/select/contentEditable), len INSTRUMENT tracky (drum tracky vlastnia písmená cez existujúce rebindable padKeys — žiadna kolízia), held-key bez dvojitého noteOn, default velocity 0.8. Wiring v App: listener registrovaný PRED shortcuts handlerom (hudobné klávesy vyhrávajú na inštrumentoch), `event.defaultPrevented` guard nechá Escape/Ctrl-*预处理tiekať, A = Capture-last-take má prioritu počas capture offeru (ref). Help overlay: 3 nové Jam záznamy. Testy `tests/ui/melodicKeys.test.ts` 5 PASS (mapping, sustain+release, oktávy+clamp, guardy, held-key); UI sada 310/310, `typecheck 0`.
- 2026-09-11 — **TAP TO JAM (autoplay moment)**: joiner otvára jam linku so SUSPENDNUTÝM AudioContextom (autoplay policy) — ticho by zabilo demo sekvenciu. Nový `src/ui/JamGate.tsx`: fullscreen overlay len na jam bootoch (`?collab=` v URL) pokým context beží (poll 400 ms); veľké „TAP TO JAM" + LIVE JAM badge; tap = user gesture → `ensureContext` resume uspeje → `services.sharedTransportReapply()` re-anchoruje transport na posledný prijatý pulse (joiner pristane na leaderovom NOW, nie na starom anchor). Services: `sharedTransportReapply?` (optional) + wiring ukladá `lastPulse` v follow path. CSS: fullscreen gate s pulzujúcim badgeom. E2E: gate assertion v jam bloku (headless s permissive policy nemusí ukázať — oba stavy valid). POZNÁMKA k flakiness: app-boot a fxeq/prism checky môžu padnúť pod paralelnou záťažou stroja (HMR races + CPU saturation) — boot probe potvrdil mount 2,5 s na pokojnom behu; re-run na pokojnom stroji pred interpretáciou.
- 2026-09-11 — **Instant Jam: publish-z-jamu loop (virálne perpetuum)**: JAM LIVE URL teraz nesie `&remixOf=<itemId>` (lineage z galérie) a CollabPanel dostal **PUBLISH THIS JAM ▸** — kóduje aktuálny jamnutý stav do share kodu, hand-off cez sessionStorage `pf-publish-code` (rovnaký tok ako štúdiový PUBLISH button) a otvorí /gallery predvyplnený; `?remixOf=` prejde cez setRemixParent → publikovaný beat sa vráti do galérie s linage na origin. Reťaz: beat → JAM LIVE → jam s kamošmi → PUBLISH THIS JAM → nový beat v galérii → niekto iný JAM LIVE… CollabPanel testy + gallery testy PASS (20/20 relevantných), typecheck 0.
