# Maintenance Audit Progress

> **Nadväzujúci plán:** po dokončení všetkých auditov pokračuj podľa [`RELEASE_ROADMAP.md`](./RELEASE_ROADMAP.md) (release blokery → optimalizácie → release engineering).

Sequential maintenance & hardening pass over **Pulse Forge** (browser DAW).

- Prompts: `prompts/00_CORE` (master operating rules), `prompts/01_COMMON` (audits 01–12), `prompts/03_PULSE_FORGE` (audits P01–P07).
- `MASTER_DAW_HARDENING_PROMPT.md` in `00_CORE` is the shared operating constitution (loop + non-negotiable rules), applied to every audit below — it is not a separate mission.
- Status legend: `[ ]` pending · `[~]` running · `[x]` completed · `[!]` blocked / needs human review.
- Rule: strictly one audit at a time, in the order below. Do not start the next until the current one is complete, verified (typecheck + tests), and recorded here.
- Each audit must end with: defects found/fixed, tests added/strengthened, commands run + outcomes, remaining risks, files changed.

## Execution order

### Phase A — Common audits (01_COMMON)

- [x] 01 — Critical Path Audit (`01_CRITICAL_PATH_AUDIT.md`)
- [x] 02 — Recovery Path Audit (`02_RECOVERY_PATH_AUDIT.md`)
- [x] 03 — Project Persistence & Data Integrity Audit (`03_PROJECT_DATA_INTEGRITY_AUDIT.md`)
- [x] 04 — Undo / Redo Integrity Audit (`04_UNDO_REDO_INTEGRITY_AUDIT.md`)
- [x] 05 — Cross-Component Contract Audit (`05_CROSS_COMPONENT_CONTRACT_AUDIT.md`)
- [x] 06 — State Consistency & Invariant Audit (`06_STATE_INVARIANT_AUDIT.md`)
- [x] 07 — Import / Export Robustness Audit (`07_IMPORT_EXPORT_ROBUSTNESS.md`)
- [x] 08 — Performance & Resource Leak Audit (`08_PERFORMANCE_RESOURCE_LEAK_AUDIT.md`)
- [x] 09 — Dependency Health & Supply Chain Audit (`09_DEPENDENCY_HEALTH_AUDIT.md`)
- [x] 10 — Security Surface Audit (`10_SECURITY_SURFACE_AUDIT.md`)
- [x] 11 — Dead / Suspicious / Legacy Code Audit (`11_DEAD_SUSPICIOUS_CODE_AUDIT.md`)
- [x] 12 — Final Reliability Sweep (`12_FINAL_RELIABILITY_SWEEP.md`)

### Phase B — Pulse Forge domain audits (03_PULSE_FORGE)

- [x] P01 — Audio Scheduler Precision Audit (`01_AUDIO_SCHEDULER_PRECISION_AUDIT.md`)
- [x] P02 — Browser Audio Lifecycle Audit (`02_BROWSER_AUDIO_LIFECYCLE_AUDIT.md`)
- [x] P03 — Sequencer Integrity Audit (`03_SEQUENCER_INTEGRITY_AUDIT.md`)
- [x] P04 — Web Audio Graph Lifecycle Audit (`04_WEB_AUDIO_GRAPH_LIFECYCLE_AUDIT.md`)
- [x] P05 — Beat Engine Stress Audit (`05_BEAT_ENGINE_STRESS_AUDIT.md`)
- [x] P06 — Browser Compatibility Hardening Audit (`06_BROWSER_COMPATIBILITY_HARDENING.md`)
- [x] P07 — Offline Render / Export Accuracy Audit (`07_OFFLINE_RENDER_EXPORT_ACCURACY_AUDIT.md`)

### Final

- [x] Full verification suite: `npm run typecheck` + `npm test` + `npm run build`
- [x] Final summary: fixes, tests added, remaining risks, blocked items (see Results log + session summary)

## Results log

| Audit | Status | Findings | Fixes | Tests added/changed | Verification |
| ----- | ------ | -------- | ----- | ------------------- | ------------ |
| (baseline) | done | `npm run typecheck` clean. Full `npm test`: 1811 passed / 3 failed / 94 skipped — failures pre-exist the audit (2 files incl. `tests/ui/EffectRack.test.tsx`). Full re-run with log capture in progress. | — | — | typecheck ✓ / tests ✗(3) |
| 01 Critical Path | done | **D1:** `openProject` fire-and-forget continuations raced `closeProject` — a late frozen-audio restore re-pointed the shared engine at the closed project's doc (`engine.setProject(store.doc)` in `finally`), and a late Web MIDI permission grant wired handlers into a dead store (engine divergence, phantom MIDI playback). **D2:** `main.tsx openDoc` dropped `openProject` rejections → silent no-op when opening fails. **D3:** flaky lazy-chunk UI test waits (1s default timeout vs dynamic-import latency under suite load; failed 3→2 tests across two baseline runs). | D1: `closed` flag in `openProject` scope guards the restore continuation (unfreeze commands + `finally` engine sync) and the MIDI grant continuation; set first in `closeProject`. D2: rejection surfaces via error screen (`openDoc`); `backToBrowser` still lands in browser on close failure with `console.error`. D3: explicit 10s timeouts on lazy-panel waits. | NEW `tests/services-close-race.test.ts` (4 tests; verified failing 3/4 on pre-fix code by temporary revert); stabilized `tests/ui/EffectRack.test.tsx` + `tests/ui/midi-io.test.tsx` waits. | typecheck ✓; targeted vitest ✓ (39 tests); prettier ✓ |
| 02 Recovery Path | done | **D1:** `ensureContext()` ran `void ctx.resume()` unguarded — rejects NotAllowedError without user gesture (the exact scenario it targets) → unhandled rejection on every suspended ensureContext (visibilitychange, clicks). Every other resume site in the repo already had `.catch`. **D2:** MIDI controller unplugged mid-hold never delivers note-off → Note Repeat roll fires forever with no UI recovery; `reconnectAll` removed the listener but left holds alive. Verified already-hardened (prior passes, no new work): interrupted save/pagehide drain, corrupt+newer-schema records, crash & per-panel ErrorBoundaries, save-error UI retry, missing media (synth fallback, missedAssets, auto-unfreeze, per-entry user-sample restore), scheduler suspend/resume re-anchor, db open retry, collab hardening suite. Residual (not defects): autosave depends on `onDocChanged` completing — engine-sync throw would skip save scheduling (no reachable input found; speculative guard rejected); no snapshot-restore UI (product gap, out of scope). | D1: `.catch(() => {})` on the resume, pinned by a source-grep test. D2: `NoteRepeatController.stopWithPrefix(prefix)` + `MidiInput.onstatechange` releases `midi:`-prefixed holds on `port.state === "disconnected"` (and on port-map removal in `reconnectAll`); UI `pad:` holds unaffected. | `tests/audio-engine-lifecycle.test.ts` +1 (resume catch pinned); `tests/note-repeat.test.ts` +2 (prefix stop semantics, timer prune); `tests/midi/MidiInput.test.ts` +2 (disconnect releases midi holds, connect/no-op keeps them). | typecheck ✓; targeted vitest ✓ (44 tests); prettier ✓ |
| 03 Project Data Integrity | done | No defects found — verified clean. Prior coverage proved partial-write atomicity (two-store transaction, `tx` resolves on `oncomplete`), corrupt/newer-version record isolation, and shape validation, but the required *deep* invariants were untested (existing round-trip tests only checked id/name/bpm/counts). New suite proves over ALL 8 templates: deep `save→load` equality (minus `updatedAt` re-stamp, by design), load-of-load fixed point, `normalizeProject` idempotency, `migrateProject` idempotency + determinism, corruption healing to a stable doc surviving persistence, and JSON-boundary (share-code/export shape) round-trip. One observation, not a defect: persisted `updatedAt` rides inside the doc body via unknown-field passthrough (harmless, tolerated by validation). | None needed. | NEW `tests/persistence/round-trip-integrity.test.ts` (7 tests). | typecheck ✓; targeted vitest ✓ (7 tests) |
| 04 Undo/Redo Integrity | done | No defects found in the history engine itself — verified clean. Store coverage (coalescing windows, redo invalidation, 256-cap with snapshot pruning, replaceDoc watermark, failed-command atomicity, App.tsx ad-hoc whole-doc commands building `oldDoc`/`newDoc` synchronously) already solid; `undo`/`redo` re-entrancy is safe (throw in execute leaves stacks untouched). Gap: `jumpTo` and full-history checkpoint stability were untested. | None needed. | NEW `tests/undo-redo-integrity.test.ts` (4 tests): mixed 9-command history through the real store with deep JSON checkpoints at every step, two full undo/redo cycles, divergent-edit redo invalidation, `jumpTo(i)` for every index, id/reference survival across cycles. | typecheck ✓; targeted vitest ✓ (4 tests) |
| 05 Cross-Component Contract | done | **F1 (HIGH):** collab Y projection dropped `arrangement.audioClips` entirely — every audio clip vanished at collab session open and on first edit. **F2 (HIGH):** `setPadParams` scoped its undo baseline AND collab fast path to the FIRST drum track — pads on later tracks: silent no-op in collab, wrong undo baseline in solo; same bug in `setPadSynth`. **F3 (HIGH):** projection also dropped track `color`, group `collapsed`, pad `color`/`sliceLoop*`/`mod`, instrument `velocityLayers`, MIDI `programMap`/`bankSelect`/`aftertouch*` — all silently reverted in collab. Root cause shared: whitelist drift between read (`yMapTo*`), sync (`*_SCALARS`), and seed (`*ToYMap`) paths. **F4:** scheduler `midiNoteOn/Off` deps discarded the scheduled `when` — hardware got note-on+off back-to-back (drum hits = clicks, sustained notes silent). **F5:** `YDocStore.undoStackLength` stubbed 0/1 → CommandToast + history panel froze after first command in collab. **F6:** `jumpTo` missing on YDocStore → history click-to-jump silently no-oped in collab. **F8:** `YDocStore.replaceDoc` didn't reset `lastSavedAt` (defect 4.2 parity). **F9:** `yToggleStep` hardcoded 0.8 instead of the factory's `defaultVelocity` (latent divergence). Verified OK: targets-catalog ↔ registry clamps, scheduler↔engine signatures/units (incl. ms→s latency offset, velocity ×127), store duck-typing surface beyond the above, save-status machine, services-swap selection reset. Not changed (documented): automation clamp (0..2) vs catalog/UI (0..1.5) for track gain; +0.005s marker/clip scheduling pad asymmetry; dead `"syncing"` enum member. | F1/F3: audioClips mirrored via blob container (presence-preserving), new fields added to ALL THREE paths (`yMapTo*` reads, `*_SCALARS` sync mirrors, `*ToYMap` seed creators — the insert path uses creators, which is why seeds dropped fields). F2: pad resolution + fast-path scan across all drum tracks (labeled `outer` loop). F4: `sendNoteOn/Off(..., delayMs?)` schedules sends (cancellable pending set, cleared on dispose before All-Sound-Off); services converts `when`→delay. F5: real `undoManager.undoStack.length`. F6: `jumpTo` via undo/redo loops. F8: watermark reset ported. F9: velocity threaded through bridge. | Extended `tests/collab-hardening.test.ts` conformance fixture with every previously-dropped field (now pins the adapter). NEW `tests/collab-contract-parity.test.ts` (6 tests): second-drum-track edit/undo/collab-peer + setPadSynth undo; undoStackLength growth; jumpTo landing; replaceDoc watermark. `tests/midi/MidiOutput.test.ts` +3 (delayed send fires, due-now sends immediately, dispose cancels pending). | typecheck ✓; vitest ✓ (166 tests across 11 affected files); prettier ✓ |
| 06 State Invariant | done | **D1 (reachable violation):** group tracks could be FROZEN — the mixer rendered FreezeButton unconditionally on group strips, `freezeTrack` didn't check kind, and the offline renderer includes only the (source-less) group itself → silent buffer persisted to IndexedDB, wrong "FROZEN" UI, no CPU saving; the state survived save/load (normalize never touched `frozen`) and collab. **D2:** `deleteTrack` cleaned automation/LFOs/macros/rows/notes but NOT `arrangement.audioClips` routed at the track — orphaned clips persisted in every save until reload's normalize. Verified clean: duplicate-ID generation across all duplicate/paste/capture/consolidate paths (note ids are pattern-scoped by contract, documented); normalize dedups audioClip/transition ids; stale selections degrade safely (all throwing factories wrapped, filter-based deletes, fallback selection); engine no-ops orphan triggers. | D1: three narrow layers — `freezeTrack` rejects groups with a clear error; `FreezeButton` hides freeze/unfreeze for groups (bounce kept) and early-returns in `handleFreeze`; `normalizeProject` strips `frozen` from group tracks as the migration for already-poisoned projects (children return to live playback, no data loss). D2: `deleteTrack` filters `arrangement.audioClips` by the deleted track (same contract as its automation cleanup); full deletion stays undoable via snapshot delta. | `tests/project-invariants.test.ts` +3 (audioClip orphan cleanup + undo; freezeTrack group rejection; normalize frozen-group migration + idempotency). | typecheck ✓; vitest ✓ (175 tests across 8 affected files incl. round-trip integrity re-run, Mixer/freeze/collab suites); prettier ✓ |
| 07 Import/Export Robustness | done | Probe over all surfaces (project JSON, share/pack/kit/binds/theme codes, audio drop-zone, WAV/MP3/ZIP/video/MIDI). **D1 (data loss):** `ProjectBrowser.handleImportFile` saved under the file's EMBEDDED id — re-importing a file silently overwrote the library project with that id (comment claimed otherwise). **D2 (hang):** `video.ts` export hung forever when the tab was hidden mid-export (rAF loop never completed, `recorder.stop()` never ran) or when MediaRecorder errored (`finished` never settled, no `onerror`). **D3 (crash):** SMF writer spread the whole track payload as call args — dense track (~120k notes) threw `RangeError: Maximum call stack size exceeded` mid-export. **D4 (DoS):** `decodeShareCode` had no token-length or decompressed-size cap — a crafted `?import=` link decompressed + parsed at boot could OOM the tab (embed + gallery remix share the path). **D5 (unhandled rejection):** pack install's `userKits.save` chain had no `.catch` → half-installed pack + unhandled rejection on IndexedDB failure. **D6 (UX):** a corrupted `?import=` link silently fell through to the empty browser; now surfaces an error like the embed player. Verified OK: precise JSON import errors, no partial mutation on failed import, blob-URL revoke at ALL create sites, WAV 4 GB guard, MP3 fail-before-write + yielding, MIDI parser edge cases, pack/kit/theme field hardening, filename sanitization. Documented, not fixed (product-level): no file-size caps on audio/JSON import (needs a product decision on limits), no cancellation for MP3/video exports, `video.ts` records 1s trailing silence for short buffers, MIDI parser tolerates lying track counts (imported as-is, lossless warning omitted). | D1: fresh `uid("project")` before save+open. D2: 500 ms watchdog resolving the render loop from audio-clock time when rAF is dead; `recorder.onerror` rejects; stream tracks stopped in `finally`. D3: copy loop instead of spread (verified: old code throws RangeError on the regression test, new passes). D4: 2 MB token cap + 8 MB decompressed cap before JSON.parse. D5: `.catch` surfaces a kit-save status message. D6: `?import=` present-but-invalid → error screen. | NEW `tests/ui/ProjectBrowser.test.tsx` (fresh-id import); `tests/export/share-code.test.ts` +3 (oversized token null, precomputed 31 KB→9 MB bomb rejected via `tests/fixtures/bomb-token.txt`, legit decode still works); `tests/midi-file.test.ts` +1 (120k-note export; verified failing on old code). | typecheck ✓; vitest ✓ (123 tests across export/midi/ui suites + 10 share-code); prettier ✓ |

> Append one row per completed audit. Keep this file updated continuously so interrupted runs can resume from the first unfinished item.
| 08 Performance & Resource Leak | done | Full lifecycle sweep found ONE actionable defect (F1) plus bookkeeping gaps; all previously-fixed areas re-verified as genuinely fixed. **F1 (MEDIUM):** an in-flight `LiveRecorder` (mic REC in ArrangementPanel; master/track/mic resample in ExportPanel) survived panel unmount AND `closeProject` — mic stream stayed hot (privacy/battery), a `MediaStreamAudioDestinationNode` tap stayed connected to the shared engine, `MediaRecorder` kept pushing Blobs into `chunks` unboundedly. **F3 (LOW):** `engine.missedAssets` never cleared nor reset per project — stale missing-asset ids accumulated forever on the long-lived shared engine and polluted the new project's diagnostics. **F2 (LOW, residual, documented):** `YDocStore` undo history unbounded vs ProjectStore's 256 cap — Yjs `UndoManager` has no safe truncation; collab-session-scoped slow growth, accepted as residual risk (clearing mid-session would silently destroy undo). **F4/F5 (benign):** `capture.ringTrackIds` cleared only on capture-last-take (bounded by distinct track ids); one-shot drag listeners survive mid-drag unmount until pointerup. Verified OK: every interval cleared on teardown (scheduler/note-repeat/ghost/midi-clock/probe/sidechain-fallback/UI panels), every addEventListener paired, every UI `.subscribe` unsubscribed in effect cleanup, engine Maps disposed on doc-diff and context swap, stretchCache LRU-48, meter history capped, capture ring bounded at 2048, all buffer sources tracked or self-cleaning via onended, panic covers both ctx/no-ctx paths, rafLoop last-writer-wins with eviction, worklet ports nulled + teardown-posted in all wrappers (pinned by fx-node-dispose tests), snapshots pruned to 20. | F1: `useEffect(() => () => recRef.cancel(), [])` unmount guards in ArrangementPanel + ExportPanel (project switch unmounts App, so both paths covered). F3: `missedAssets.clear()` in `setProject` on project-id change, alongside `clearStretchCache`. | No new test files — behavioral recorder cleanup needs a real MediaRecorder; covered by existing suites passing. | typecheck ✓; vitest ✓ (24 tests: engine lifecycle, recorder, timeline-rec, fx-node-dispose, rafLoop); prettier ✓ |
| 09 Dependency Health | done | `npm audit` prod: 0 vulnerabilities. Dev chain: `fast-uri` 3.0.0–3.1.5 (4 high advisories: SSRF/host-confusion; transitive via vite-plugin-pwa → workbox-build → ajv) — FIXED by non-breaking `npm audit fix` → 3.1.7; production build + typecheck verified after. Remaining (documented, not fixed): `esbuild <=0.24.2` moderate (GHSA-67mh-4wv8-2f99, dev-server exposure only) nested under `vitest@2.1.9`/`vite-node` — remediation requires vitest 4/5 + vite 8 (two majors; npm audit fix --force would break the tree). Top-level vite 6.4.3 uses patched esbuild 0.25.12, so the PRODUCTION build is clean — exposure is test/dev-server only; planned migration noted. Also identified: peer-range mismatch `@vitest/mocker@2.1.9` wants `vite ^5`, repo on 6.4.3 (pre-existing; vitest 2.1.9 is the newest 2.x; full suite passes in practice) — resolves with the same vitest major bump. Outdated-but-intentional majors documented: react 19, vite 8, vitest 5, typescript 7 (all breaking; not this pass). Verified OK: lockfile consistent (`npm ls` clean tree, all foundational packages deduped to single instances — react/react-dom/typescript/vite/yjs), no unmet peers besides the mocker range note, no native node-gyp modules in prod deps (esbuild/playwright postinstall binaries are the only install scripts), no unexpectedly bundled/unused prod deps (all 9 have imports). | Non-breaking `npm audit fix` only (fast-uri). | None (lockfile change). | typecheck ✓; `npm run build` ✓ (precache 46 entries, size budgets OK); prod audit 0 vulns |
| 10 Security Surface | done | Full-boundary probe. **Highest-value checks came back clean:** NO prototype-pollution gadget (no deep-merge anywhere, no `for...in`, docDelta materializes copies via object-literal computed keys → DefineOwnProperty semantics, all ingestion points clamp/whitelist) and NO XSS sink (zero innerHTML/dangerouslySetInnerHTML/document.write in src, React escaping everywhere, no dynamic hrefs — origin-prefixed only, noopener/noreferrer consistent, colors via `sanitizeColor`). Verified OK: `?import=` never used as navigation; Freesound key stays user-local, queried via URLSearchParams, ids slugged before IndexedDB; localStorage parses guarded + normalized; gallery server body caps, rate limits, strict tag regex, server-side code re-validation with 4 MB cap; embed has NO postMessage/parent messaging at all. **FIXED S1 (medium):** `?collab=x&server=wss://evil` was an unvalidated sink — a crafted link silently relayed the victim's whole project + edits through an attacker websocket (interception/exfil; not exec — remote updates ARE normalized). Now: URL-provided `server=` must parse as ws/wss AND match `location.hostname`, else falls back to the default server; the CollabPanel input still passes explicit servers (deliberate self-hosted relays unaffected). **FIXED S2:** `normalizeProject` violated its never-throws contract — `defaultInstrumentParams` threw TypeError on an unknown/missing instrument kind from hostile remote Y state, killing the collab sync path; now heals unknown kinds to the first valid instrument. **FIXED S3 (hardening):** `sends` contents passed through unsanitized (inert but trust-gap) and the instrument-params backfill copied arbitrary unknown keys with unvalidated values; both now filter to safe keys (`^[A-Za-z0-9_-]{1,64}$`, no `__proto__`/`constructor`/`prototype`) with finite numeric values. | S1: `isAllowedServerUrl` gate in `collabParamsFromSearch`. S2: kind guard + heal in `normalizeTracksDomain`. S3: `sanitizeSends` at all three track-kind spots + safe-key/numeric filter in the params backfill loop. | `tests/collab-session.test.ts` +1 (5 malicious `server=` values fall back to default; same-host override still honored — old permissive assertion replaced, it encoded exactly the attack); `tests/project-model.test.ts` +2 (unknown-instrument heal, setter-trap/non-numeric params+sends dropped with pollution probe on Object.prototype). | typecheck ✓; vitest ✓ (131 tests across 8 files); prettier ✓ |
| 11 Dead / Suspicious Code | done | Repo-wide scan (1886 exported names → 111 zero-reference candidates) + mechanical marker sweeps. **DELETED (7, all verified: zero refs in src/tests/scripts/server/docs, none added by in-flight WIP, no dynamic/string dispatch, safe after deletion):** `collabSessionInfo` (services.ts), `decodeAudioFile` (UserSampleRepository), `assertPatternInvariants` (ai/invariants), `ensurePatternRows` (schema — alias of normalizeProject), `dicePreviewIntent` (intent/dice — superseded by jitteredIntentForSeed path), `clearPadModelCache` (ai/markov — redundant, modelCache has its own MODEL_CACHE_LIMIT eviction), `assetById` (sample-library/manifest). Orphaned imports removed with them. **LEFT UNTOUCHED (classified):** `bounceStemsToAudioClip` — zero callers BUT explicitly documented in EDIT-ROADMAP as the bounce-zone guardrail command (documented intent ≠ high-confidence dead; candidate for a product decision); ~90 remaining candidates inside vendored plugin cores (`src/effects/{fxeq,ultina,ozvena}-core/`) — deliberate contract surface of vendored DSP libraries, deletion risk outweighs value; `SaveUnloadGuards` interface (documentation type); `describe.skipIf(OfflineAudioContext===undefined)` suites — environment-conditional, not disabled; single `TODO: delete clips` in ContextMenu — live placeholder branch in a working menu. Silent catches (328 grep hits) spot-checked during audits 01–10: all carry disposition comments (best-effort restore, already-disconnected nodes) or guard genuinely-optional paths — no new swallowing bugs found. Commented-out production code: none found. TODO/FIXME/HACK markers: exactly one (the ContextMenu placeholder). | 7 dead-code deletions. | None. | typecheck ✓; vitest ✓ (139 tests across ai/intent/dice/persistence/services suites); prettier ✓ |
| 12 Final Reliability Sweep (Phase A close) | done | Broadest safe validation: FULL suite **1854 passed / 0 failed / 94 skipped** (skips = `skipIf(OfflineAudioContext===undefined)` environment-conditional DSP suites; baseline had 3 failures — now zero). Cross-system self-review of the cumulative hardening diff found and fixed ONE escaped interaction: `normalizeProject`'s instrument heal was reverted by the params-merge branch (spread base was `track` instead of `t`) when BOTH instrument+params healed — fixed and pinned by an extended combined-path test (59/59 project-model). Other diff interactions re-verified: sanitizeSends doesn't depend on return-normalization order (charset/number filter only); group auto-unfreeze path consistent with frozen-strip migration; `freezeTrack` single caller (FreezeButton) is gated; `MidiOutput.dispose` cancels pending before All-Sound-Off; video watchdog ends cleanly on both rAF and timer paths (guard against double-resolve); `?server=` gate doesn't affect the CollabPanel's explicit-server flow. **Residual-risk list (Phase A, severity × likelihood):** (1) MED/MED — no import size caps on audio/JSON (needs product decision; OOM on adversarial input only); (2) MED/LOW — collab `?server=` now origin-gated, but the CollabPanel input still accepts arbitrary hosts (deliberate feature; relay trust is by-design); (3) LOW/MED — YDocStore undo history unbounded in long collab sessions (Yjs has no safe truncation); (4) LOW/LOW — automation clamp 0..2 vs catalog/UI 0..1.5 for track gain; (5) LOW/LOW — exports uncancellable (MP3/video); (6) LOW/LOW — esbuild dev-server advisory pending vitest major migration; (7) INFO — `bounceStemsToAudioClip` documented-but-unwired; (8) INFO — MIDI parser tolerates lying track counts. | heal/params spread-base fix in `normalizeTracksDomain`. | `tests/project-model.test.ts` heal test extended to the combined path. | FULL `npm test` ✓ 1854/0/94; typecheck ✓ |
| P01 Audio Scheduler Precision | done | Deep probe (window bookkeeping, automation windows, scene tempo, swing purity, start/stop cycling, clock consistency). **F1 (HIGH) FIXED — ratchet tails dropped at window boundaries:** the parent-level gate (`tick < fromTick → continue`) discarded a ratchet's tail sub-hits whenever the parent sat near a window edge — window A filtered the tail out (≥ toTick), window B rejected the parent (< fromTick); the tail played NOWHERE (ratchet-8 → 1-2 hits). Fix: removed the parent gate — the per-hit `[from,to)` filter is the single source of truth; the existing ±1-step scan already covers every parent whose tail (max < 1 step) can reach the window, seeded draw order unchanged → replay still deterministic, each hit lands in exactly one window. Pinned by a new split test (ratchet-8 at step 7 × 3 split points). **F3 (MED-HIGH) FIXED — FL-slide glide origin broken:** `glideStart = when − (when − slideFrom.tick·spt)` algebraically reduced to `slideFrom.tick·spt` — an identity anchored at tick-0 using doc.bpm; after any pause/seek/scene-tempo the glide clamped to 0 or started after `when` (inaudible/mis-phased). Fix: the scheduler now passes `slideFromWhen = timeAt(slideFrom.tick)+offset` from the SAME transport map that produced `when` (threaded through SchedulerDeps.noteOn → services → engine.noteOn as an optional 9th arg; engine falls back to `when − durationSec` when absent). **F4 (MED) FIXED — panic() left one-shot sources playing:** AudioClips, marker cues and metronome clicks were untracked (not in `voices`) — stop/seek left a multi-bar AudioClip sounding and fired stale cues up to 120 ms later. Fix: bounded `oneShotSources` Set (self-removing onended), hard-stopped in BOTH panic() branches. **F5 (MED) FIXED — pattern launch stuck pending past loop end:** `nextBar` quantization could exceed the loop end; the boundary condition could then never fire on any wrap pass (badge stuck; only stop() committed). Fix: scheduler tracks the effective `activeLoopEnd` per window and commits such a launch AT the loop edge (boundary == wrap). Verified OK: half-open windows without overlap/double-fire (incl. quantized-launch split and loop clamp), single audio clock everywhere, groove RNG purity/determinism, rapid play/stop/play state resets, seek ordering, project-BPM re-anchor safety, scene-automation boundary continuity, modulator stream purity. **Documented residual (architectural, not attempted per no-redesign rule):** F2 scene-tempo seam (transport flips ~95-120 ms early at a non-musical window edge; committed events keep old tempo; live ≠ offline tempo map — proper fix needs tempo-map-split windows like the launch split); F6 automation as wall-clock staircase + stale `setTargetAtTime` override (~75 ms one-window wobble; a cancel-based fix would eat pending modulator steps — worse trade); F7 groove shift can exceed the 1-step scan margin (extreme settings, rare); F8 +5 ms cue offsets vs notes (documented asymmetry); F9 MIDI clock master via bare setInterval (external gear jitter); F10 scene tempo ignored by frozen-align/sync-LFO/clip-duration capture; F11 hygiene (dead `pendingMarkers`, inclusive marker window, pre-roll plays content, accent formula). | F1/F3/F4/F5 as above. | `tests/groove.test.ts` +1 (ratchet tail across 3 split points); `tests/scheduler.test.ts` +1 (launch past loop end commits at wrap). | typecheck ✓; vitest ✓ (141 tests across 9 scheduler/groove/midi suites); prettier ✓ |
| P02 Browser Audio Lifecycle | done | Verified clean — the mission's coverage was already landed by prior passes plus this sequence's audits 01/02: autoplay unlock (AudioUnlock first-gesture + suspended banner on touch devices, cleaned-up listeners/interval); single live AudioContext (engine created once; the only other `new AudioContext()` sites are scope-correct — embed page, per-export video ctx closed in `finally`, dev browser-checks); suspended/resume (scheduler gates windows on live context state, re-anchors to the live playhead on resume — no machine-gun burst; `onstatechange` observers on both ensureContext/useContext paths with stale-context guards; `ensureContext` resume rejection swallowed — audit 02 fix); visibility hide/show (flush + resume/resync); sleep/wake (same resume path); unload (pagehide + beforeunload guards + close-race `closed` flag — audit 01 fix); worklet init failure (loader never rejects, factories degrade to bypass/fallback, rebuild queued on running); mic permission (explicit surfaced errors: unavailable / denied / MediaRecorder ctor failure); device changes (browser-managed output switch; Web MIDI device changes release stuck holds — audit 02 fix). No duplicated engines, no zombie schedulers (scheduler interval single-instance, cleared on stop/close), transport state survives lifecycle edges (verified by scheduler/transport/pwa/broadcast-channel/save-lifecycle/close-race suites — 34 tests green). Residual documented: Chrome's audible-tab throttling assumption (environmental; no code guard possible for extreme throttling). | None needed this pass. | None. | vitest ✓ (34 lifecycle tests) |
| P03 Sequencer Integrity | done | Probe over aggressive note/pattern editing. **S1 (HIGH) FIXED — note commands resolved the ACTIVE pattern at apply time, not the edited one:** every note command (add/move/resize/velocity/delete×2/quantize/duplicate/split/glue/nudge/setVelocities — 13 factories, 23 call sites) ran its apply/undo against `doc.activePatternId` resolved INSIDE execute/undo. Undo stacks outlive pattern switches, so an undo after a switch was a silent no-op (filter/map miss) — or, for whole-list undos (quantize/nudge/split/glue), a WHOLESALE replacement of the wrong pattern's note list. Fix: each factory pins `patternId = doc.activePatternId` at factory time; `withTrackNotes` targets the pinned id (missing pinned pattern → no-op, never throw). Real-world trigger includes collab (remote-origin pattern switches don't enter the local undo stack). **S2 (LOW/MED) FIXED — group-solo divergence:** `addToGroup` didn't inherit the group's solo — the engine keeps children of a soloed group audible (group gain) while the scheduler gates content on the track's OWN solo → live preview sounded but playback content was silent. Joining a soloed group now inherits solo (restored on undo). **S3 (LOW) FIXED — `setPatternLength(NaN)`** crashed on `new Array(NaN)`; now rejected with an explicit error. **S4 (LOW) FIXED — stale stepMeta resurrection:** shrink 32→16 then grow 16→32 resurrected old probability/ratchet/p-locks on zeroed steps; `setPatternLength` now prunes meta beyond the new length. **S5 (LOW) FIXED — `setStepVelocity` missing-row crash** (rows only guaranteed at normalize boundaries) + non-integer/negative stepIndex guard. Verified OK: overlapping notes legal by design; fresh note ids on all clone/duplicate paths; velocity clamps everywhere; stepCount can't reach 0/negative; mute+solo precedence consistent (mute wins) engine↔scheduler; scheduler re-reads doc per window (deleted tracks/notes safe mid-window); no panic at loop wrap → long notes ring their full tail with proper note-off; row-length mismatch heals at normalize (idempotently). Documented, not changed: shrink silently drops notes extending past the new length (consistent with the load filter — product decision territory); add/move/resize lack factory-time bounds guards (defense-in-depth — UI clamps, normalize heals at load). | S1–S5 as above. | `tests/undo-redo-integrity.test.ts` +2 (addNote-undo after switch; quantize undo with remote-origin switch corrupting B — models the real collab path). | typecheck ✓; vitest ✓ (155 tests across 6 suites); prettier ✓ |
| P04 Web Audio Graph Lifecycle | done | Verified clean — the mission's coverage substantially re-verified: audit 08's leak sweep had already cleared node/Map disposal on doc-diff + context swap, voice/preview tracking with self-cleaning `onended`, worklet port teardown (pinned by fx-node-dispose tests), PDC latency subscription cleanup in all dispose paths, send-map staleness (syncSends prunes dead returns), and object-URL hygiene. This pass additionally checked the P04-specific amplification vectors: FX chain rebuilds are disconnect-FIRST (`input.disconnect()` + `pdcDelay.disconnect()` before rewiring — no duplicated connections, no reconnect gain build-up); the PDC delay node is created once per chain and reused; the master chain disconnects its analyser/splitter before rebuild (no duplicated master paths); sidechain feeds to deleted source tracks are nulled via `setSidechainInput(null)` on the next rebuild (bounded, self-healing stale reference). Effect chains do not survive track deletion (disposeTrackNodes on doc diff). One new tracking addition from P01 (oneShotSources Set) is bounded and self-cleaning. Residual documented: a runtime's sidechain JS reference to a deleted source track's input node is retained until that effect's next rebuild (single disconnected node, negligible). | None needed this pass. | None. | vitest ✓ (56 passed / 42 env-skipped across dispose/latency/lifecycle/worklets/effects/freeze/rack suites) |
| P05 Beat Engine Stress | done | Existing stress coverage was structural (doc shape, 256-undo, plain iteration) — extended with REAL scheduler-core stress: 24 drum tracks × 16 pads × 64-step dense pattern (~24.5k hits/pattern) with ratchet-8 + probability + microtiming + swing + humanize. New tests: (1) per-window scheduling budget — 20 consecutive ~119-tick windows stay under a CI-safe 300 ms ceiling (pathological algorithms would blow it by orders of magnitude); (2) window-split losslessness under a mute/solo storm (every 5th track soloed, every 11th muted) with uneven 119-tick windows — merged halves must equal the whole exactly; (3) identical-state replay determinism across window phases. All pass. Also re-verified existing gates green: stress suite, intent-engine performance gates, fxeq/ozvena/ultina soak + performance suites (full-suite runs). No pathological algorithm found in the audio-critical path; the P01 ratchet fix actually REMOVED work near window edges (no parent rejection pass). Residual documented: real-CPU/GC observation is environment-bound (covered by the soak suites' heap-growth assertions; browser profiling out of scope for jsdom CI). | None — coverage added, no defect found. | `tests/stress.test.ts` +3 dense-scheduler tests. | vitest ✓ (9 stress tests incl. 3 new) |
| P06 Browser Compatibility | done | Verified clean. Feature detection is uniform and UA-hack-free: `typeof` guards for AudioContext (engine throws a clear error only when truly absent), OfflineAudioContext (renderer degrades with a thrown precise error; user-sample restore decoder), MediaRecorder + `isTypeSupported` codec walk for video (UI disables the option via `canExportVideo()`), `navigator.requestMIDIAccess`, `audioWorklet` presence (loader no-ops to fallback factories), `navigator.mediaDevices.getUserMedia` (surfaced messages). No user-agent sniffing anywhere (browser names appear only in comments); InstallPrompt self-hides when `beforeinstallprompt` never fires. Workers: both `new Worker` sites (IR generator, onset detector) try/catch with synchronous fallbacks. No SharedArrayBuffer/cross-origin-isolation dependencies. Storage: IndexedDB behind fail-fast open + catch-all repos; localStorage JSON parses guarded + normalized; file APIs via FileReader/arrayBuffer inside try/catch. Offline rendering guarded for missing OfflineAudioContext. Codec decode relies on the browser's `decodeAudioData` with per-file surfaced failures. Autoplay/lifecycle differences handled via the gesture unlock + state observers (P02). Residual documented: Safari MediaRecorder codec availability is capability-checked, not version-guaranteed (by design — degrade, don't claim); jsdom CI can't exercise real codec paths (browser-checks.ts covers them in real Chromium runs). | None needed this pass. | None. | vitest ✓ (browser-compat/embed/pwa/ExportPanel — 14 tests) |
| P07 Offline Render / Export Accuracy | done | Full live-vs-export parity probe. **X1 (HIGHEST) FIXED — frozen tracks exported WRONG:** offline never populates `frozenBuffers` (live-transport-only), so frozen INSTRUMENT tracks dropped every note (engine `instruments` never populated for frozen tracks → silent track in every export) and frozen DRUM tracks rendered DRY (no FX — the FX was baked into the never-used frozen buffer). Fix: `renderProject` renders the UNFROZEN document (`unfreezeDoc`) — the frozen buffer was itself rendered from the track's real chain, so live-chain rendering reproduces the correct export content (and correctly includes AudioClips on frozen tracks, which live's frozen path skips). **X2 FIXED — slide glide origin offline:** renderer didn't pass `slideFromWhen` → engine fell back to `when − durationSec` (mid-clip glide divergence vs live); now passes `timeAt(slideFrom.tick)` from the SAME tempo map as live (completes the P01 F3 fix). **X3 FIXED — per-track export of a GROUP track produced silent WAVs** (children don't match the stem filter); ExportPanel now skips group tracks. Verified MATCH: master chain incl. tape/clipper/limiter worklets offline (per-context worklet preload), mute/solo via soloAudibility, sends/returns wet levels (stems sum to mix), automation via tempo-map `timeAt`, random S&H/step modulators (pure, tick-indexed — offline CAN equal live), probability/humanize pass numbering aligned with a live playthrough from tick 0, repeat exports byte-identical, 2 s tails, empty-project fallback, count-in/pre-roll correctly excluded, 16-bit dither deterministic, stems include only returns with send>0. **Documented residuals (parity features / product decisions, not small fixes):** sceneAutomation lanes not rendered offline; scene intensity pinned at default 0.7 (curves have no offline equivalent); marker cue one-shots absent from master WAV (bounce/scorepack deliberate — master panel semantics need a product call); bounce-zone head-trim at project tempo ignoring stretchRate; fxeq "random" LFO uses Math.random at audio runtime (vendored worklet bundle + main-thread mirror — seeding is a DSP change); 32-bit float WAV hard-clips at ±1.0 (16/24-bit soft-knee); master tape/limiter worklet-less fallback is a different algorithm (only in broken environments); window-edge family from P01 (note durations/automation phase/clip duration near tempo flips). | X1 `unfreezeDoc` + render through it; X2 `slideFromWhen` in renderer; X3 group filter in ExportPanel. | `tests/render-event-parity.test.ts` +1 (unfreezeDoc strips frozen, preserves others, idempotent, no-op fast path). | typecheck ✓; vitest ✓ (68 render/export/golden/stems/bounce/wav/freeze tests + 3 parity); prettier ✓ |
| FINAL verification | done | `npm run typecheck` ✓ · FULL `npm test` ✓ **186 files / 1866 passed / 0 failed / 94 skipped** (skips = env-conditional OfflineAudioContext DSP suites; baseline was 1811 passed / 3 FAILED) · `npm run build` ✓ (precache 46 entries, entry 987 KB / 995 budget, total JS 1634 KB / 2400 budget) · prod `npm audit` 0 vulnerabilities. All 19 audits `[x]`. | — | +55 net new tests across 11 new/extended files | all green |

> Housekeeping: `full-sweep.log` / `final-sweep.log` / `baseline-tests.log` are transient verification artifacts and can be deleted.
